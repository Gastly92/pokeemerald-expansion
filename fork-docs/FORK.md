# Fork changes

This is a fork of [RHH's `pokeemerald-expansion`](https://github.com/rh-hideout/pokeemerald-expansion).
It tracks upstream and layers a set of custom features on top to build a
**standalone single-player romhack** centered on quality-of-life improvements to
the Battle Frontier facilities, growing into a fuller game over time. See
[`CLAUDE.md`](../CLAUDE.md) for the conventions and the upstream-sync process.

This file is an **index**, not a spec: each feature is gated by a config flag
whose comment in `include/config/*.h` is the source of truth for its exact
behavior. The table records what the flag comment can't — status, known
limitations, and where to look.

## Features

| Feature | Flag(s) | Where | Status | Notes |
|---|---|---|---|---|
| Boot straight to the main menu | `SKIP_TITLE_SEQUENCE` | `config/fork.h` | ✅ | Skips copyright/intro/title. RHH intro still plays if `EXPANSION_INTRO`. |
| Trim Prof. Birch's new-game intro | `SKIP_BIRCH_SPEECH` | `config/fork.h` | ✅ | Keeps look + name selection, drops the monologue. |
| Gender-neutral text | `GENDER_NEUTRAL_TEXT` | `config/fork.h` | ✅ | Replaces gendered wording with neutral text. Currently the new-game look picker; the flag can extend to other gendered strings later. Text-only; selection unchanged. |
| Start a new game at the Battle Frontier | `START_AT_BATTLE_FRONTIER` | `config/fork.h` | ✅ | Warps to the Frontier dock as if first arriving. See `SetBattleFrontierFirstArrivalState()` in `src/new_game.c`. No effect on FRLG. Also sets `FLAG_SYS_POKEMON_GET` up front — a new game gives no starter, so without it the overworld START menu would hide the POKéMON (party) option; the Battle Tower needs the player's own team (added via the debug menu), so the party screen is unlocked from the start. |
| Battle Frontier facility guide | _(no flag; part of the Frontier-start intro)_ | `BattleFrontier_OutsideWest`/`BattleFrontier_ReceptionGate` scripts, `VAR_FRONTIER_FACILITY_GUIDE`, `FLAG_HIDE_FRONTIER_FACILITY_GUIDE`/`_GREETER` | ✅ | Continues the redirected Scott intro: when the player steps out of the Reception Gate (the gate's Scott scene sets `VAR_FRONTIER_FACILITY_GUIDE = 1` and reveals a greeter), an `OnFrame` trigger has the greeter walk the player from the gate exit (26, 61) to the Battle Factory door (11, 39), then the greeter is swapped (`removeobject` + `addobject`) for a permanent directions-giver that stands by the entrance and, on interaction, points to any facility via a two-level multichoice menu (`MULTI_FRONTIER_GUIDE_AREAS`/`_WEST`/`_EAST`). The escort uses two hand-authored, BFS-derived movement paths (greeter leads one tile ahead, player follows directly behind in lockstep — scripted `applymovement` doesn't object-collide, so no spacing delay is needed); paths avoid every NPC home tile and `lockall` freezes wanderers for the walk. Exchange-shop directions are not yet listed (planned). |
| New-game option defaults | `NEW_GAME_TEXT_SPEED`, `NEW_GAME_BATTLE_STYLE` | `config/fork.h` | ✅ | Sets the initial text speed (default Fast) and battle style (default Set) for a fresh save in `SetDefaultOptions()`. Value-defines using `OPTIONS_*` constants; the player can still change them in Options. |
| Start with battle-gimmick items | `START_WITH_BATTLE_GIMMICK_ITEMS` | `config/fork.h` | ⚠️ partial | New games give Mega Ring / Z-Power Ring / Dynamax Band / Tera Orb in the bag (`GiveStartingBattleGimmickItems()` in `src/new_game.c`). Mega/Z-Move/Ultra Burst work right away. **Dynamax & Tera are intentionally left disabled** (`B_FLAG_DYNAMAX_BATTLE` / `B_FLAG_TERA_ORB_*` in `config/battle.h` stay `0`) — the player holds the items but can't use those gimmicks until a planned player-facing toggle wires those flags to real flags and sets them. |
| Debug menus always on | `DEBUG_OVERWORLD_MENU`, `DEBUG_BATTLE_MENU` | `config/debug.h` | ✅ | Overworld menu (hold R + Start), battle menu (Select). |
| 6v6 Battle Frontier | `B_FRONTIER_PARTY_SIZE_6V6` | `config/frontier.h` | ⚠️ partial | Frontier-wide (not just the Factory): singles **and doubles** use full 6-mon teams (doubles: two active, four in reserve — `FRONTIER_DOUBLES_PARTY_SIZE` is also 6 under the flag, kept ≤ `FRONTIER_PARTY_SIZE` so `MAX_FRONTIER_PARTY_SIZE` and the saveblock party arrays don't grow); Factory auto-rents the team (rental select skipped, always max IVs). The **party picker now supports up to 6** (vanilla only defined the FIRST..FOURTH selection-order labels, so the 5th/6th pick printed "ABLE"/"NOT ABLE"): added `PARTYBOX_DESC_FIFTH/SIXTH` + `gText_Fifth_PM`/`gText_Sixth_PM`, and `party_menu.c` maps the order through `sFrontierPartyOrderDescIds[]` instead of the vanilla contiguous `i + PARTYBOX_DESC_FIRST`. The lobby challenge-explanation and rules-board text say "a full team of six" instead of the vanilla "three" (assembler `.if` in `BattleFactoryLobby/scripts.inc`); the Tower lobby's "select the … POKéMON" prompts likewise say "six". **The flag is defined as `1`/`0`, not `TRUE`/`FALSE`** — unlike the other `B_FRONTIER_*` flags it feeds a numeric constant (`FRONTIER_PARTY_SIZE`/`FRONTIER_DOUBLES_PARTY_SIZE`) computed with `#if` that **map scripts read as a value** (`setvar VAR_0x8005, FRONTIER_PARTY_SIZE` in the Tower lobby); the script cpp pass leaves `TRUE` undefined, so `#if TRUE`→`#if 0` would have silently given scripts the vanilla 3 while C saw 6 (the picker then only let you choose 3). A literal `1` makes `#if` agree across the C and script cpp passes (`.if` still works). **Battle Dome is not yet generalized to 6** — its coordinate tables are stubbed so it builds, but the 6-mon layout is incorrect. |
| 6v6 + endless Battle Tower | `B_FRONTIER_PARTY_SIZE_6V6`, `B_FRONTIER_ENDLESS`, `B_FRONTIER_TOWER_DISABLE_MULTI_LINK` | `config/frontier.h`, `src/battle_tower.c`, `src/battle_frontier.c`, `BattleTowerLobby`/`BattleTowerBattleRoom` scripts | ⚠️ partial | Brings the Factory's 6v6 + endless treatment to the **Battle Tower** for Singles and Doubles. **Picks 6** (Singles/Doubles both select a full team via the picker fix above). **Endless:** the battle-room loop never ends after a fixed number of wins — **BP is awarded after every win** (per-win scaling shared with the Factory in `GiveBattlePoints`, the Tower branch reading `towerWinStreaks`), the **Salon Maiden (Anabel)** appears at the 50th/100th win (`gFrontierBrainInfo[TOWER].streakAppearances` `{50,100,50,1}` under the flag) and, once beaten, awards BP = the win number and the streak **continues** instead of warping out. **The opponent draw is keyed to the per-mode win streak, never `curChallengeBattleNum`** (`SetNextTowerOpponent`/`SetTowerBattleWon` derive `battleNum = winStreak % FRONTIER_STAGES_PER_CHALLENGE`); this is the independence mechanism — it lets a rested **Singles** run and a rested **Doubles** run continue **independently** (each mode keeps its own streak + active flag), **not** a difficulty ramp. There is **no scaling**: the opponents' mons are drawn uniformly from the competitive roster (see below). **"Rest"** saves and returns to the lobby (no reboot) with the player's real party and the streak left active; the lobby no longer auto-resumes a paused Tower challenge, so the player can leave and, on returning, talk to the attendant and **choose a fresh team** (`tower_init` preserves an active streak) to continue. **General opponents are drawn from the competitive roster** (`frontier_extended_mons.c`) like the Factory, via the shared `GetRandomFrontierExtendedMonId()` (uniform, format-aware) wired into `FillTrainerParty` for the Tower — always max IVs; the vanilla per-trainer themed `monSet`/high-tier gate is bypassed for the Tower only. **Each general opponent holds exactly one Mega Stone** (the player hand-picks their team, so opponents always bring a Mega): `FillTrainerParty` reserves a random slot for a guaranteed Mega holder, forbids megas elsewhere, and applies the shared gimmick rule (`frontier_draft.c`, caps Z-Crystals at one). **The Salon Maiden (Anabel) fields a fork-owned static competitive team** — modelled on her vanilla set (psychic specialist + legendary beasts + Lati twins), full-six, with separate Silver (50th win) and tougher Gold (100th win) variants (`battle_tower_trainers.c`, routed via `CreateFrontierBrainPokemon`). **Gym-leader bosses:** every set's last win (10th/20th/… that isn't a 50/100 Brain mark) is a real Hoenn gym leader — own overworld sprite, battle pic, name and class — fielding an authored competitive team with one legendary-tier mon and one Mega; chosen at **random**, the roster (`sTowerBosses[]`, 8 Hoenn leaders) is extensible (Elite Four/Champions later). A fork `BOSS_TRAINER` id range above `TRAINER_PLAYER` is recognized by branches in `FillTrainerParty`/`SetBattleFacilityTrainerGfxId`/`GetFrontierTrainerName`/`GetFrontierTrainerFrontSpriteId`/`GetFrontierOpponentClass`/`GetOpponentIntroSpeech`/`CopyFrontierTrainerText`. **Multi & Link Multi are disabled** (`B_FRONTIER_TOWER_DISABLE_MULTI_LINK`): those two attendants show a "not available" message; the code/scripts are kept intact behind the flag. **General opponents are tier-gated** like the Factory: no legendaries or mythicals, and at most one pseudo-legendary per team (`TierRejectsCandidate` from `frontier_draft.c`, every slot `TIER_NORMAL`); bosses/Brain return earlier so their fixed legendaries are unaffected. Known limitations: **hard AI** is still Factory-only; bosses are **pure-random** (no immediate-repeat avoidance — a mutable static would land in `.data`, which the ld script doesn't place for fork files); the boss/brain **battle music** is the default frontier track; resting *exactly* on a Brain-threshold win (49/99) can skip that Brain appearance; the shared `trainerIds` dedup list can briefly hold the other mode's trainers after a Singles↔Doubles switch; **gym-leader bosses *glide* in rather than walk** — their overworld sheets are only 3 frames (one pose per facing, no step cycle), unlike the Salon Maiden's full 9-frame walk sheet, so the approach to the battle spot slides; this is a sprite-art limitation (a real walk cycle needs ~6 new authored frames per leader, ×8), not a logic bug, left as-is for now (see the boss gfx hook in `frontier_util.c`). |
| Frontier battles forced to Lv100 | `B_FRONTIER_FORCE_LVL_100` | `config/frontier.h` | ✅ | Factory **and Battle Tower** force Open Level mode (both teams at `MAX_LEVEL`): the lobby skips the Lv50/Open multichoice entirely and the attendant just proceeds (assembler `.if` forces `VAR_RESULT = FRONTIER_LVL_OPEN` before the eligibility check). The Reception Gate "rules" guide NPC also hides the now-irrelevant level-mode entries (TWO STYLES / LV. 50 / OPEN LEVEL), using the reduced `MULTI_FRONTIER_RULES_LV100` list. See known quirks below. |
| Endless Frontier challenge | `B_FRONTIER_ENDLESS` (+ `FRONTIER_STAGES_PER_CHALLENGE`) | `config/frontier.h`, `constants/battle_frontier.h` | ⚠️ partial | A challenge no longer ends after a set number of wins — the player keeps battling indefinitely. A "set" is `FRONTIER_STAGES_PER_CHALLENGE` (bumped 7 → 10) wins. **BP awarded after every win** (by the attendant in the pre-battle room), scaling per set (2/win for the first set, 4 for the next, 6 for the next, …). **"Rest"** now saves + restores the party + returns to the lobby (no reboot), keeping the streak active; the attendant offers to resume. In-challenge / resume detection uses the win streak, not `curChallengeBattleNum`/`challengePaused`. Because a singles and a doubles challenge can both be rested at once, the rental team (the shared `rentalMons`) is snapshotted per battle mode into `frontier.factoryRentedMonsBackup` on rest and restored on resume, so the two modes keep separate teams. **Frontier Brain** moved to the 50th win (silver) / 100th win (gold), awarding BP = the current win number (50 / 100). Gated by `B_FRONTIER_ENDLESS` (C `#if` + script `.if`, vanilla preserved in each `.else`). **Lobby/pre-battle text is brought in line with the endless flow:** the rules board drops the "can't swap with the seventh TRAINER" note (there is no fixed final opponent), and the pre-battle-room attendant's cryptic opponent hints (type — "favors Fire" — and style — "slow and steady") are skipped entirely (the foe team is still generated; the unhelpful hint text just isn't shown), with the lobby NPC that grumbled about deciphering them reworded to match. The Battle Factory is the first facility wired up; the rest follow in later passes. |
| Frontier max PP | `B_FRONTIER_MAX_PP` | `config/frontier.h` | ✅ | Maxes PP Up bonus on all four move slots in `CreateFacilityMon`, the shared facility mon builder — so it covers every facility: Battle Factory (rentals, opponents, Brain), Tower/Dome/Palace/Arena/Pyramid trainers, Battle Tent, and multi-battle partners. |
| Frontier max IVs | `B_FRONTIER_MAX_IVS` | `config/frontier.h` | ⚠️ partial | Forces 31 IVs in every stat via `GetFactoryMonFixedIV` (the one Factory IV source), so rentals, opponents, and the Frontier Brain are all maxed instead of the vanilla per-challenge ramp. Factory-only for now; other facilities' IV ramps live elsewhere. |
| Frontier hard AI | `B_FRONTIER_HARD_AI` (+ `B_FRONTIER_HARD_AI_FLAGS`) | `config/frontier.h` | ⚠️ partial | Every Factory opponent and the Frontier Brain use the strongest AI preset (default `AI_FLAG_SMART_TRAINER` — basic AI + OMNISCIENT + smart switching/mon choices + PP-stall prevention + smart Tera) instead of the vanilla per-challenge scaling. Gated in `GetAiScriptsInBattleFactory`; Battle Tent keeps no AI. Tune via `B_FRONTIER_HARD_AI_FLAGS`. Factory-only for now. |
| Species-aware AI overrides | `AI_FLAG_SMART_SPECIES_LOGIC` | `src/fork/battle_ai_species_overrides.c`, `include/fork/battle_ai_species_overrides.h` | ⚠️ partial | Opt-in AI flag (bit 34, defined in the fork-owned header) that patches spots where the generic AI misplays specific mons; OR'd into `B_FRONTIER_HARD_AI_FLAGS` so the Frontier gets it. All logic lives in `src/fork/battle_ai_species_overrides.c`, reached from three small additive hooks (each gated by the flag): a switch veto in `ShouldSwitch` (`src/battle_ai_switch.c`), a move-score entry at `sBattleAiFuncTable[34]` and a Mega-defer in `ReconsiderGimmick` (`src/battle_ai_main.c`). Covers: **Palafin** — `AI_ShouldKeepTransformedFormIn` stops the AI voluntarily pivoting out a transformed Hero form (it'll still flee a guaranteed OHKO it can't win); **Sharpedo** — `AI_ShouldBankSpeedBoostBeforeMega` makes a pre-Mega Speed Boost mon Protect on its first turn to bank +1 Speed, then Mega Evolve next turn, but only when the Mega form *loses* Speed Boost (so Mega Blaziken, which keeps it, Megas immediately). Freshness is keyed off Speed still being at its default stage. Covered by `test/fork/ai_smart_species_logic.c`. Extend by adding cases to the module + a hook if a new decision surface is needed. |
| Factory swap: opponent summary | `B_FRONTIER_FACTORY_OPP_SUMMARY` | `config/frontier.h` | ✅ | On the post-battle rental-swap screen, selecting an *opponent* mon now opens the same Summary/Swap/Rechoose popup the player's mons get (vanilla jumps straight to "Accept this Pokémon?" with no way to inspect moves/stats first); "Swap" accepts the mon. Gated in `src/battle_factory_screen.c` (`Swap_*`). The duplicate-species guard still runs before the menu. The lobby rules-board "swap notes" text is flipped to say the player *may* check the stats of the mon they're about to receive (assembler `.if` on this flag). |
| Extended frontier roster | `B_FRONTIER_EXTENDED_MONS` | `config/frontier.h`, `src/fork/frontier_extended_mons.c` | ✅ | Replaces the vanilla `gBattleFrontierMons` on the Battle Factory's code paths with a fork-owned roster of modern competitive sets (tuned around the `DETERMINISTIC_*` changes), drawn **uniformly** (the vanilla tier ramp in `sInitialRentalMonRanges`/`GetFactoryMonId` is bypassed). The roster carries **several distinct builds per species** (so the opponent's exact set can't be read off the species) and tags each set **FORMAT_SINGLES/DOUBLES/BOTH** — `GetFactoryMonId` only draws sets valid for the current battle format. (Named "extended" rather than "factory/competitive" because other frontier facilities will adopt this same list.) The list is ordered by National Pokédex number. Draft rule additions: unique species (the vanilla `currSpecies` one-dup quirk is closed under the flag), unique held item, and **at most one Mega Stone + one Z-Crystal** per generated team (`TeamHasGimmickItemConflict`, applied to player rentals, opponents, and the Frontier Brain; post-win swaps can exceed this, which is fine — the per-battle Mega/Z limit still applies). **Tier-gated drafting** (via the species tier map, `TierRejectsCandidate`): the player's initial team and ordinary opponents carry **no legendaries/mythicals and at most one pseudo**; **every `FRONTIER_STAGES_PER_CHALLENGE`-th battle** (the last of each set) the opponent gains **one guaranteed legendary** at a random slot, the rest drafted normally; the **Frontier Brain** fields **one legendary + one mythical** at two random slots, the other four drafted normally. Forced slots are reserved with `ReserveForcedTierSlot` before drafting, so the special mon's party position is randomized. **Illusion stays functional:** a drafted Illusion mon (Zoroark, the Eon duo) is never placed in the team's *last* slot, where it would have no party member behind it to disguise as (`GetIllusionMonPartyId` bails on a last-slot Illusion mon, so the disguise silently never forms) — `IllusionMonRejectsSlot` (`src/fork/frontier_draft.c`) rejects such a candidate from `partySize - 1` during the draft (applied to Factory opponents, the Frontier Brain, and Tower opponents; covered by `test/fork/frontier_draft.c`). Roster swap is **Factory-only** for now via `GetFactoryMonsTable()`/`GetFactoryMonsCount()`; Tower/Dome keep `gBattleFrontierMons` until they adopt the list. **Generations I–IX are comprehensively built out — _every_ fully-evolved species has at least one build** (1–2 flavor/utility sets for niche mons, ~2–4 for stronger ones, mega & non-mega), **with a deliberate offense/defense balance** — wall/support/pivot sets (Eviolite/Assault Vest/Rocky Helmet/Heavy-Duty Boots tanks, hazard/Defog/cleric/Trick-Room support) alongside the sweepers. **All regional forms (Alolan, Galarian, Paldean, Hisuian) and their cross-gen evolutions** appear as their own entries, slotted at their base species' National Dex number, alongside Paradox Pokémon (with Booster Energy + Protosynthesis/Quark Drive), Ultra Beasts, and notable formes (Therian, Necrozma, Origin Dialga/Palkia, Urshifu, Calyrex riders, Ogerpon masks, Bloodmoon Ursaluna, the appliance Rotoms, etc.). **Held items are deliberately varied** — Leftovers, berries, Assault Vest, Rocky Helmet, Heavy-Duty Boots, type-boost items, and screens/utility items are favored over defaulting everything to Choice items / Life Orb. Roster rules: **only fully-evolved Pokémon appear**, except NFEs with a genuine niche their evolution doesn't dominate (Eviolite Chansey, Light Ball Pikachu); cross-gen evolutions are used in place of their pre-evos (e.g. Magnezone/Electivire/Magmortar/Rhyperior/Tangrowth replace the Gen I Magneton/Electabuzz/Magmar/Rhydon/Tangela). **Innate abilities are respected** — species with an innate Levitate (`src/fork/innate_abilities.c`) are never given a redundant Air Balloon, and a set's `.ability` slot carries a **complementary** chosen ability that runs alongside the innate Levitate/Regenerator/Unaware (e.g. Slowbro `OWN_TEMPO` + innate Regenerator, Rotom `LIGHTNING_ROD` + innate Levitate, Clefable `MAGIC_GUARD` + innate Unaware). For *ability-locked* innate species whose only real ability is the one now granted innately, that complementary ability is made selectable via the fork-owned **species ability overrides** table (`src/fork/species_ability_overrides.c`; see its row) rather than by editing upstream species data. Every set's `.ability` is verified legal through that hook by `test/fork/frontier_extended_roster.c`, so a typo can't silently downgrade to slot 0. Movesets are **not restricted to the species' in-game learnset** — flavorful/powerful coverage is allowed. The vanilla `FRONTIER_MONS_HIGH_TIER` (849) Level-50 gate is **disabled under the flag** (the roster has no tier ordering), so there is no 849-entry ceiling on the list. Saved rentals reference the roster by **array index** (no stable-ID layer), so inserting/removing entries mid-list invalidates an in-progress rented team in an existing save — appending is save-safe, mid-list edits are not. Tera data is recorded but inert (Tera disabled in this fork). **Max-gimmick readiness is granted at draft time** by the fork-owned `ApplyDraftGimmickReadiness` (`src/fork/frontier_draft.c`), called once from `CreateFacilityMon` so the only upstream edit is a single additive call line: every facility mon gets the **maximum Dynamax Level** (`MAX_DYNAMAX_LEVEL`, 10) so a Dynamaxed mon gets the full HP boost, and any mon whose species has a G-Max form (`DoesSpeciesHaveFormChangeMethod(species, FORM_CHANGE_BATTLE_GIGANTAMAX)`) also gets the **Gigantamax Factor** so it Gigantamaxes instead of plain Dynamaxing — no per-entry annotation needed. Both honor an explicit per-entry override (`.dynamaxLevel` / `.gigantamaxFactor`); covered by `test/fork/frontier_extended_roster.c`. Plain `#if`-gated (not the runtime-config/`WITH_CONFIG` system), like the other `B_FRONTIER_*` flags. **Post-battle rental swap reverts battle form changes:** a defeated opponent's in-battle form change (e.g. Palafin's Zero to Hero → `PALAFIN_HERO`) is only undone on the *player's* party at battle end, so renting that mon used to carry the changed form into the next battle (Zero to Hero firing on send-out); `CopySwappedMonData` now reverts the swapped-in mon to its base form (`src/battle_factory_screen.c`). **Authoring helpers** (data-file readability, no behavior change, designed for editing on mobile): each entry opens with a `{ // NNNN` National-Dex comment (like `innate_abilities.c`); EVs are written by name with `EVS(.hp = 252, .def = 252, .spd = 4)` (unset stats default to 0, order-free) instead of positional `TRAINER_PARTY_EVS`; natures are written by effect with `NATURE(DEF_UP, ATK_DOWN)` (boosted stat first, lowered second; neutral natures use `NATURE_NEUTRAL`) instead of recalling that `NATURE_BOLD` is +Def/-Atk. All three are defined in `include/fork/frontier_extended_mons.h` and compile to identical bytes. **Enabled (`TRUE`).** |
| Species ability overrides | _(data; no flag)_ | `src/fork/species_ability_overrides.c`, `include/fork/species_ability_overrides.h` | ✅ | Fork-owned `{species, slot, ability}` table + `GetSpeciesAbilityOverride()`, consulted by `GetSpeciesAbility()` (`src/pokemon.c`) — the single accessor every ability read funnels through — so an override behaves like a real ability everywhere (battle, summary, AI, the Battle Factory ability picker) **without editing upstream `gSpeciesInfo`**. Built for the extended roster: several Battle Factory sets carry an innate Levitate/Regenerator yet want a *second*, chosen ability on top of it, but their species are "ability-locked" upstream (their only real ability is the one now provided innately), so the chosen ability has no slot and `CreateFacilityMon` would silently fall back to slot 0. Each row gives that ability a real slot — an empty normal slot on ability-locked floaters (Rotom→Lightning Rod, Hydreigon→Sheer Force, Lati@s→Illusion, the lake trio→Trace/Moody/Victory Star, Cresselia→Cloud Nine, Giratina-Origin→Dragon's Maw, …) or the now-redundant Regenerator slot on full-slot innate-Regenerator mons (Tangrowth→Sap Sipper, Audino→Cute Charm, Alomomola→Water Absorb), or — the **all-abilities-innate** case — a species whose *every* real slot is now innate (Skeledirge = Blaze + Unaware both innate → chosen `MUMMY`), or — the lone non-ability-locked case — a real Hidden Ability that is dead weight on the roster's sets (Sceptile's Overgrow is innately latched and its HA Unburden does nothing on its non-consumable-item sets, so the slot is repurposed for its Mega's `LIGHTNING_ROD`). Each row's *chosen* ability is picked to be **stable** — cross-referenced against `INNATE_ABILITIES_PROGRESS.md` and preferring a `:x:` (never-an-innate) ability like Lightning Rod over a `:white_large_square:` (pending) one, since a pending pick has to be re-pointed once it lands as an innate. The whole table was audited on this rule: every row now hands out a `:x:` ability (Sceptile→Lightning Rod, Solrock→Drought, Cryogonal→Snow Warning, Vikavolt→Motor Drive, Ogerpon-Cornerstone→Earth Eater, …) except the two that hand out an already-implemented `:white_check_mark:` innate (Carnivine→Chlorophyll, Tornadus-Therian→Prankster). Separately, the slot a row *frees* must already be redundant via an *implemented* `:white_check_mark:` innate. The table is sorted by National Dex number with a trailing-comment dex on each row. `CreateFacilityMon`'s ability picker was pointed at `GetSpeciesAbility` (it read `gSpeciesInfo[].abilities[]` directly) so the override is visible when choosing the slot (`FORK:`-tagged, in `src/battle_frontier.c`). Modeled on `src/fork/innate_abilities.c` (data in a fork-owned file, never in `gSpeciesInfo`). Enforced by `test/fork/frontier_extended_roster.c`, which fails CI if any roster `.ability` doesn't resolve to a real slot through the hook. |
| Species tier map | _(data; no flag)_ | `src/fork/species_tiers.c`, `include/fork/species_tiers.h` | ✅ | Fork-owned species→tier classification table + `GetSpeciesTier(species)` / `SpeciesIsTier(species, tier)` accessors, so facility logic can **control what appears where**. Three tiers (this fork's own groupings, **not** the official Game Freak categories): **`TIER_MYTHICAL`** = the strongest "restricted" legends — box/cover legendaries (Groudon, Kyogre, Dialga, Zacian, Koraidon…), Mewtwo, Lugia/Ho-Oh, **plus** the official event Mythicals (Mew, Celebi, Jirachi, Arceus, Diancie, Magearna, Pecharunt…); **`TIER_LEGENDARY`** = the sub-legendaries (birds, beasts, lake trio, Regis, genies +Therian, musketeers, Tapus, Heatran, Cresselia, Urshifu, Ogerpon, the Loyal Three…); **`TIER_PSEUDO`** = the 600-BST pseudo-legendaries (Dragonite, Garchomp, Dragapult…), Ultra Beasts, Paradox Pokémon, and Treasures of Ruin. Derived from the upstream `isRestrictedLegendary`/`isSubLegendary`/`isMythical`/`isUltraBeast`/`isParadox` flags with those groupings layered on top. **Keyed by exact species id** (forme-specific), so each forme is classified on its own merits instead of inheriting a single tier from its base's National Dex number. This lets a powerful forme outrank its base — **Shaymin-Sky is `TIER_LEGENDARY`** (its fork-boosted Serene Grace) while ordinary Shaymin is `TIER_NORMAL` (simply absent from the table) — and a weak base sit below its formes — **base Calyrex is `TIER_NORMAL`** while its Ice/Shadow riders are `TIER_MYTHICAL`. Modeled on `src/fork/innate_abilities.c` (data in a fork-owned file, never in `gSpeciesInfo`). Currently covers the special species/formes used by the extended frontier roster; species not listed return `TIER_NORMAL`. **Consumer:** the Battle Factory draft (`B_FRONTIER_EXTENDED_MONS`) gates player/opponent/Brain teams by tier — see that row. |
| All species legal in Frontier | `B_FRONTIER_ALL_SPECIES_LEGAL` | `config/frontier.h`, `include/fork/frontier_legality.h` | ✅ | When `TRUE`, the per-species `isFrontierBanned` flag (set on legendaries/mythicals etc. in `gSpeciesInfo`) is ignored, so **every** species is legal in the Battle Frontier facilities. The upstream banned-flag reads were funnelled through `IsSpeciesFrontierBanned()` (a static-inline chokepoint in the fork-owned `frontier_legality.h`), which returns `FALSE` under the flag; covers the party-menu eligibility check (`src/party_menu.c`) and the `AppendIfValid` / `CheckPartyIneligibility` / caught-banned-species-list logic (`src/frontier_util.c`). With the flag on, the "ineligible Pokémon" list is always empty (no species is banned). Upstream `gSpeciesInfo` data is left untouched, so `FALSE` restores the vanilla per-species ban. |
| Disable Frontier battle recording | `B_FRONTIER_DISABLE_RECORD_BATTLE` | `config/frontier.h`, `src/frontier_util.c` | ✅ | When `TRUE`, no facility attendant offers to record a battle onto the Frontier Pass. Recorded-battle "playback" is not a video — it re-simulates the battle from a saved RNG seed + the players' per-turn inputs, so it only renders correctly if the engine reproduces the original run byte-for-byte. The expansion's far more stateful battle engine doesn't hold that invariant reliably (a long-standing **upstream** fragility, not a fork regression — the recording engine files `recorded_battle.c`/`frontier_pass.c` are stock upstream), so the replay desyncs into a glitched battle. Forces `FRONTIER_DATA_RECORD_DISABLED` on in `GetFrontierData()`, so every attendant's "Record" menu option falls through to its existing no-record menu (the scripts already branch on this field) and the Frontier Pass "Record" area has nothing to play. The Reception Gate "Frontier Pass" guide NPC also hides its RECORD help entry, using the reduced `MULTI_FRONTIER_PASS_INFO_NO_RECORD` list. The recording/playback code is left fully intact, so flipping this back to `FALSE` (or a future upstream determinism fix) revives the feature unchanged. |
| In-battle INFO viewer | `B_FRONTIER_BATTLE_INFO` | `config/frontier.h` | ⚠️ partial | In facilities where the bag is disabled (`BATTLE_TYPE_FRONTIER_NO_PYRAMID` — Tower, Dome, Palace, Arena, Factory, Pike; **not** the Pyramid, which keeps a working bag), the BAG action slot becomes **INFO**, opening a read-only screen (`src/fork/frontier_battle_info.c`, `CB2_FrontierBattleInfo`). **Styling** reuses stock assets only (no new art): the player's chosen Options window frame drawn around the window (`DrawTextBorderOuter` + `GetWindowFrameTilesPal`), the standard menu palette (`gStandardMenuPalette`) for conventional `TEXT_COLOR_*` text, a transparent window over a soft backdrop colour, and coloured page titles (red) / footer (blue) for hierarchy — the full-width window's char block is nearly full, so the frame is drawn on its own BG (with a heap tilemap buffer freed on close, since a static one would grow EWRAM into the heap). The same viewer can also be opened with **SELECT** from the in-battle "choose a Pokémon" party menu (e.g. picking a replacement for a fainted mon), so the player can review field/foe state before deciding; closing it returns to the party menu. The return target is configurable (`OpenFrontierBattleInfo(returnCallback)`): the action-menu path returns to the battle screen, the party-menu path re-opens the party menu (`CB2_OpenBattleInfoFromPartyMenu`/`CB2_ReturnToPartyMenuFromBattleInfo` in `src/party_menu.c`, gated on `ShouldReplaceBagWithInfo()` so it's battle-only). Five pages, **L/R** cycle (both directions), with a right-aligned `n/N` page indicator (`PrintPageIndicator`) and the last-viewed page/foe remembered across re-opens **within a battle only** (the viewer is opened repeatedly per turn; closing saves the page/foe in `gBattleStruct->infoViewerPage`/`infoViewerFoeIndex`, reopening resumes there). Those live in `gBattleStruct` — zero-allocated each battle — specifically so the position **resets between battles** (to Speed Tiers / foe 0) instead of carrying a stale foe tab into the next battle, which a file static did (it could land on a previous battle's foe index, showing an unrevealed slot). Defaults on the first open to **Speed Tiers**, the page that actually drives a turn decision, which now leads the cycle: **Field** (weather/terrain + entry hazards & side screens both sides; plus, under `DETERMINISTIC_DAMAGE`, the current turn and that turn's fixed damage multiplier so the player can read the exact roll — `DrawDeterministicDamageLine`), **Conditions** (each on-field battler's primary status + notable volatiles — confusion, leech seed, taunt, etc. — both sides), **Stat Changes** (each on-field battler's non-default stat stages, e.g. `Atk+2 Spe-1`, both sides), **Speed Tiers** (each **revealed** foe's *possible* Speed range — `min-max`, where min = 0 IVs/0 EVs/hindering nature ×0.9 and max = 31 IVs/252 EVs/boosting nature ×1.1 at the foe's known level, derivable from base stats alone so the player can place a foe on the speed tier without knowing its exact spread; only seen slots are listed — unseen ones are omitted entirely, so neither an unrevealed mon **nor the foe's party size** leaks — and they're sorted fastest-first by the top of their range so the list reads as a true tier. The player's active mons are listed above for comparison, but using their **effective** Speed — `GetBattlerTotalSpeedStat`, which folds in everything the player already knows about their *own* side (Choice Scarf, Tailwind, paralysis, stat stages, Speed abilities) — and the fastest of them anchors a per-foe comparison **glyph**: `▼` (`CHAR_DOWN_ARROW`) when your effective Speed already exceeds that foe's best-case base Speed, `▲` (`CHAR_UP_ARROW`) when even its worst-case base Speed exceeds yours, blank when the ranges overlap. **Glyph, not colour, on purpose** (legible for colour-blind players), and framed as a pure number comparison — your *known* Speed vs the foe's *possible base* Speed — **not** a guarantee of turn order, since the foe's range still deliberately ignores its own hidden Choice Scarf / paralysis / Speed ability. `DrawSpeedPage`/`CalcSpeedBound`/`AppendSpeedGlyph`, mirrors `CalculateMonStats`. The foe range is raw base-stat only), **Foe** (the foe's **revealed-only** party data; `<>` cycles mons — species/gender/level, `FNT` when fainted, plus moves/PP/ability/held-item). "Seen" comes from `gBattleStruct->partyState[...].sentOut`; species/gender/level/fainted read from the party once seen. **Illusion-safe:** a foe whose Illusion (Zoroark/Zorua) is currently `ILLUSION_ON` shows the *disguise* mon's species/gender/level (and Speed-tier range) — not the real one — via `GetFoeDisplayMon`, mirroring the health box (which also reads `GetIllusionMonPtr`); the true species reveals only once the Illusion breaks. Everything else is gated on genuine reveal, because `gAiPartyData` is the AI's *knowledge* model (pre-filled under `AI_FLAG_OMNISCIENT` and padded with STAB/status assumptions), not "what the player saw": used moves are tracked in `gBattleStruct->infoUsedMoves` (set at the move-use site), and ability/held-item in `gBattleStruct->infoAbilityRevealed`/`infoItemRevealed` (set in `RecordAbilityBattle`/`RecordItemEffectBattle`, but **only while a turn is actually executing** — `BattleInfoCanRevealNow()` skips the reveal when `gAiLogicData->aiCalcInProgress` is set, so the AI's *speculative* move evaluation no longer leaks the foe's ability/item before the player sees it. Without this gate the foe's ability read **"Prankster"** at battle start and on every switch-in, because `GetBattleMovePriority`'s Prankster check records through this path for every status move while the AI computes turn order. The *displayed* ability value is read from a reveal-time snapshot, `gBattleStruct->infoRevealedAbility[side][slot]` (written under the same gate), **not** from `gAiPartyData->mons[].ability`: that live knowledge model is freely overwritten by the AI's speculative move/switch evaluation (e.g. a benched Prankster mon simulated in the active slot records Prankster onto it) while the already-set reveal bit stays put — which made a foe whose Intimidate fired at battle start display as **"Prankster"** anyway. **Innate-aware (`FEATURE_INNATE_ABILITIES`):** a mon's innates are a *static property of its species* (like the type line), fully determined the moment the foe's species is known — so, unlike the genuinely-hidden 1-of-N chosen-ability roll, they are **not** reveal-gated: the viewer lists every innate of the *displayed* species (Illusion-safe via `displaySpecies`, so a disguised Zoroark/Zorua never leaks its real identity through its innate list) as soon as the mon is seen. They share the Ability line rather than a dedicated `Innate:` row, which would push the bottom Moves slot under the nav bar (the page is at its fixed height): the chosen ability prints plainly and the innates follow as `(+Name, …)`, e.g. `Magnet Pull (+Levitate, Sturdy)` — or **`? (+Levitate, Sturdy)`** while the chosen ability is still unseen (the passives are public, the rolled ability stays hidden until witnessed). Only the *chosen* ability stays gated: `RecordAbilityBattle` still won't mark it revealed when what the player witnessed was an innate pop-up (`gBattleScripting.abilityPopupOverwrite`, an innate Levitate/Sturdy forcing the pop-up to its name) rather than the chosen ability, so an innate reveal never leaks the chosen one. The item bit is additionally set wherever an item visibly reveals itself — a fired hold effect at the `ItemBattleEffects` chokepoint in `battle_hold_effects.c` (Life Orb recoil, Leftovers/berry heal, status orbs, stat-boost berries; silent effects like Amulet Coin return `ITEM_NO_EFFECT` and are skipped), and the visible removal sites: Knock Off in `battle_move_resolution.c`, the `Cmd_removeitem` destruction path for Fling/Corrosive Gas/Incinerate, and steals via `StealTargetItem` — so an item the player watched activate or leave reads as its name or **`None`**, never `?`). Reuses the `B_ACTION_DEBUG` controller path so the turn isn't consumed; touches only the player controller, those record fns, the move-use & item reveal/removal sites (`FORK:` tags) + a menu-label string. **Foe page reads opponent A only** (single-trainer doubles / singles); two-opponent multi battles show just the first foe trainer's party for now. |
| Deterministic critical hits | `DETERMINISTIC_CRITICAL_HITS` | `config/deterministic.h` | ✅ | First of the `DETERMINISTIC_*` flags (a project to strip RNG from battles). Removes the random crit-chance roll (`IsCriticalHit`, `src/battle_util.c`) — crits land only when *guaranteed* (always-crit moves, Laser Focus, Merciless vs. a poisoned target, 1/1 crit-stage); crit-blockers unchanged, AI crit valuations made config-aware. Full mechanics + AI wiring: [`DETERMINISM.md`](DETERMINISM.md#deterministic_critical_hits). Registered for `WITH_CONFIG` (baseline off — see the Test harness note below); covered by `test/fork/deterministic_critical_hits.c`. **Enabled (`TRUE`).** |
| Deterministic damage | `DETERMINISTIC_DAMAGE` (+ `…_BASE_PERCENT`, `…_TURN_INCREMENT`) | `config/deterministic.h` | ✅ | Replaces the random 85%–100% damage roll with a fixed multiplier that scales with the battle's turn count (default 92%, +1%/turn, **intentionally uncapped** past turn 9); the AI is fed the same figure so it never mis-predicts. Full mechanics: [`DETERMINISM.md`](DETERMINISM.md#deterministic_damage). Registered for `WITH_CONFIG` (baseline off — see the Test harness note below); covered by `test/fork/deterministic_damage.c`. **Enabled (`TRUE`).** |
| Deterministic flinch | `DETERMINISTIC_FLINCH` | `config/deterministic.h` | ✅ | Anti flinch-lock cap that composes with `DETERMINISTIC_ADDITIONAL_EFFECTS`: a gated flinch can't be re-applied to a foe that flinched last turn (Fake Out / guaranteed flinches exempt), so a fast flincher can't stunlock. Required by the additional-effects flag so its gated flinches can't lock. Full mechanics: [`DETERMINISM.md`](DETERMINISM.md#deterministic_flinch). Registered for `WITH_CONFIG` (baseline off); covered by `test/fork/deterministic_flinch.c`. **Enabled (`TRUE`).** |
| Deterministic additional effects | `DETERMINISTIC_ADDITIONAL_EFFECTS` | `config/deterministic.h` | ✅ | A move's sub-100% secondary effect (burn, paralysis, stat drop, flinch) lands on a fixed condition instead of a roll: a super-effective hit (types that *can* be SE) or a STAB user (Normal). Serene Grace / Pledge Rainbow instead make it *certain*. Flinch obeys the same gate, with `DETERMINISTIC_FLINCH` (required) adding the anti-lock cap. Full mechanics: [`DETERMINISM.md`](DETERMINISM.md#deterministic_additional_effects). Registered for `WITH_CONFIG` (baseline off); covered by `test/fork/deterministic_additional_effects.c`. **Enabled (`TRUE`).** |
| Deterministic paralysis | `DETERMINISTIC_PARALYSIS` (+ `…_PP_TAX`, `…_PRIORITY_TAX`) | `config/deterministic.h` | ✅ | Drops the random 25% full-paralysis miss and the Speed cut; instead every move the paralyzed battler uses costs +1 PP and −1 priority (defaults), keeping full Speed (Quick Feet exempt). AI turn-order prediction tracks it for free; its paralysis valuation is made config-aware. Full mechanics: [`DETERMINISM.md`](DETERMINISM.md#deterministic_paralysis). Registered for `WITH_CONFIG` (baseline off — see the Test harness note below); covered by `test/fork/deterministic_paralysis.c`. **Enabled (`TRUE`).** |
| Deterministic hold effects | `DETERMINISTIC_HOLD_EFFECTS` | `config/deterministic.h` | ✅ | Chance-to-trigger hold items become guaranteed one-shot **entry items** (fire on the holder's first turn out, then are consumed): Focus Band → any-HP one-shot Sash, Quick Claw → first-in-bracket, crit items / Lansat Berry → guaranteed crit, King's Rock / Razor Fang → guaranteed flinch, Starf Berry → highest stat. Evasion items handled by the accuracy/evasion flag. Full mechanics: [`DETERMINISM.md`](DETERMINISM.md#deterministic_hold_effects). Registered for `WITH_CONFIG` (baseline off); covered by `test/fork/deterministic_hold_effects.c`. **Enabled (`TRUE`).** |
| Deterministic accuracy/evasion | `DETERMINISTIC_ACCURACY_EVASION` (+ `…_OHKO_MAX_HP_PERCENT`) | `config/deterministic.h` | ✅ | Accuracy/evasion become a **PP economy** rather than a hit/miss coin flip: moves always hit, sub-100% moves get reduced max PP plus a per-use accuracy/evasion-stage PP tax/refund and flat item/ability taxes; OHKO moves deal 40% max HP (immunities kept); sub-100% sleep moves cause drowsiness; 50%-accuracy moves gain a recharge turn; move-info shows projected PP cost. Full mechanics: [`DETERMINISM.md`](DETERMINISM.md#deterministic_accuracy_evasion). Registered for `WITH_CONFIG` (baseline off); covered by `test/fork/deterministic_accuracy_evasion.c`. **Enabled (`TRUE`).** |
| Deterministic abilities | `DETERMINISTIC_ABILITIES` | `config/deterministic.h` | ⚠️ partial | Strips RNG / random choice from abilities: contact statuses (Static, Flame Body, Poison Point/Touch, Cute Charm, Toxic Chain, Cursed Body) always attempt; Effect Spore → drowsy; Stench / Quick Draw fire first-turn-only; Moody, Pickup, Trace, Forewarn, Rivalry made state-based; the AI is taught the new guarantees. **Overworld ability RNG is out of scope** (deferred to the Pyramid rework). Full mechanics: [`DETERMINISM.md`](DETERMINISM.md#deterministic_abilities). Registered for `WITH_CONFIG` (baseline off); covered by `test/fork/deterministic_abilities.c`. **Enabled (`TRUE`).** |
| Deterministic status | `DETERMINISTIC_STATUS` (+ `…_INFATUATION_TURNS`, `…_INFATUATION_DMG_PERCENT`, `…_SLEEP_TURNS`) | `config/deterministic.h` | ✅ | Infatuation drops the gender requirement and the 50% coin flip — the battler always acts but deals reduced damage to the loved target for a fixed duration; sleep always lasts a fixed 2 turns; confusion becomes one guaranteed 40-BP self-hit that doesn't deny the action and can't be refreshed. AI valuations adjusted. Full mechanics: [`DETERMINISM.md`](DETERMINISM.md#deterministic_status). Registered for `WITH_CONFIG` (baseline off); covered by `test/fork/deterministic_status.c`. **Enabled (`TRUE`).** |
| Deterministic move results | `DETERMINISTIC_MOVE_RESULTS` (+ multi-hit / Population Bomb / rampage / wrap / Present tuning) | `config/deterministic.h` | ✅ | Fixes or makes state-based the rolls baked into move outcomes: multi-hit → 3 (5 with Skill Link/Loaded Dice), Population Bomb → 5/10, rampage 2 turns, binding 4/7, speed ties by a fixed Speed→weight→HP% ladder, consecutive Protect always fails, and Tri Attack / Dire Claw / Magnitude / Present / Fickle Beam / Shell Side Arm / phazing made state-based. Full mechanics: [`DETERMINISM.md`](DETERMINISM.md#deterministic_move_results). Registered for `WITH_CONFIG` (baseline off); covered by `test/fork/deterministic_move_results.c`. **Enabled (`TRUE`).** |
| Shell Bell buff | `BUFF_SHELL_BELL` (+ `BUFF_SHELL_BELL_DENOMINATOR`) | `config/buff.h` | ✅ | First of the `BUFF_*` flags (a project to rebalance items/functionality). Buffs Shell Bell recovery from the stock 1/8 of damage dealt to 1/`BUFF_SHELL_BELL_DENOMINATOR` (1/4 by default) in `TryShellBell()` (`src/battle_hold_effects.c`). Registered boolean toggle (test-flippable) + plain compile-time denominator, mirroring `DETERMINISTIC_DAMAGE`'s toggle+tuning split. **Enabled (`TRUE`) in this fork.** Covered by `test/fork/buff_shell_bell.c` (opt-in via `WITH_CONFIG`); see the fork-flag test harness note below. |
| Leech Seed buff | `BUFF_LEECH_SEED` (+ `BUFF_LEECH_SEED_DENOMINATOR`) | `config/buff.h` | ✅ | A `BUFF_*` flag. Two changes: (a) **stacking** - several battlers can seed one target at once, each draining it independently every turn (a doubly-seeded foe loses HP twice per turn, one share to each seeder); (b) re-using Leech Seed on a foe **you already seed** deals an **immediate 1/8 drain** instead of failing (seeding a foe only *another* battler seeded stacks your seed on). Per-tick (and immediate) drain fraction is 1/`BUFF_LEECH_SEED_DENOMINATOR` (kept at 8 = vanilla rate; the buff is the stacking + re-drain, not a bigger tick). Storage is additive: upstream's single-seeder `leechSeed` volatile is left as the "primary" seeder (truthiness/Baton Pass/AI), and a fork-owned `leechSeededBy` per-battler bitmask holds the full seeder set; the end-turn loop drains one seeder per engine step (low-index-first) via a re-entrancy tracker, the immediate re-drain reuses dedicated `BattleScript_LeechSeedReDrain*` scripts, and the AI's already-seeded penalties + end-of-turn drain prediction are flag-gated (`battle_ai_main.c`, `battle_ai_util.c`). Registered boolean toggle + plain compile-time denominator, mirroring `BUFF_SHELL_BELL`. **Enabled (`TRUE`) in this fork.** Covered by `test/fork/buff_leech_seed.c` (opt-in via `WITH_CONFIG`). |
| Innate abilities | `FEATURE_INNATE_ABILITIES` | `config/feature.h`, `src/fork/innate_abilities.c` | ⚠️ partial | First of the `FEATURE_*` flags. Some species gain one or more **innate** abilities always active *in addition to* their single chosen ability. The species→innate table lives in `src/fork/innate_abilities.c` (**not** `gSpeciesInfo`, so upstream syncs never touch it); innates satisfy "does this battler have ability X?" via `BattlerHasAbility()` and honor the same suppression as a real ability (`IsInnateActive()`), but are **never** the battler's copyable/swappable *identity* (Trace, Skill Swap, the ability pop-up, `RecordAbilityBattle` still read only the primary slot). **Design principle:** an innate is a *pure boon* — where the real ability carries a downside the innate drops it (innate Levitate still soaks terrain/Toxic Spikes; innate Unaware ignores the foe's boosts but keeps its drops; innate Prankster keeps +1 priority but still hits Dark-types); where the real ability is already a clean upside it's a 1:1 copy. **Allowlist, grown one ability at a time** to bound the upstream merge surface — declaring any *other* ability as an innate silently does nothing. Supported: `LEVITATE`, `REGENERATOR`, `UNAWARE`, `STURDY`, `NATURAL_CURE`, `PRANKSTER`, the pinch abilities `OVERGROW`/`BLAZE`/`TORRENT`/`SWARM`, the weather speed-doublers `SWIFT_SWIM`/`CHLOROPHYLL`/`SAND_RUSH`/`SLUSH_RUSH`, `FILTER`, `PRESSURE`, `STENCH`, `BATTLE_ARMOR`/`SHELL_ARMOR`, `SPEED_BOOST`, `LIMBER`, `CUTE_CHARM`, `OBLIVIOUS`, `SAND_VEIL`/`SNOW_CLOAK`, `COMPOUND_EYES`/`KEEN_EYE`/`ILLUMINATE`, `INSOMNIA`/`VITAL_SPIRIT`/`SWEET_VEIL`, `EARLY_BIRD`, `IMMUNITY`/`PASTEL_VEIL`, `THICK_FAT`, `TECHNICIAN`, and the offensive move-power boosters `IRON_FIST`/`RECKLESS`/`STRONG_JAW`/`TOUGH_CLAWS`/`SHARPNESS`/`MEGA_LAUNCHER`/`STEELWORKER`/`STEELY_SPIRIT`/`ROCKY_PAYLOAD`/`SAND_FORCE`/`ANALYTIC`/`ADAPTABILITY`/`PUNK_ROCK`/`STAKEOUT` (Sand Force also shrugs off sandstorm chip; Steely Spirit boosts allies' Steel moves too), `SERENE_GRACE` (doubles the chance of the holder's moves' additional effects), and the defensive damage reducers `MULTISCALE`/`SOLID_ROCK`/`FUR_COAT`/`ICE_SCALES`/`HEATPROOF`/`FRIEND_GUARD`/`WATER_BUBBLE` (Heatproof also halves burn damage; Water Bubble also doubles the holder's Water moves and blocks burn), and the status-conditional stat boosts `GUTS`/`MARVEL_SCALE`/`QUICK_FEET`/`TOXIC_BOOST`/`FLARE_BOOST` (Guts also negates burn's physical cut; Quick Feet also ignores the paralysis Speed/PP/priority penalty), and the crit-rate / crit-damage modifiers `SUPER_LUCK`/`SNIPER`/`MERCILESS` (Super Luck +1 crit stage; Sniper crits deal ×2.25 instead of ×1.5; Merciless auto-crits a poisoned target), and the accuracy / type-effectiveness / effect-chance modifiers `SHIELD_DUST`/`TINTED_LENS`/`SCRAPPY`/`WONDER_SKIN`/`TANGLED_FEET` (Shield Dust blocks the additional effects of moves used against the holder; Tinted Lens doubles resisted-move damage; Scrappy hits Ghosts with Normal/Fighting moves *and* shrugs off Intimidate (Gen 8+); Wonder Skin caps incoming status moves at 50% accuracy — a PP tax under `DETERMINISTIC_ACCURACY_EVASION`; Tangled Feet doubles evasion while confused), and the priority granters `GALE_WINGS`/`TRIAGE` (Gale Wings gives the holder's Flying moves +1 priority while at full HP; Triage gives its healing moves +3 priority — both 1:1 clean-upside copies wired beside Prankster in `GetBattleMovePriority`, so the AI's turn-order prediction respects them for free), and the terrain modifiers `SURGE_SURFER`/`GRASS_PELT` (Surge Surfer doubles the holder's Speed on Electric Terrain — the terrain edition of the weather speed-doublers, wired in `GetBattlerTotalSpeedStat`; Grass Pelt boosts its Defense ×1.5 on Grassy Terrain, wired in `CalcDefenseStat` beside Marvel Scale — both 1:1 clean-upside copies in shared calcs, so on-field AI prediction is free and the AI's terrain-setting heuristic credits them via `DoesInnateBenefitFromFieldStatus`), and the physical-Attack doublers `HUGE_POWER`/`PURE_POWER` (each doubles the holder's physical Attack — both 1:1 clean-upside copies sharing one clause in `CalcAttackStat`, guarded off when the chosen ability is either, so the shared damage calc makes on-field AI prediction free), and the stat-drop protectors `CLEAR_BODY`/`WHITE_SMOKE`/`HYPER_CUTTER`/`BIG_PECKS` (Clear Body/White Smoke keep *any* of the holder's stats from being lowered by another mon; Hyper Cutter protects Attack, Big Pecks Defense — all 1:1 clean-upside copies wired in `IsAbilityBlocked` (`battle_stat_change.c`) by generalizing the Keen Eye/Illuminate accuracy block, with the pop-up overwritten to the innate and the AI's `CanLowerStat` + Intimidate switch-in heuristic made innate-aware), and the priority-move blockers `DAZZLING`/`QUEENLY_MAJESTY`/`ARMOR_TAIL` (opponents can't use increased-priority moves against the holder or its allies — all 1:1 clean-upside copies wired at the shared `CancelerPriorityBlock` site (`battle_move_resolution.c`) via `GetBattlerDazzlingAbility`, with the pop-up overwritten to the innate and the AI's `Ai_IsPriorityBlocked` helper made innate-aware; canon-only — Bruxish/Tsareena/Farigiraf), and the redirection-ignore abilities `PROPELLER_TAIL`/`STALWART` (the holder's moves ignore Follow Me/Rage Powder and Lightning Rod/Storm Drain redirection, hitting the selected target — identical 1:1 clean-upside copies wired at the shared redirection sites in `battle_move_resolution.c` plus the Ally-Switch retarget in `battle_anim_effects_1.c`, with the AI's `IsMoveRedirectionPrevented` helper made innate-aware; no pop-up, canon-only — the Arrokuda line/Propeller Tail and the Duraludon line + Skarmory-Mega/Stalwart), and the trapping abilities `SHADOW_TAG`/`ARENA_TRAP`/`MAGNET_PULL` (the holder keeps opposing mons from switching out or fleeing — Shadow Tag any foe, Arena Trap grounded foes, Magnet Pull Steel-types — all 1:1 clean-upside copies wired at the single chokepoint `IsAbilityPreventingEscape` (`battle_util.c`) via `BattlerHasAbility`, with the can't-switch party-menu message naming the actual trapping ability and the AI's `IsBattlerTrapped` + benched-trapper switch heuristic `AI_CanSwitchinAbilityTrapOpponent` (off-field via `SpeciesHasInnate`) made innate-aware; no pop-up, canon-only — the Wobbuffet/Gothita/Litwick lines (Shadow Tag), Diglett/Trapinch (Arena Trap), the Magnemite/Alolan-Geodude/Nosepass lines (Magnet Pull)), and the status-condition immunities `MAGMA_ARMOR`/`WATER_VEIL`/`OWN_TEMPO`/`INNER_FOCUS`/`LEAF_GUARD`/`OVERCOAT` (Magma Armor blocks freeze/frostbite, Water Veil burn, Own Tempo confusion, Inner Focus flinching, Leaf Guard all non-volatile status while the holder is in harsh sun, Overcoat powder moves + sandstorm/hail chip — all 1:1 clean-upside copies, same class as Limber/Immunity/Insomnia, wired at the shared trait chokepoints `CanSetNonVolatileStatus`/`CanBeConfused`/`IsLeafGuardProtected`/`IsAffectedByPowderMove`/the sandstorm-hail end-turn block; Inner Focus & Own Tempo also grant the Gen 8+ Intimidate immunity, and each gets the matching switch-in status cure; AI flinch/confuse/weather-damage reads made innate-aware; canon-only except a molten/burning-body Magma Armor flavor set — Torkoal, the Coalossal line), and the miscellaneous single-site traits `SUCTION_CUPS`/`GUARD_DOG`/`ROCK_HEAD`/`LONG_REACH`/`SKILL_LINK`/`INFILTRATOR`/`CORROSION`/`STICKY_HOLD`/`UNSEEN_FIST`/`PIERCING_DRILL`/`HEAVY_METAL`/`LIGHT_METAL` (Suction Cups/Guard Dog resist forced switch-out — Dragon Tail/Circle Throw and Red Card in C, and Roar/Whirlwind via an innate-aware `BS_JumpIfRoarFails`; Rock Head negates recoil; Long Reach makes the holder's moves non-contact at the `IsMoveMakingContact` chokepoint; Skill Link maxes its multistrike hits incl. the `DETERMINISTIC_MOVE_RESULTS` reroute; Infiltrator ignores the foe's screens/Safeguard/Mist/Substitute; Corrosion poisons Poison/Steel types; Sticky Hold keeps its held item from Knock Off/Thief/Trick/Incinerate/Bug Bite/Magician with the pop-up overwritten to the innate; Unseen Fist + Piercing Drill hit through Protect; Heavy Metal/Light Metal double/halve weight — all 1:1 clean-upside copies, canon-only, with the matching AI effect reads made innate-aware), and the end-of-turn effects `RAIN_DISH`/`ICE_BODY`/`SHED_SKIN`/`HYDRATION`/`HEALER`/`HARVEST`/`CUD_CHEW`/`PICKUP`/`BAD_DREAMS`/`POISON_HEAL` (Rain Dish/Ice Body heal 1/16 max HP in rain/snow; Shed Skin (30%, always under `DETERMINISTIC_ABILITIES`) and Hydration (in rain) self-cure status; Healer (30%, always under `DETERMINISTIC_ABILITIES`) cures an ally; Harvest recovers a used Berry; Cud Chew re-eats a Berry the turn after; Pickup grabs an item consumed this turn; Bad Dreams chips sleeping/Comatose foes 1/8 max HP — the first nine are active, scripted end-turn innates that reuse the Speed Boost driver (`TryActivateInnateEndTurnEffects` → `IsActiveEndTurnInnate`) to delegate to the upstream `ABILITYEFFECT_ENDTURN` case, so the heal/cure/item recovery/chip damage/script/pop-up match the real ability with the pop-up overwritten to the innate at each effect site — plus a fix so the end-turn Pickup recycle is innate-aware (`tryrecycleitem`, `battle_script_commands.c`) and a valid-target guard so Bad Dreams can't leak its pop-up overwrite; **Poison Heal** is NOT a driver innate — it *replaces* the poison-damage step (heal 1/8 instead of losing HP), wired at `HandleEndTurnPoison` (`battle_end_turn.c`) via `BattlerHasAbility`, with the AI's poison-harm heuristics made innate-aware — all 1:1 clean-upside copies, canon-only except a Munna-line flavor pair carrying an observable innate Bad Dreams since Darkrai always has it chosen), and the berry/item-synergy abilities `GLUTTONY`/`RIPEN`/`CHEEK_POUCH`/`UNBURDEN` (Gluttony eats a pinch Berry at 1/2 HP instead of 1/4, wired at `HasEnoughHpToEatBerry`; Ripen doubles every Berry effect across its ~11 heal/PP/stat-raise/chip/resist-berry/Micle-accuracy sites, with the resist-berry cut in the shared damage calc so AI prediction is free and the two dedicated berry-KO/Recycle AI reads made innate-aware; Cheek Pouch heals 1/3 max HP on eating a Berry — a scripted heal with the pop-up overwritten to the innate; Unburden doubles the holder's Speed once its item is consumed/lost, the flag armed innate-aware in `CheckSetUnburden` and doubled in `GetBattlerTotalSpeedStat` so the AI's turn-order prediction respects it — all 1:1 clean-upside copies, canon-only), and the on-hit contact reactions `ROUGH_SKIN`/`IRON_BARBS`/`GOOEY`/`TANGLING_HAIR` (Batch K first sub-group — when the holder is hit by a contact move, Rough Skin/Iron Barbs chip the attacker 1/8 max HP and Gooey/Tangling Hair lower the attacker's Speed by 1; the **first active, scripted ON-HIT innates**, fired through a new re-entrant on-hit driver `TryActivateInnateOnHitEffects` → `IsActiveOnHitInnate` — the on-hit analogue of the Speed Boost end-turn driver — hooked from the new `MOVEEND_ABILITIES_INNATE` step (`battle_move_resolution.c`) and delegating to the upstream `ABILITYEFFECT_MOVE_END` case so the recoil/stat-drop/script match the real ability with the pop-up overwritten to the innate at each effect site; the AI's Rough Skin/Iron Barbs contact-avoidance reads made innate-aware — all 1:1 clean-upside copies, canon-only; Step 3.5 freed twelve frontier sets, e.g. Sharpedo/Garchomp/Ferrothorn/Wugtrio via fork-owned overrides), and the on-faint retaliation abilities `AFTERMATH`/`INNARDS_OUT` (Batch K second sub-group — Aftermath chips a contact attacker 1/4 max HP when it KOs the holder, Innards Out deals the attacker the holder's lost HP on any KO; both reuse the same on-hit driver, a one-line `IsActiveOnHitInnate` addition each, firing from the `ABILITYEFFECT_MOVE_END` step once the holder faints — still credited because `notOnField` is not yet set — with the pop-up overwritten to the innate; no AI wiring needed since neither has a dedicated AI read; all 1:1 clean-upside copies, canon-only; Step 3.5 freed five frontier sets via fork-owned overrides — Drifblim ×2→Unaware, Skuntank/Garbodor→Poison Touch, Pyukumuku→Water Absorb), and the on-hit stat/charge abilities `STEAM_ENGINE`/`THERMAL_EXCHANGE`/`WIND_POWER` (Batch K third sub-group — when the holder is hit, Steam Engine raises Speed +6 on a Fire/Water hit, Thermal Exchange raises Attack +1 on a Fire hit *and* grants burn immunity, Wind Power charges the next Electric move on a wind hit *or* when Tailwind takes effect; the on-hit boost/charge reuses the same on-hit driver, a one-line `IsActiveOnHitInnate` addition each delegating to the upstream `ABILITYEFFECT_MOVE_END` case with the pop-up overwritten to the innate; Thermal Exchange's burn immunity is wired at `CanSetNonVolatileStatus` + the switch-in cure beside Water Veil, so `CanBeBurned` AI is free, and Wind Power's Tailwind trigger at `BS_TryWindRiderPower`; the dedicated Wind Power self-charge/switch-in AI reads and the doubles partner-fire heuristics for Steam Engine/Thermal Exchange made innate-aware; all 1:1 clean-upside copies, canon-only — the Rolycoly/Frigibax/Wattrel lines; Step 3.5 freed three frontier sets — Coalossal→Flash Fire, both Baxcalibur→a Snow Warning override), and the on-hit move-disable ability `CURSED_BODY` (Batch K fourth sub-group — when the holder is damaged by a move, a 30% chance (always, under `DETERMINISTIC_ABILITIES`) to disable the move the attacker just used; reuses the same on-hit driver, a one-line `IsActiveOnHitInnate` addition delegating to the upstream `ABILITYEFFECT_MOVE_END` case with the pop-up overwritten to the innate; no AI wiring needed since it has no dedicated AI read; a 1:1 clean-upside copy, canon-only — the Shuppet/Banette (incl. Mega Banette), Frillish/Jellicent, Sinistea/Polteageist, Dreepy/Dragapult lines + Froslass, Corsola-Galar, Marowak-Alola, with sole-Cursed-Body Gengar omitted as redundant; Step 3.5 freed Jellicent→Water Absorb and Polteageist→Weak Armor, the Froslass/Banette all-innate sets deferred like Batch J/T), and the item-steal reactions + drain punish `PICKPOCKET`/`MAGICIAN`/`LIQUID_OOZE` (Batch K fifth/final sub-group, **completing Batch K** — Pickpocket steals a contact attacker's held item when the holder has none, a one-line innate-aware swap at the dedicated `MoveEndPickpocket` step (not the on-hit driver — it has its own move-end step); Magician steals a held item off a target the holder damaged, and being *attacker*-side needs a new attacker-side on-hit driver `TryActivateInnateOnHitAttackerEffects` → `IsActiveOnHitAttackerInnate` hooked from the new `MOVEEND_ABILITY_EFFECT_FOES_FAINTED_INNATE` step and delegating to the upstream `ABILITYEFFECT_MOVE_END_FOES_FAINTED` case; Liquid Ooze is a passive calc modifier (no driver, like Filter/Unaware) that makes an HP-draining move damage the attacker instead of healing it, wired at the three drain sites (`SetHealScript`, `SetUpLeechSeedDrain`, the non-BUFF Leech Seed tick) with the pop-up overwritten to the innate; Liquid Ooze's dedicated drain-avoidance AI reads (`ShouldAbsorb`/`AI_IsMoveEffectInMinus` + the Leech-Seed scoring) made innate-aware, Pickpocket/Magician have no AI read; all 1:1 clean-upside copies, canon-only — Pickpocket the Seedot/Sneasel/Weavile/Binacle/Impidimp/Shroodle/Tinkatink lines, Magician the Fennekin line + Mega Delphox + Klefki + Hoopa forms, Liquid Ooze the Tentacool/Gulpin lines; Step 3.5 deferred all sixteen affected frontier sets like Batch J/T since each species now has all its useful real abilities innate), and the switch-in Attack drop `INTIMIDATE` (Batch L first sub-group — on switch-in the holder lowers every opposing battler's Attack by 1 stage; the **first active, scripted SWITCH-IN innate**, fired through a new re-entrant switch-in driver `TryActivateInnateSwitchInEffects` → `IsActiveSwitchInInnate` hooked from the new `FIRST_EVENT_BLOCK_GENERAL_ABILITIES_INNATE` step (`battle_switch_in.c`) inside the `switchinevents` state machine that drives every normal switch-in (battle intro, pivot moves, post-faint replacement, forced switch) — the ability-swap/Tera/form-change `switchinabilities` sites are deliberately not hooked so a species-bound innate never re-fires on Skill Swap or Mega evolution; delegates to the upstream `ABILITYEFFECT_ON_SWITCHIN` case so the drop/script/pop-up — and every downstream reaction, incl. the target's Clear Body/Own Tempo/Inner Focus/Oblivious/Scrappy Intimidate-immunity halves already made innate-aware in earlier batches — match the real ability, with the pop-up overwritten to the innate; the AI's switch-in Attack-drop simulation (`SetBattlerStatStagesForSwitchin`) and Intimidate-cycling switch heuristic (`ShouldSwitchIfAbilityBenefit`) and foe-Intimidate free-switch timing read made innate-aware; a 1:1 clean-upside copy, canon-only (no flavor picks — a ~45-species canon set), keyed exactly per form with base creatures' Megas as pure-boon mirrors (Gyarados/Salamence/Mawile), sole-Intimidate Megas (Manectric-Mega, Scrafty-Mega) omitted as redundant, and Landorus-Therian (sole-Intimidate frontier set) taking the innate + a fork-owned chosen Sheer Force override; Step 3.5 freed Landorus-Therian via that override and deferred the other ~40 sets like Batch J/T/K), and the switch-in information reveals `ANTICIPATION`/`FOREWARN`/`FRISK` (Batch L second sub-group — on switch-in Anticipation warns of a foe's super-effective/OHKO move, Forewarn reveals a foe's strongest move, Frisk reveals the foes' held items; all three reuse the Intimidate switch-in driver, a one-line `IsActiveSwitchInInnate` addition each, delegating to the upstream `ABILITYEFFECT_ON_SWITCHIN` case so the message/reveal/script/pop-up match the real ability with the pop-up overwritten to the innate; pure-information 1:1 clean-upside copies with no dedicated AI read, so no AI wiring; canon-only, keyed exactly per form — Frisk the Gothita/Shuppet (incl. Mega Banette mirror)/Duskull/Espathra/Munkidori/Yanma/Noibat/Orbeetle/Impidimp and Wigglytuff/Exeggutor-Alola/Typhlosion-Hisui/Sentret/Stantler/Wyrdeer/Phantump/Pumpkaboo/Gourgeist lines, Forewarn the Munna/Drowzee/Smoochum lines, Anticipation Ferrothorn + the Barboach/Ponyta-Galar/Eevee-Starter/Wormadam/Croagunk/Hatenna lines (Flittle carries both Frisk and Anticipation); Step 3.5 deferred the ~14 affected frontier sets like Batch J/T/K/Intimidate). **Batch L third sub-group** adds two **switch-in stat changes** — Download raises the holder's Attack or Sp. Atk toward the foe's weaker defense, Supersweet Syrup lowers every foe's evasiveness once per battle (tracked per party mon) — both reusing the Intimidate switch-in driver (a one-line `IsActiveSwitchInInnate` addition each), delegating to the upstream `ABILITYEFFECT_ON_SWITCHIN` case so the stat change/script/pop-up match the real ability with the pop-up overwritten to the innate; 1:1 clean-upside copies (self-boost / foe-debuff) with the AI's switch-in stat simulation (`SetBattlerStatStagesForSwitchin`) and the Eject-Pack free-switch timing read made innate-aware; canon-only — Download the Porygon line + every Genesect form, Supersweet Syrup the Dipplin/Hydrapple line; Step 3.5 freed the Porygon2 set to a chosen Trace and deferred the all-innate Porygon-Z/Genesect sets. **Batch L fourth/final sub-group** adds two **switch-in effects** — Unnerve denies every opposing battler its Berries, Hospitality restores 1/4 of the ally's max HP in doubles — **completing Batch L**. Unlike the earlier members they run through *different* upstream switch-in cases at *different* phases (`ABILITYEFFECT_UNNERVE` / `ABILITYEFFECT_DEPENDS_ON_ALLY`, not `ABILITYEFFECT_ON_SWITCHIN`), so the driver gained an `abilityEffect` phase selector (`SwitchInInnateAbilityEffect` maps each switch-in innate to its effect) and two new hooks — a `SWITCH_IN_EVENTS_UNNERVE_INNATE` event and a `SECOND_EVENT_ABILITIES_INNATE` step (`battle_switch_in.c`) — each firing the driver right after its chosen-ability counterpart. Unnerve's Berry block is the passive `IsUnnerveAbilityOnOpposingSide` gate (`battle_util.c`), made innate-aware via `BattlerHasAbility` — the switch-in driver only adds the matching message/pop-up; Hospitality's heal lives entirely in its `ABILITYEFFECT_DEPENDS_ON_ALLY` case. Both pop-ups overwrite to the innate; the AI's switch-in Berry-heal estimate (`GetSwitchinSingleUseItemHealing`) is made innate-aware for Unnerve, Hospitality needs no AI wiring. 1:1 clean-upside copies, canon-only — Unnerve the Ekans/Meowth/Persian/Aerodactyl/Mewtwo/Ursaring/Ursaluna/Houndour/Houndoom/Tyranitar/Masquerain/Vespiquen/Joltik/Galvantula/Axew/Litleo/Bewear/Rookidee/Corviknight lines + base creatures' Megas as pure-boon mirrors (sole-Unnerve Calyrex omitted as redundant), Hospitality the Poltchageist/Sinistcha line (all four forms); Step 3.5 freed Sinistcha's Hospitality set to a chosen Flash Fire override and deferred the ~14 chosen-Unnerve sets like the earlier Batch L sub-groups. **Batch M's first sub-group** adds two **stat-drop reactions** — `DEFIANT`/`COMPETITIVE` (when a *foe* lowers one of the holder's stats — a move, Intimidate, or Sticky Web — Defiant raises the holder's Attack and Competitive its Sp. Atk by 2 stages). Both are 1:1 clean-upside copies wired at the single scripted reaction site `BS_TryDefiantRattled` (`battle_script_commands.c`): when the chosen ability isn't itself reactive, an innate Defiant/Competitive is credited and the pop-up overwritten to it, with the real-ability path (chosen Defiant/Competitive/Rattled) byte-for-byte unchanged; because the reaction funnels through the shared stat-drop message it reacts to Intimidate (chosen *or* innate) and Sticky Web for free. Not a driver innate (the reaction has always been its own native command). The negative-stat-change animation suppression and the two dedicated AI reads — the don't-lower-a-reactive-foe check (`IncreaseStatDownScore`) and the don't-cycle-Intimidate-into-a-benefiting-foe check (`ShouldSwitchIfIntimidateBenefit`) — are made innate-aware; the soft incoming-ability value scorer stays keyed to the chosen ability (mirroring Intimidate). Canon-only (no flavor picks — a +2 swing is potent), keyed exactly per form: Defiant the Mankey/Primeape/Annihilape, Farfetchd, Pawniard/Bisharp/Kingambit, Braviary, Tornadus-Incarnate, Thundurus-Incarnate, Purugly, Passimian, Obstagoon, Falinks (base) lines; Competitive the Jigglypuff, Milotic, Piplup/Prinplup/Empoleon (their Gen 9 Hidden Ability), Gothita, Meowstic-F, Wattrel/Kilowattrel, Boltund lines; sole-ability species omitted as redundant (Zapdos-Galar, Articuno-Galar, Ogerpon/Ogerpon-Teal, Falinks-Mega). Innate **Rattled** — which also reacts through `BS_TryDefiantRattled` but only to Intimidate — is a separate Batch M sub-group. Step 3.5: the ~30 chosen-Defiant/Competitive frontier sets already resolve to a real slot, so they keep the now-redundant chosen ability, freeing deferred like Batch J/T/K/L. **Batch M's second sub-group** adds four **on-hit stat boosts** — `JUSTIFIED`/`STAMINA`/`WATER_COMPACTION`/`ANGER_POINT` (when the holder is hit, Justified raises Attack +1 on a Dark move, Stamina raises Defense +1 on any move, Water Compaction raises Defense +2 on a Water move, Anger Point maxes Attack on a critical hit). All four fire from the upstream `ABILITYEFFECT_MOVE_END` case, so each is a one-line `IsActiveOnHitInnate` addition reusing the Batch K on-hit driver, delegating to that case so the stat change/script/pop-up match the real ability with the pop-up overwritten to the innate at each effect site; they trigger at 100% on the right hit (Anger Point on a guaranteed/rolled crit), so there is **no `DETERMINISTIC_*` surface**. AI made innate-aware: the "don't feed the on-hit boost" `AI_CheckBadMove` read (Justified; Stamina/Water Compaction/Anger Point have no such avoid-read, their trigger isn't a move-type the AI dodges), the doubles partner-fire `scoringPartnerAbility` promotion (Justified/Water Compaction/Anger Point, the Steam Engine precedent), and the always-crit self-Anger-Point + Beat-Up-for-Justified reads. 1:1 clean-upside copies, canon-only, keyed exactly per form with base creatures' Megas as pure-boon mirrors — Justified the Growlithe/Arcanine, Absol (+ Mega), Gallade (+ Mega), Lucario (+ Mega/Mega-Z) lines; Stamina the Mudbray/Mudsdale line + Archaludon; Water Compaction the Sandygast/Palossand line; Anger Point the Mankey/Primeape, Tauros (+ all three Paldea forms), Camerupt (+ Mega), Sandile/Krokorok/Krookodile, Crabrawler/Crabominable lines. Sole-Justified species (the Swords of Justice trio Cobalion/Terrakion/Virizion + Keldeo) omitted as redundant like the Defiant/Competitive sub-group. Step 3.5: the chosen-Justified/Stamina/Water-Compaction/Anger-Point frontier sets already resolve to a real slot, so they keep the now-redundant chosen ability, freeing deferred like Batch J/T/K/L. **Batch M's third sub-group** adds the **fear-response Speed pair** — `RATTLED`/`STEADFAST` (both raise the holder's Speed +1 when frightened). Rattled spans the two Batch M sites already opened: a Dark/Ghost/Bug hit (a one-line `IsActiveOnHitInnate` addition reusing the on-hit driver, like Justified) and a foe's Intimidate (credited at `BS_TryDefiantRattled` beside Defiant/Competitive, but gated on `intimidateActivated` + Gen8+, so it reacts *only* to Intimidate). Steadfast reacts to flinching (made innate-aware at the `CancelerFlinch` site). Pop-up overwritten to the innate at each site (Rattled's `ABILITYEFFECT_MOVE_END` case previously lacked the overwrite — added, like Justified's). AI is innate-aware for Rattled — the `AI_CheckBadMove` avoid-a-Dark/Ghost/Bug-hit read, the doubles partner-fire `scoringPartnerAbility` promotion, and the `ShouldSwitchIfIntimidateBenefit` Intimidate-cycling switch (Gen8+, mirroring how `DoesIntimidateRaiseStats` already flags a chosen Rattled); Steadfast needs none (upstream's AI doesn't avoid flinching a Steadfast holder). 1:1 clean-upside copies, canon-only, keyed per form — Rattled the Meowth-Alola/Persian-Alola, Magikarp, Ledyba, Bonsly/Sudowoodo, Whismur, Snubbull/Granbull, Poochyena, Dunsparce/Dudunsparce, Clamperl, Basculin-White-Striped, Cubchoo, Yamper, Toxel, Wiglett/Wugtrio lines; Steadfast the Machop/Machoke/Machamp (+ Gmax), Farfetch'd-Galar/Sirfetch'd, Tyrogue/Hitmontop, Scyther, Gallade (+ Mega), Rockruff/Lycanroc-Midday, Dubwool lines. Sole-ability species omitted as redundant (Gimmighoul-Chest, Mega Mewtwo X). **Contradiction omission:** Steadfast is dropped on the Riolu/Lucario line, whose innate Inner Focus prevents flinching (so an innate Steadfast could never fire — the Spinda Tangled-Feet-vs-Own-Tempo class). Step 3.5: the chosen-Rattled/Steadfast frontier sets already resolve to a real slot, so they keep the now-redundant chosen ability, freeing deferred like Batch J/T/K/L. **Batch M's fourth and final sub-group** adds three **KO / on-damage / on-faint stat boosts** — `MOXIE`/`BERSERK`/`SOUL_HEART` — **completing Batch M**. Moxie raises the holder's Attack +1 for each foe it knocks out with a move (fired from the upstream `ABILITYEFFECT_MOVE_END_FOES_FAINTED` case, a one-line `IsActiveOnHitAttackerInnate` addition reusing the Batch K attacker-side on-hit driver, beside Magician). Berserk raises Sp. Atk +1 when an attack drops the holder's HP to 1/2 or less — it fires from the *per-damaged-battler* `ABILITYEFFECT_COLOR_CHANGE` step (not `ABILITYEFFECT_MOVE_END`), so it adds a small new on-damage driver `TryActivateInnateOnDamageEffects` → `IsActiveOnDamageInnate` hooked from the new `MOVEEND_COLOR_CHANGE_INNATE` step (`battle_move_resolution.c`) right after the chosen-ability color-change block and looped over every damaged battler like it, delegating to `ABILITYEFFECT_COLOR_CHANGE`. Soul-Heart raises Sp. Atk +1 whenever any Pokémon faints (made innate-aware at the `BS_TryActivateSoulheart` native command, run unconditionally on every faint). Each effect site overwrites the pop-up to the innate when the chosen ability differs; all three trigger at 100% on their event, so there is **no `DETERMINISTIC_*` surface**. AI: Moxie's two effect reads (the Protect self-faint check and the sacrifice-the-ally spread scoring, both via `IsMoxieTypeAbility`) are made innate-aware via `IsInnateActive`; Berserk and Soul-Heart have no dedicated AI read. 1:1 clean-upside copies; **Moxie is canon-only** keyed per form with base creatures' Megas as pure-boon mirrors — the Pinsir (+ Mega), Gyarados (+ Mega), Honchkrow, Heracross (+ Mega), Mightyena, Salamence (+ Mega), Sandile/Krokorok/Krookodile, Scraggy/Scrafty, Litleo/Pyroar, Quaxly/Quaxwell/Quaquaval lines; **Berserk is canon-only** — only Drampa (its Sap Sipper/Cloud Nine slots leave the innate observable), with sole-Berserk Galarian Moltres and Drampa-Mega omitted as redundant; **Soul-Heart is flavor-only** (the Bad Dreams/Darkrai precedent — its sole canon user Magearna is sole-Soul-Heart, so it's omitted as redundant, and a soul-collector flavor set carries the observable innate: the Duskull/Dusclops/Dusknoir line + Spiritomb). Step 3.5: the chosen-Moxie/Berserk/Soul-Heart frontier sets already resolve to a real slot, so they keep the now-redundant chosen ability, freeing deferred like Batch J/T/K/L. **Batch U** adds the five **ally-support** abilities `BATTERY`/`POWER_SPOT` (partner damage boosters, wired in `CalcAttackStat` beside Steely Spirit), `TELEPATHY` (dodge an ally's move), `AROMA_VEIL` (side-wide shield vs infatuation/Taunt/Disable/Encore/Torment/Heal Block — wired via the new `IsInnateOnSide()` companion at the C guards and centrally in `Cmd_jumpifability`'s Aroma-Veil-only side cases) and `FLOWER_VEIL` (Grass allies immune to status + stat drops, via `IsFlowerVeilProtected`/`StatChange_IsFlowerVeilProtected`); all 1:1 clean-upside copies, canon-only, with the dedicated AI side reads made innate-aware via `AI_IsInnateOnSide`. Step 3.5 freed six sets (Musharna/Rabsca→Synchronize, Oranguru/Florges→Symbiosis, Stonjourner→a Solid Rock override) and deferred the all-abilities-innate Dialga/Palkia/Giratina/Orbeetle/Aromatisse sets like Batch J/T/K/L. **Batch V** completes two partial halves that shipped before the switch-in driver existed: an innate **Guard Dog** is now immune to Intimidate's Attack drop and boosts its own Attack +1 instead (wired at `IsIntimidateBlocked`, mirroring the chosen `ABILITY_GUARD_DOG` case with the pop-up/record overwritten to the innate and the two `ShouldSwitchIfIntimidateBenefit` AI reads made innate-aware), and an innate **Pastel Veil** now runs the real ability's full switch-in ally-cure (`SwitchInInnateAbilityEffect` maps it to `ABILITYEFFECT_ON_SWITCHIN` on the Batch L switch-in driver, curing the holder + its ally's pre-existing poison with the pop-up overwritten to the innate; the `ABILITYEFFECT_IMMUNITY` self-cure remains a fallback). **Batch Y sub-group Y1** adds three **promoted-from-rejected clones** `CHILLING_NEIGH`/`GRIM_NEIGH`/`ELECTROMORPHOSIS` — each a 1:1 clean-upside copy of an already-implemented ability whose driver exists, so near-free. Chilling Neigh / Grim Neigh raise the holder's Attack / Sp. Atk +1 per foe it KOs (the on-KO half of Moxie): one-line `IsActiveOnHitAttackerInnate` additions reusing the shared `ABILITYEFFECT_MOVE_END_FOES_FAINTED` case, with the pop-up already overwritten to the innate. Electromorphosis charges the holder's next Electric move when hit by *any* damaging move (Wind Power minus the wind gate): a one-line `IsActiveOnHitInnate` addition reusing the `ABILITYEFFECT_MOVE_END` case the two already share as a fall-through. The two Moxie-type AI reads (`IsMoxieTypeAbility`) credit an innate neigh via the new `IsMoxieTypeInnateActive()` helper; Electromorphosis needs no AI wiring. No `DETERMINISTIC_*` surface. Canon-only — Chilling Neigh→Glastrier, Grim Neigh→Spectrier (both sole-ability genderless legends, so like Landorus-Therian each takes the innate + a fork override: Glastrier→Snow Warning, Spectrier→Infiltrator), Electromorphosis→Bellibolt (Static/Damp leave the innate observable); Step 3.5 freed all five hardcoded frontier sets (Glastrier ×2→Snow Warning, Spectrier ×2→Infiltrator, Bellibolt→Static). **Batch Y sub-group Y2** adds two more **promoted-from-rejected clones** `TRANSISTOR`/`DRAGONS_MAW` — flat type-power-booster clones of Steelworker / Rocky Payload, two lines in `CalcAttackStat` beside Rocky Payload (Transistor ×1.3 in GEN_9+, else ×1.5, on Electric moves; Dragon's Maw ×1.5 on Dragon moves), AI-free (shared damage calc, no dedicated AI read). Canon-only — Transistor→Regieleki, Dragon's Maw→Regidrago (both sole-ability genderless Regi legends, so like Glastrier/Spectrier each takes the innate + a fork override: Regieleki→Lightning Rod, Regidrago→Adaptability); Step 3.5 freed all four hardcoded frontier sets (Regieleki ×2→Lightning Rod, Regidrago ×2→Adaptability). **Batch Y sub-group Y3** adds four **damage/power calc clones** `PRISM_ARMOR`/`SHADOW_SHIELD`/`NEUROFORCE`/`SUPREME_OVERLORD`. Prism Armor (−25% vs a supereffective hit) rides Filter/Solid Rock's clause and Shadow Shield (halve at full HP) rides Multiscale's clause in `GetDefenderAbilitiesModifier`; both are **unbreakable**, so — unlike their breakable Batch B cousins — an attacker's Mold Breaker cannot pierce them (`IsInnateActive` reads each ability's own `breakable` flag). Neuroforce (+25% to supereffective hits) is the offensive mirror of Tinted Lens in `GetAttackerAbilitiesModifier`. Supreme Overlord (+10% move power per fallen teammate, max +50%) is a `CalcAttackStat` modifier gated on a switch-in-latched counter, so it also rides the Batch L switch-in driver (a one-line `SwitchInInnateAbilityEffect`→`ABILITYEFFECT_ON_SWITCHIN` addition) to set the counter + show its pop-up. AI-free save the Shadow Shield full-HP-survival read (`battle_ai_util.c`). Canon-only — Prism Armor→Necrozma/Dusk-Mane/Dawn-Wings, Shadow Shield→Lunala (sole-ability legends + frontier sets, so like the Regi legends each takes the innate + a chosen Adaptability override so it's observable and the set is freed), Neuroforce→Necrozma-Ultra (transform-only, no override — observed via a test's forced chosen ability), Supreme Overlord→Kingambit (joins its innate Defiant/Pressure; its two sets choose Defiant). **Batch Y sub-group Y4** adds two **stat-drop / accuracy / hit-trait clones** `FULL_METAL_BODY`/`MINDS_EYE`. Full Metal Body is the **unbreakable** clone of Clear Body — it rides Clear Body's full stat-drop-protection path in `GetInnateStatDropProtector`/`IsAbilityBlocked` (+ the two AI reads), but its `.breakable = FALSE` means Mold Breaker can't pierce it (for free via `IsInnateActive`). Mind's Eye combines Keen Eye (ignore the target's evasion + own accuracy can't be lowered) and Scrappy (Normal/Fighting hit Ghosts), with **no** Intimidate immunity; it clones each half at the site its model already touches (`GetTotalAccuracy`/`GetAccEvasionStageDelta`, `GetInnateStatDropProtector`, `MulByTypeEffectiveness`) and is breakable like Keen Eye/Scrappy. Full Metal Body→Solgaleo, Mind's Eye→Ursaluna-Bloodmoon — both sole-ability legends + frontier sets, so each takes the innate + a fork chosen override (Solgaleo→Tough Claws, Ursaluna-Bloodmoon→Unaware) so it's observable and the set is freed. Batch Y5 adds two **status-immunity clones** `PURIFYING_SALT`/`GOOD_AS_GOLD` (both 1:1 clean-upside copies, canon-only): Purifying Salt (the Nacli/Naclstack/Garganacl line) makes the holder immune to **every** non-volatile status (the catch-all block in `CanSetNonVolatileStatus`, so the `CanBe*` AI callers are innate-aware for free) and halves incoming Ghost damage (beside innate Thick Fat) — its only real cost, blocking the holder's own Rest, is dropped as a pure boon (the Insomnia precedent); Good as Gold (Gholdengo) blocks incoming status moves (`CanAbilityAbsorbMove` + four dedicated AI reads via `BattlerHasAbility`) — a **very strong** blanket status-move immunity, a deliberate balance divergence kept canon-only to contain it. Both breakable (Mold Breaker pierces). Gholdengo (sole-ability) takes a fork chosen **Sticky Hold** override so the innate is observable + its frontier sets freed; the salt line is a Batch W all-innate deferral. Batch Y6 adds the switch-in stat boosts `INTREPID_SWORD`/`DAUNTLESS_SHIELD` (Zacian/Zamazenta + their Crowned formes) — Atk/Def +1 the first time the holder enters battle, riding the Batch L switch-in driver (real once-per-battle latch + pop-up for free; AI switch-in stat sim mirrors the self-boost). All four are sole-ability frontier sets, so each takes a fork chosen override (Zacian → **Tough Claws**, Zamazenta → **Filter**) so the innate is observable + the frontier set freed; neither is breakable. **Batch Y7** adds the on-KO best-stat clone `BEAST_BOOST` (Moxie, best-stat edition) — a one-line addition to `IsActiveOnHitAttackerInnate` (shares Moxie's `ABILITYEFFECT_MOVE_END_FOES_FAINTED` case, which already reads `GetHighestStatId`), AI credited via `IsMoxieTypeInnateActive`; every canon user is a sole-Beast-Boost Ultra Beast, so the ten evolved/frontier UBs (Nihilego/Buzzwole/Pheromosa/Xurkitree/Celesteela/Kartana/Guzzlord/Naganadel/Stakataka/Blacephalon) take the innate + a fork chosen override (Merciless/Iron Fist/Tough Claws/Lightning Rod/Filter/Sharpness/Filter/Sheer Force/Solid Rock/Infiltrator) so it's observable + each frontier set freed; non-frontier pre-evo Poipole omitted as redundant; not breakable. **Tier 5.1** adds the fork-custom ability `MEGA_SOL` (the holder's own moves treat the weather as harsh sun) — a 1:1 clean-upside copy wired at the single chokepoint `GetAttackerWeather(battler, ...)` (`battle_util.c`) via `IsInnateActive` (the helper gained a leading `battler` param, threaded through its ~15 callers), so every attacker-weather read (Weather Ball → Fire, sun-boosted Fire damage, Solar Beam skip-charge, Growth +2, Thunder/Hurricane accuracy) is covered by one clause; the off-field party-menu Weather Ball prediction (`battle_main.c`) credits it via `SpeciesHasInnate`. AI-free (shared move calc); suppressible, not breakable. Canon carrier Mega Meganium (its sole ability); base Meganium takes it as a tight observable flavor pick (chosen Overgrow). Step 3.5 no-op (no frontier set hardcoded it). **Tier 5.2** adds `QUICK_DRAW` (the holder's moves have a 30% chance of going first within their priority bracket; under `DETERMINISTIC_ABILITIES`, the shipping default, they always go first on the holder's entry turn like Quick Claw) — a 1:1 clean-upside copy wired at the two effect sites in `TryChangingTurnOrderEffects` (`battle_main.c`) via `BattlerHasAbility`, with the activation pop-up / "can act faster" message overwritten to Quick Draw when the chosen ability differs; the determinism layer is untouched (the per-battler `quickDrawRandom[]` roll is precomputed for every battler regardless of ability, so wiring the ability read suffices), and the `DETERMINISTIC_ABILITIES` turn-order override in `AI_WhoStrikesFirst` (`battle_ai_util.c`) is innate-aware (the random 30% roll is unpredictable, so the AI models it only under the deterministic config, as in stock). Canon carrier Galarian Slowbro (its primary ability) takes the innate; the Galarian Farfetch'd → Sirfetch'd duelist line is a tight observable flavor pick (chosen Steadfast/Scrappy differ). Step 3.5 no-op (no frontier set hardcoded it). `COMATOSE` (Tier 5.3 — a **pure-boon divergence**, canon-only): the holder is immune to **every** non-volatile status (via the catch-all Comatose/Purifying-Salt block in `CanSetNonVolatileStatus`, so the AI's `CanBe*` status reads are innate-aware for free) and counts as asleep for its **own** Snore / Sleep Talk (usability gates in `battle_move_resolution.c` + their AI heuristics), but — unlike the real ability — is **not** treated as asleep at the **cost** sites (enemy Hex / Dream Eater / Nightmare / Bad Dreams all read the chosen slot, and the holder's own Rest block is left chosen-only, the Insomnia precedent), so the always-asleep trait only ever helps it; the switch-in "drowsing" display flavor is dropped (no driver). `cantBeSuppressed`, so Mold Breaker can't pierce the innate (the distinctive contrast with the breakable Purifying Salt). Canon-only: Komala takes it beside its innate Unaware, plus a fork chosen **Sticky Hold** override (it clings to its log) so both innates are observable and its frontier set is freed. `MAGIC_GUARD` (Tier 5.4 — a 1:1 clean-upside copy, canon-only): the holder takes damage **only from direct attacks**, sparing it every indirect/chip source (sandstorm & hail, poison/burn/toxic, recoil/crash/Chloroblast, entry hazards Spikes/Stealth Rock/Steelsurge, Flame Burst splash, Leech Seed, Curse, Nightmare & binding, Life Orb, the Sticky-Barb/Black-Sludge-vs-non-Poison item chip, and Rough Skin/Iron Barbs/Rocky-Helmet contact recoil). A **cross-cutting sweep with no new machinery**: every chip site was made innate-aware, most via the new **`IsAbilityOrInnateAndRecord`** drop-in for `IsAbilityAndRecord` (records the chosen slot exactly as before, credits an active innate without recording — the Rock-Head recoil precedent) across `battle_end_turn.c`/`battle_hold_effects.c`/`battle_move_resolution.c`/`battle_switch_in.c`/`battle_util.c`, the `==`/`!=` sites via `BattlerHasAbility`; the AI's indirect-damage predictors and status/hazard/recoil/weather heuristics (`battle_ai_util.c`/`battle_ai_main.c`/`battle_ai_switch.c`/`battle_ai_field_statuses.c`) are innate-aware too. Breakable (Mold Breaker pierces, for free via `IsInnateActive`). Canon-only (a strong defensive boon, like Y5 / Comatose): the Clefairy line (Cleffa / Clefairy / Clefable, + fork Mega Clefable), the Abra line (Abra / Kadabra / Alakazam, + Mega Alakazam), Sigilyph, and the Solosis line (Solosis / Duosion / Reuniclus), each merged into its existing innate row. Step 3.5: the 3 Alakazam frontier sets freed to chosen **Synchronize** (its real, non-innate ability); the all-innate Clefable / Sigilyph / Reuniclus sets deferred to Batch W. `MOLD_BREAKER` (Tier 5.5 — a 1:1 clean-upside copy, canon-only): the holder's moves **ignore the target's breakable ability** (Levitate, Sturdy, Thick Fat, Multiscale, Volt/Water Absorb, …), exactly like the real ability. The whole effect flows through **one flag**, `gBattleStruct->moldBreakerActive`, set from `IsMoldBreakerTypeAbility` (`src/battle_util.c`) — that function gained a single `IsInnateActive(battler, ABILITY_MOLD_BREAKER)` clause, so every effect site (the `CanBreakThroughAbility` gate in `GetBattlerAbility`) and every AI read that routes through it (`battle_ai_util.c`/`battle_ai_main.c`/`battle_ai_switch.c`) became innate-aware for free with no per-site edits. No `RecordAbilityBattle` and no switch-in "breaks the mold" pop-up for the innate; suppressible (Gastro Acid/Neutralizing Gas), never self-broken (`.breakable = FALSE`). Species canon-only: the Pinsir (+ Mega), Cranidos/Rampardos, Drilbur/Excadrill (+ Mega), Basculin/Basculegion, Axew, Pancham, Tinkatink lines + Throh/Sawk/Druddigon/Hawlucha (+ Mega)/Veluza/Ogerpon-Hearthflame; the all-Mold-Breaker Megas (Emboar/Gyarados/Ampharos Mega) are omitted as redundant. Step 3.5: Rampardos freed to its `:x:` Sheer Force HA; the other (all-abilities-innate) sets deferred to Batch W. Its **Teravolt/Turboblaze** clones (**Batch Y8**, now done) share this exact machinery — two more `IsInnateActive` clauses in `IsMoldBreakerTypeAbility`, so every effect site + AI read is covered for free; each canon carrier is a sole-ability frontier legend taking the innate + a fork chosen override (Reshiram + Kyurem-White → Flash Fire, Zekrom + Kyurem-Black → Motor Drive), which frees all seven affected frontier sets. `OPPORTUNIST` (Tier 5.6 — a 1:1 clean-upside copy, canon-only): whenever an **opposing** battler's stat is boosted, the holder copies that exact boost onto itself (the ability twin of the Mirror Herb item — a foe's Swords Dance mirrors as Attack +2, etc.). No new machinery: the real ability already watches every stat raise in `ChangeStatBuffs` (`src/battle_stat_change.c`) and applies the queued boosts at the `ABILITYEFFECT_OPPORTUNIST` site (`src/battle_util.c`), both made innate-aware via `IsInnateActive` — the effect site's three callers pass the chosen slot, so an `IsInnateActive` clause there catches an innate whose chosen ability differs, with the pop-up/record overwritten to Opportunist (Speed Boost precedent). Only copies a foe's *gains*, never drops, so a plain 1:1 copy; the AI's don't-boost-into-Opportunist reads (`battle_ai_util.c`) are innate-aware via `AI_IsInnateOnSide`. Sole carrier: **Espathra** (its primary ability), joining its innate Frisk / Speed Boost; Opportunist stays observable as its natural chosen slot. Step 3.5: all three of Espathra's abilities are now innate with no free slot, so both frontier sets keep their now-redundant chosen Opportunist and freeing is deferred to Batch W (the Mold Breaker all-abilities-innate precedent). `MIRROR_ARMOR` (Tier 5.7 — a 1:1 clean-upside copy, canon-only): instead of being affected by a stat-lowering effect, the holder bounces it back at the battler that caused it (the reactive twin of Opportunist). Wired at the three sites the real ability already uses — `IsMirrorArmorReflected` (`src/battle_stat_change.c`, the reflection itself, made innate-aware with `IsInnateActive`), the spread-move pending-flag setter `StatChangeMirrorArmor` (`src/battle_move_resolution.c`) and the Gooey/Tangling-Hair contact-reflect corner case (`src/battle_util.c`) via `BattlerHasAbility` — with the pop-up/record overwritten to Mirror Armor when the chosen ability differs (Speed Boost precedent). Only ever spares the holder its own drop, so a plain 1:1 copy; breakable (Mold Breaker pierces, consistently with the chosen path through `GetBattlerAbility`). The AI's don't-Intimidate-switch-into-Mirror-Armor read (`ShouldSwitchIfIntimidateBenefit`, `src/battle_ai_switch.c`) credits an innate foe; the "will this drop land" prediction rides the shared reflection calc for free. Sole carrier: **Corviknight** (+ Gmax, its hidden ability), joining its innate Pressure / Unnerve. Step 3.5: all three abilities now innate with no free slot, so its three frontier sets keep their now-redundant chosen Mirror Armor (observable + correct) and freeing is deferred to Batch W (Opportunist / Mold Breaker precedent). `MAGIC_BOUNCE` (Tier 5.8 — a 1:1 clean-upside copy, canon-only): the holder reflects a bounceable status move (Toxic, Thunder Wave, Taunt, entry hazards, ...) back at its user — the whole-*move* analogue of Mirror Armor. Reuses the upstream move-bounce machinery, so it needed no new hook — only crediting the innate at the detection site `TryMagicBounce` (`src/battle_move_resolution.c`) with the pop-up/record overwritten to Magic Bounce at `MoveEndBouncedMove` when the chosen ability differs, plus the Dark-type Prankster-block precedence read (`CanTargetBlockPranksterMove`, `src/battle_util.c`) via `BattlerHasAbility`. Never hurts the holder, so a plain 1:1 copy; breakable (Mold Breaker pierces, via `IsInnateActive`). Three AI don't-throw-a-bounceable-move reads are innate-aware (`AI_ShouldSetUpHazards` + the holder/partner move-scoring cases, `src/battle_ai_util.c`/`src/battle_ai_main.c`). Carriers: Espeon, the Natu line, the Hatenna line (observable innate), plus the pure-boon Mega rows whose real ability is Magic Bounce (Sableye / Absol / Diancie / Clefable Megas). Step 3.5: the Xatu + two Espeon sets free to their real `:x:`-stable Synchronize; the three all-innate Hatterene sets defer to Batch W. Also `DANCER` (**Tier 5.9 — a 1:1 clean-upside copy**): the instant *any* battler uses a dance move (foe *or* ally), the holder immediately copies it and still gets to use its own move that turn. It needed no new machinery — the engine already drives the copy off the per-battler `activateDancer` volatile (queued in `MoveEndQueueDancer`, `src/battle_move_resolution.c`; fired in `TryDancer`/`ABILITYEFFECT_DANCER`, `src/battle_util.c`), so that queue site's chosen-slot read gained one `IsInnateActive(battler, ABILITY_DANCER)` clause and `TryDancer` forces the pop-up to Dancer when the chosen slot differs (Sturdy/Magic Bounce precedent). AI-free (nothing reads Dancer); not breakable (Mold Breaker can't stop it, like the real ability), suppressible by Gastro Acid / Neutralizing Gas. Carriers: the four Oricorio forms (its sole ability, redundant-but-correct) plus a tight dance-themed flavor set that lacks it (Ludicolo, Bellossom, the Lilligant lines, Meloetta's two formes, Maractus — observable innate). Step 3.5: the two Oricorio frontier sets keep chosen Dancer (no other real slot) and defer to Batch W. **Known limitations:** only those abilities are honored; the *active, scripted* innates wired so far are Speed Boost + the Batch J end-turn effects (via the re-entrant end-turn driver), Cute Charm/Stench (on-hit, inline prechecks that predate the driver), the Batch K on-hit reactions Rough Skin/Iron Barbs/Gooey/Tangling Hair + the on-faint pair Aftermath/Innards Out + the on-hit boost/charge Steam Engine/Thermal Exchange/Wind Power + Cursed Body's move-disable + the Batch M on-hit stat boosts Justified/Stamina/Water Compaction/Anger Point + Rattled's Dark/Ghost/Bug-hit Speed boost (via the re-entrant target-side on-hit driver; Rattled's Intimidate half + Steadfast's flinch Speed boost are their own scripted sites, `BS_TryDefiantRattled`/`CancelerFlinch`), Pickpocket's contact item-steal (its own move-end step) and Magician's on-damage item-steal (via a new re-entrant attacker-side on-hit driver) — **Batch K is now complete** — and the **complete Batch L** switch-in actives — **Intimidate** (Attack drop), the information-reveal trio **Anticipation/Forewarn/Frisk**, the stat-change pair **Download/Supersweet Syrup**, and the switch-in effects **Unnerve** (Berry denial) + **Hospitality** (ally heal) — all via the new re-entrant switch-in driver (extended with a per-phase `abilityEffect` selector so Unnerve/Hospitality reach their own upstream switch-in cases); a wild-battle RUN attempt / Teleport blocked by an innate trapper names the holder's *chosen* ability in the flavor text (the trap still works — the party-menu switch message is correct); an innate **Sticky Hold** covers all the common item removal (including — as of **Batch X** — a Pickpocket's contact-steal and Corrosive Gas, whose blocks live in `jumpifability` script paths now made innate-aware); and **Duraludon** (whose data lists both Heavy and Light Metal) carries innate **Light Metal only**, since the two contradict as simultaneous innates. **Batch I's frontier-slot freeing (Step 3.5) is partial**: the status-immunity species are unusually innate-dense, so ~24 all-innate sets keep `.ability` on the now-innate ability (functional/CI-safe, just redundant) rather than take a game-wide-ability-deleting override; freeing them is deferred to a focused pass (the cleanly-tractable ~35 sets were freed via empty-slot overrides or stable real-slot repoints). **Batch S, by contrast, freed all ~64 of its hardcoded frontier sets** — 9 to a stable real slot, the rest via fork-owned overrides (empty-slot fills, or repurposes of an innate-redundant/pending slot audited against `test/battle`). **Batch J's frontier freeing (Step 3.5) is likewise partial**: five sets were freed to a stable `:x:` real slot (Breloom→Effect Spore, Goodra→Sap Sipper, Mr. Rime→Screen Cleaner, Arctovish→Water Absorb, Sandaconda→Sand Spit); the other ~40 keep `.ability` on the now-innate ability (functional/CI-safe, just redundant) because ~20 affected species now have **all** real abilities innate and need a game-wide override each — deferred to a focused pass. **Batch T's freeing (Step 3.5)** frees eleven sets (Raticate-Alola→Hustle, the Drifblim/Hawlucha/Sneasler Unburden sets→a stable real slot, the Accelgor/Slurpuff sets repointed to their existing overrides, and the Simi trio/Victreebel/Greedent→a new empty-slot override); Snorlax, Linoone, Hitmonlee, Liepard, Thievul, Dedenne and Appletun keep the now-redundant chosen ability (all real slots innate, no free complementary slot) — deferred like Batch J. Two species carry an innate that is **not** their canon slot-0 ability because the two would contradict: **Zangoose** gets innate **Toxic Boost** (its actual frontier identity), not Immunity (Immunity blocks the poison Toxic Boost needs — innate Immunity stays on Gligar/Snorlax); and **Spinda** gets innate **Tangled Feet**, not Own Tempo (Own Tempo blocks the confusion Tangled Feet needs). Zangoose detail: its data carries both Immunity (slot 0) and Toxic Boost (HA), which are contradictory as innates (Immunity blocks the poison Toxic Boost needs), so it gets innate **Toxic Boost** (its actual frontier identity) and innate Immunity stays on Gligar/Snorlax. Displayed on the repurposed summary-screen "Innates" page and the Frontier battle-info viewer. Registered in the runtime config system (`WITH_CONFIG`); test baseline forces it off. **Enabled (`TRUE`).** Covered by `test/fork/innate_abilities.c`. **Per-ability wiring (effect sites, AI, species rationale, pure-boon divergences) and the step-by-step extension playbook: [`INNATE_ABILITIES.md`](INNATE_ABILITIES.md); progress tracker: [`INNATE_ABILITIES_PROGRESS.md`](INNATE_ABILITIES_PROGRESS.md).** |
| New types | `FEATURE_NEW_TYPES` | `config/feature.h` | ✅ | Overwrites the types of selected species with a fork-defined typing. The override table lives in `src/fork/new_types.c`, **not** `gSpeciesInfo`, so upstream syncs never touch it; each row maps a species to its two replacement type slots (a pure type repeats in both). Applied at the single canonical accessor `GetSpeciesType()` (`src/pokemon.c`), so the re-typing flows everywhere with one hook: battle type matchups/STAB (battle setup fills `gBattleMons[].types` from it), the type icons, the Pokédex, the summary screen, and `IsSpeciesOfType()` (`GetTeraTypeFromPersonality()` and the `MON_DATA_TERA_TYPE` default were also routed through `GetSpeciesType` so type identity stays uniform). The in-battle dynamic type (`gBattleMons[].types`, mutated by Soak/Reflect Type/Roost) starts from the override and is otherwise untouched. **First entry re-types Galarian Ponyta (stock pure Psychic) and Galarian Rapidash (stock Psychic/Fairy) to Fire/Fairy.** Type identity only — movepool/abilities/learnset unchanged. Registered in the runtime config system so tests flip it with `WITH_CONFIG`; the test baseline forces it off. **Enabled (`TRUE`).** Covered by `test/fork/new_types.c`. Adding a species is documented in [`NEW_TYPES.md`](NEW_TYPES.md). |
| Item-free battle gimmicks | `FEATURE_FREE_GIMMICKS` | `config/feature.h` | ✅ | Drops the held-item / key-item / charge requirements for the four battle-transformation gimmicks and lets a mon pick among them with a picker. **(1) Item-free enablement:** Mega Evolution needs no Mega Stone, Z-Moves no Z-Crystal, Terastallization no Tera Orb (nor `B_FLAG_TERA_ORB_*`), Dynamax no Dynamax Band (nor `B_FLAG_DYNAMAX_BATTLE`); the bag/hold-item/charge checks in `CanMegaEvolve`/`CanUseZMove`/`CanTerastallize`/`CanDynamax` are skipped. **(2) Stat-based X/Y Megas:** with no stone to disambiguate, `GetMegaStoneForBattler()` (`src/battle_util.c`) picks the X/Y form by the battler's stats — physical (X) when Attack ≥ Sp. Atk, special (Y) otherwise, so a tie defaults to X — by feeding the stat-appropriate stone into the existing item-driven form-change machinery (so eligibility, the form change, and the fork Speed-Boost AI override all resolve through one path). Only Charizard and Mewtwo have X/Y forms. **(3) Item-free Z derivation:** any move becomes its signature Z-Move (matched on species+move) or its type-based Z-Move. **(4) Picker:** the candidate gimmicks per battler are tracked as a set (`gimmick.gimmickCandidates[]`) alongside the selected one (`usableGimmick`); during move selection **Start cycles** through every gimmick the mon can currently use, then off (`CycleGimmickSelection`/`RefreshGimmickTriggerSprite`, `src/battle_gimmick.c`). **(5) Restrictions are unchanged** — one gimmick per mon and once per type per trainer fall out of the existing `GetActiveGimmick`/`HasTrainerUsedGimmick` checks, so a trainer can still use e.g. one Mega and one Z across a battle but each mon picks one. A **transformed mon (Ditto/Imposter) cannot Mega Evolve** (`CanMegaEvolve` mirrors `CanBattlerFormChange`'s `B_TRANSFORM_FORM_CHANGES >= GEN_5` transform guard): item-free Mega no longer reads the held stone, which previously let a transformed Ditto arm Mega only for the form change to be silently refused *after* the gimmick was spent; Z-Move/Dynamax/Tera stay available to a transformed Ditto. The picker also **always opens unarmed on the base moves** — `PlayerHandleChooseMove` clears `playerSelect` before the initial move-name paint, so a stale armed selection (e.g. Dynamax's Max-move names) can't leak into a fresh menu. The **last-move cursor is also kept across a Dynamax**: the begin animation reuses the switch-in animation for its shrink/grow visual (same mon re-entering, passed as `dontClearTransform = TRUE`), which had reset the player's action/move cursors to the first slot — `BtlController_HandleSwitchInAnim` now only clears them on a genuine switch, so the picker reopens on your last-used move just like regular moves do. **(6) AI uses everything:** `ShouldTrainerBattlerUseGimmick` returns TRUE for all gimmicks for AI battlers, so Tera/Dynamax are no longer opt-in and every eligible AI mon gimmicks; with the per-type-once tracking the team spreads its gimmicks across mons by priority (existing `ShouldUseZMove`/`DecideTerastal` reconsideration still applies). **Knock Off keeps its utility:** `ShouldUseZMove` no longer upgrades Knock Off into its damaging Z-Move (Black Hole Eclipse) when the target holds a worth-removing item and the Z-Move wouldn't secure a KO the regular hit can't — the AI strips the item and saves the one-shot Z-Move instead (`src/battle_ai_util.c`, covered by `test/fork/ai_zmove_knock_off.c`). Makes trainer battles noticeably tougher by design. The Mega message reuses the item-neutral "fervent wish" line. **Known limitations:** Ultra Burst (Necrozma) deliberately **out of scope** — it still requires holding Ultranecrozium Z; the AI gimmick choice is highest-priority-available, not situationally scored; the extended frontier roster still carries its (now inert) Mega Stones / Z-Crystals as held items — re-itemizing those tuned sets is left as balance work (`species_tiers.c`), and the draft restriction `TeamHasGimmickItemConflict` is neutralized under the flag. **Max Move priority fix:** damaging Max Moves are now correctly priority 0 — `GetBattleMovePriority` (`src/battle_main.c`) previously read the *base* move's priority for Dynamaxed damaging moves, so e.g. a Dynamaxed Sucker Punch (→ Max Darkness) kept its +1; surfaced by free gimmicks letting any priority move Dynamax (regression test in `test/battle/gimmick/dynamax.c`). Registered in the runtime config system so tests flip it with `WITH_CONFIG`; the test baseline forces it off. **Enabled (`TRUE`).** Covered by `test/fork/free_gimmicks.c`. |
| Color-blind HP bar | `COLOR_BLIND` | `config/accessibility.h` | ✅ | First of a planned accessibility set. Recolors the in-battle HP bar's healthy (>50%) band from green to the EXP bar's blue, so the bands no longer put the confusable colors at the extremes for red-green color blindness (stock ramp is green→yellow→red; new is blue→yellow→red). 2-entry palette swap on `TAG_HEALTHBAR_PAL` entries 10–11 in `ApplyHealthbarColorBlindPalette()` (`src/battle_gfx_sfx_util.c`); the yellow/red bands and the EXP bar are untouched, no new graphics. **Enabled (`TRUE`) in this fork.** Battle bar only for now; the party-menu/summary HP bars use a different (palette-by-index) mechanism and still follow stock colors. |
| Clean battle healthboxes | `B_CLEAN_HEALTHBOX` | `config/battle.h`, `src/graphics.c`, `src/battle_interface.c` | ✅ | When `TRUE`, declutters the battle healthboxes two ways. **(1) Corner pointer/tail removed:** the dark triangle on each box corner that points at the mon (in doubles it overlaps the on-healthbox type icons from `B_SHOW_TYPES`). It's **baked into the box sprite art** (the bar "frame end" tiles are the box's right *border*, not the tail), so the flag swaps `gHealthbox{Singles,Doubles}{Player,Opponent}Gfx` for fork-owned `*_notail.png` variants via `#if` in `src/graphics.c`. Each tail was edited out by hand against the pixel maps — doubles corners reconstructed by mirroring the box's clean opposite corner; player/opponent tails cleared with the border/HP bar left intact. **(2) Singles EXP bar removed:** the singles **player** box's EXP bar + its wrap-around frame (which also crowded the type icons) is gone — the `*_notail.png` for that box has a clean rounded bottom (top border mirrored down) and no EXP frame, and `UpdateHealthboxAttribute()` skips drawing the EXP bar fill under the flag (`src/battle_interface.c`). HP bar, level/HP text, status, and nick are untouched everywhere. **Enabled (`TRUE`) in this fork.** Vanilla art/behavior fully preserved at `FALSE`. |
| Illusion-safe effectiveness readout | `B_SHOW_EFFECTIVENESS` (fix; no new flag) | `src/battle_controller_player.c` | ✅ | Stops the move-select type-effectiveness indicator (`B_SHOW_EFFECTIVENESS = ALWAYS` in this fork) from **leaking an Illusion disguise**: against a foe under an active Illusion, `CheckTypeEffectiveness` would call the real damage calc and surface e.g. Zoroark's Dark weaknesses through the disguise. The fork instead computes the readout from the **apparent (illusion) species' base types** via `CalcIllusionTypeEffectiveness` (`GetIllusionMonSpecies` + the public `GetTypeModifier`, mirroring upstream's `TryNoticeIllusionInTypeEffectiveness` "presumed" calc), falling back to the true calc once the Illusion is broken or absent. Damaging moves only — status moves read NORMAL on the standard path and can't reveal anything. **Known limitation:** pure type-chart only, so it deliberately ignores the hidden mon's ability/item and move-specific overrides (Freeze-Dry, Flying Press) — an acceptable trade for never exposing the real typing. `FORK:`-tagged. **Enabled (`TRUE`).** |

Legend: ✅ done · ⚠️ partial / has known limitations.

### Determinism (`DETERMINISTIC_*`)

An ongoing project to remove as much RNG from the game as possible, one source
at a time, so outcomes follow from player choices and battle state rather than
luck. Each source gets its own flag in `config/deterministic.h` (`FALSE` = stock
behavior); the fork enables them as they mature. As random upsides are removed,
the plan is to introduce compensating systems so battles stay balanced rather
than simply harder or easier. See **[`DETERMINISM.md`](DETERMINISM.md)** for the
full rationale and per-flag breakdown.

**Design intent — this is a single-player game.** These changes are *not* a
competitive-meta rebalance; there's no human opponent, so multiplayer concerns
(how a move like Scald shifts a metagame, EV-neutral tweaks, etc.) are out of
scope. What's being protected is the **single player's experience against the
AI**, above all in the Battle Frontier facilities, where the draw is sustaining a
**long win streak on skill and Pokémon knowledge**. In stock play a strong run
can be cut short by pure hax with no counterplay — a surprise crit, a Quick Claw
flip into a Sheer Cold OHKO, a flinch you never act through. Determinism removes
the unlucky *and* the lucky variance from that loop, so a streak comes down to
decisions rather than dice; the `BUFF_*` line then compensates for the lucky
breaks the player gives up.

Flags so far: `DETERMINISTIC_CRITICAL_HITS`,
`DETERMINISTIC_DAMAGE`, `DETERMINISTIC_PARALYSIS`, `DETERMINISTIC_FLINCH`,
`DETERMINISTIC_ADDITIONAL_EFFECTS`, `DETERMINISTIC_HOLD_EFFECTS`,
`DETERMINISTIC_ACCURACY_EVASION`, `DETERMINISTIC_ABILITIES`,
`DETERMINISTIC_STATUS`, and `DETERMINISTIC_MOVE_RESULTS` (all enabled — see
table). The general shape
is that a *random upside* (a lucky crit/burn/flinch) is swapped for something the
player can read off the board: a guaranteed-or-impossible outcome, or one gated
on a legible condition (super effective, STAB, "did they flinch last turn"). The
battle AI is taught each new rule so it keeps valuing moves by what will actually
happen.

**Test harness.** Each `DETERMINISTIC_*` flag is registered into the existing
runtime config system (`DETERMINISTIC_CONFIG_DEFINITIONS` in
`include/constants/config_changes.h`), so engine code reads it via
`GetConfig(DETERMINISTIC_X)` and battle tests toggle it per-test with
`WITH_CONFIG(DETERMINISTIC_X, TRUE)`. The `#define`s in `config/deterministic.h`
remain the production defaults (on in the shipped ROM); the per-test baseline
forces every flag **off** (`TestInitConfigData`), so the entire inherited test
suite keeps running against stock behavior untouched, and only the dedicated
`test/fork/deterministic_*.c` files opt in. This means **adding a new
determinism flag costs one `#define` + one line in
`DETERMINISTIC_CONFIG_DEFINITIONS` + swapping its consumption site(s) to
`GetConfig` — no scattering `#if` guards across the upstream suite.**

### Balance / buffs (`BUFF_*`)

An ongoing project to rebalance items and other game functionality, usually as
compensation for other changes we make (e.g. the `DETERMINISTIC_*` project trades
random upsides away, so some items get buffed to keep battles balanced). Each
tweak gets its own flag in `config/buff.h` (`FALSE` = stock behavior); the fork
enables them as they mature. Flags so far: `BUFF_SHELL_BELL` and
`BUFF_LEECH_SEED` (both enabled — see table).

`BUFF_*` flags ride the **same runtime config system and test harness** as the
`DETERMINISTIC_*` flags above (`BUFF_CONFIG_DEFINITIONS` in
`include/constants/config_changes.h`): engine code reads each toggle via
`GetConfig(BUFF_X)`, battle tests opt in with `WITH_CONFIG(BUFF_X, TRUE)`, and the
per-test baseline forces every flag **off** (`TestInitConfigData`) so the
inherited suite keeps asserting stock behavior. Where a buff has a *magnitude*
(not just on/off), the toggle is registered but the magnitude is a plain
compile-time constant alongside it — e.g. `BUFF_SHELL_BELL` (registered toggle) +
`BUFF_SHELL_BELL_DENOMINATOR` (tuning), mirroring how `DETERMINISTIC_DAMAGE` pairs
its toggle with `BASE_PERCENT`/`TURN_INCREMENT`. This keeps the boolean test
baseline uniform (`force off` = stock) while leaving the numbers tunable in one
fork-owned file.

## Known quirks / future work

- **Forced Lv100 leaves dual record tracking (`B_FRONTIER_FORCE_LVL_100`).** With
  only Open Level in use, the always-empty "Lv 50" block is dropped from facility
  record displays. The **Battle Factory** and **Battle Tower** results windows now
  do this (Open Level streak only), gated on `B_FRONTIER_FORCE_LVL_100` in
  `ShowFactoryResultsWindow` / `ShowTowerResultsWindow` (`src/frontier_util.c`).
  The remaining facilities' record windows still show both blocks — same treatment
  to follow. The save block also still persists Lv50 records that can no longer
  change — candidate to drop for space if needed.
- **Battle Dome at 6v6.** See the 6v6 Factory row: the Dome's fixed 3-mon
  coordinate tables aren't generalized yet, so its layout is wrong at 6 mons.
- **The Battle Factory and Battle Tower are wired up so far.** `B_FRONTIER_ENDLESS`
  is a Frontier-wide flag; the Battle Factory and the Battle Tower (Singles +
  Doubles) implement the endless flow (per-win BP scaling, "rest to leave /
  resume", Brain-at-50/100). The Tower additionally draws opponents from the
  competitive roster and disables Multi/Link (see its row). The other facilities
  still run their vanilla once-per-challenge flow until they are converted.
  Planned next: extend it to the rest, Tower opponent tier-gating + hard AI, and
  boss battles every 10 challenges (the Frontier Brain is the current stand-in).
  **Converting a facility follows a repeatable pattern** — the step-by-step
  checklist, shared infrastructure to reuse, the rental-vs-own-party split, and
  the footguns we hit (the `1`/`0` flag gotcha, charmap chars, `make release`,
  empty rental backups) are written up in
  [`FRONTIER_ENDLESS.md`](FRONTIER_ENDLESS.md). Read it before doing Palace/Arena/etc.
- **Bumped `FRONTIER_STAGES_PER_CHALLENGE` (7 → 10) is global.** Per review, the
  shared constant was changed rather than adding a Factory-specific one, so the
  other facilities now see 10-stage challenges too. Their fixed-size layout
  tables (e.g. Battle Pyramid floor/pickup offsets, floor-name arrays) still have
  7 entries, so those facilities are visually/functionally off until generalized
  — accepted for now, to be fixed alongside their endless conversion.
- **Dead "won challenge" lobby path.** With `B_FRONTIER_ENDLESS` on, the lobby's
  `CHALLENGE_STATUS_WON` handler is no longer reached by new runs (it only ever
  fires for a save left mid-WON by a pre-update build, or with the flag off).
  Kept for that compatibility; a later cleanup can remove it.

## CI / infrastructure

- **Hardened `Install binutils` CI step.** Upstream's CI installs the GBA
  toolchain with an inline `apt-get` step copy-pasted into each job, which hung
  intermittently on dpkg-lock/mirror stalls (retrying "fixed" it). We extracted
  it into a fork-owned composite action,
  [`.github/actions/install-binutils`](../.github/actions/install-binutils/action.yml),
  that (1) **caches the ~107 MB of toolchain `.deb`s** so jobs install from
  GitHub's cache instead of the intermittently-slow Ubuntu mirror — the actual
  root cause — and (2) wraps each apt invocation in a per-attempt `timeout` and
  retries with backoff, so a stalled mirror/dpkg-lock is killed and
  **auto-retried in-job** (no manual workflow restart), failing only after 3
  attempts exhaust. A generous `timeout-minutes` backstop on each call site is
  the outer safety net. Every CI job (`build.yml`) and the on-demand ROM build
  (`rom-artifact.yml`) call it via `uses:`. The `uses:` call sites in `build.yml`
  are tagged `FORK:` (it's upstream-owned); on a merge conflict, keep our
  `uses:` + `timeout-minutes` and port any upstream apt-package change into the
  action's package list rather than re-inlining the step.

## Conventions

- Intentional divergences from upstream inside upstream-owned files are tagged
  `FORK:` (greppable: `grep -rn "FORK:" src include .github`).
- Prefer adding behavior behind a config flag in `include/config/*.h` over
  patching core logic, so changes land in files we own and survive upstream
  syncs cleanly. See `CLAUDE.md` for the full rationale.
