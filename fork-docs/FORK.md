# Fork changes

This is a fork of [RHH's `pokeemerald-expansion`](https://github.com/rh-hideout/pokeemerald-expansion).
It tracks upstream and layers a set of custom features on top to build a
**standalone single-player romhack** centered on quality-of-life improvements to
the Battle Frontier facilities, growing into a fuller game over time. See
[`CLAUDE.md`](../CLAUDE.md) for the conventions and the upstream-sync process.

This file is an **index**, not a spec: each feature is gated by a config flag
whose comment in `include/config/*.h` is the source of truth for its exact
behavior. The table records what the flag comment can't — status, known
limitations, and where to look.

## Features

| Feature | Flag(s) | Where | Status | Notes |
|---|---|---|---|---|
| Boot straight to the main menu | `SKIP_TITLE_SEQUENCE` | `config/fork.h` | ✅ | Skips copyright/intro/title. RHH intro still plays if `EXPANSION_INTRO`. |
| Trim Prof. Birch's new-game intro | `SKIP_BIRCH_SPEECH` | `config/fork.h` | ✅ | Keeps look + name selection, drops the monologue. |
| Gender-neutral text | `GENDER_NEUTRAL_TEXT` | `config/fork.h` | ✅ | Replaces gendered wording with neutral text. Currently the new-game look picker; the flag can extend to other gendered strings later. Text-only; selection unchanged. |
| Start a new game at the Battle Frontier | `START_AT_BATTLE_FRONTIER` | `config/fork.h` | ✅ | Warps to the Frontier dock as if first arriving. See `SetBattleFrontierFirstArrivalState()` in `src/new_game.c`. No effect on FRLG. Also sets `FLAG_SYS_POKEMON_GET` up front — a new game gives no starter, so without it the overworld START menu would hide the POKéMON (party) option; the Battle Tower needs the player's own team (added via the debug menu), so the party screen is unlocked from the start. |
| Battle Frontier facility guide | _(no flag; part of the Frontier-start intro)_ | `BattleFrontier_OutsideWest`/`BattleFrontier_ReceptionGate` scripts, `VAR_FRONTIER_FACILITY_GUIDE`, `FLAG_HIDE_FRONTIER_FACILITY_GUIDE`/`_GREETER` | ✅ | Continues the redirected Scott intro: when the player steps out of the Reception Gate (the gate's Scott scene sets `VAR_FRONTIER_FACILITY_GUIDE = 1` and reveals a greeter), an `OnFrame` trigger has the greeter walk the player from the gate exit (26, 61) to the Battle Factory door (11, 39), then the greeter is swapped (`removeobject` + `addobject`) for a permanent directions-giver that stands by the entrance and, on interaction, points to any facility via a two-level multichoice menu (`MULTI_FRONTIER_GUIDE_AREAS`/`_WEST`/`_EAST`). The escort uses two hand-authored, BFS-derived movement paths (greeter leads one tile ahead, player follows directly behind in lockstep — scripted `applymovement` doesn't object-collide, so no spacing delay is needed); paths avoid every NPC home tile and `lockall` freezes wanderers for the walk. Exchange-shop directions are not yet listed (planned). |
| New-game option defaults | `NEW_GAME_TEXT_SPEED`, `NEW_GAME_BATTLE_STYLE` | `config/fork.h` | ✅ | Sets the initial text speed (default Fast) and battle style (default Set) for a fresh save in `SetDefaultOptions()`. Value-defines using `OPTIONS_*` constants; the player can still change them in Options. |
| Start with battle-gimmick items | `START_WITH_BATTLE_GIMMICK_ITEMS` | `config/fork.h` | ⚠️ partial | New games give Mega Ring / Z-Power Ring / Dynamax Band / Tera Orb in the bag (`GiveStartingBattleGimmickItems()` in `src/new_game.c`). Mega/Z-Move/Ultra Burst work right away. **Dynamax & Tera are intentionally left disabled** (`B_FLAG_DYNAMAX_BATTLE` / `B_FLAG_TERA_ORB_*` in `config/battle.h` stay `0`) — the player holds the items but can't use those gimmicks until a planned player-facing toggle wires those flags to real flags and sets them. |
| Debug menus always on | `DEBUG_OVERWORLD_MENU`, `DEBUG_BATTLE_MENU` | `config/debug.h` | ✅ | Overworld menu (hold R + Start), battle menu (Select). |
| 6v6 Battle Frontier | `B_FRONTIER_PARTY_SIZE_6V6` | `config/frontier.h` | ⚠️ partial | Frontier-wide (not just the Factory): singles **and doubles** use full 6-mon teams (doubles: two active, four in reserve — `FRONTIER_DOUBLES_PARTY_SIZE` is also 6 under the flag, kept ≤ `FRONTIER_PARTY_SIZE` so `MAX_FRONTIER_PARTY_SIZE` and the saveblock party arrays don't grow); Factory auto-rents the team (rental select skipped, always max IVs). The **party picker now supports up to 6** (vanilla only defined the FIRST..FOURTH selection-order labels, so the 5th/6th pick printed "ABLE"/"NOT ABLE"): added `PARTYBOX_DESC_FIFTH/SIXTH` + `gText_Fifth_PM`/`gText_Sixth_PM`, and `party_menu.c` maps the order through `sFrontierPartyOrderDescIds[]` instead of the vanilla contiguous `i + PARTYBOX_DESC_FIRST`. The lobby challenge-explanation and rules-board text say "a full team of six" instead of the vanilla "three" (assembler `.if` in `BattleFactoryLobby/scripts.inc`); the Tower lobby's "select the … POKéMON" prompts likewise say "six". **The flag is defined as `1`/`0`, not `TRUE`/`FALSE`** — unlike the other `B_FRONTIER_*` flags it feeds a numeric constant (`FRONTIER_PARTY_SIZE`/`FRONTIER_DOUBLES_PARTY_SIZE`) computed with `#if` that **map scripts read as a value** (`setvar VAR_0x8005, FRONTIER_PARTY_SIZE` in the Tower lobby); the script cpp pass leaves `TRUE` undefined, so `#if TRUE`→`#if 0` would have silently given scripts the vanilla 3 while C saw 6 (the picker then only let you choose 3). A literal `1` makes `#if` agree across the C and script cpp passes (`.if` still works). **Battle Dome is not yet generalized to 6** — its coordinate tables are stubbed so it builds, but the 6-mon layout is incorrect. |
| 6v6 + endless Battle Tower | `B_FRONTIER_PARTY_SIZE_6V6`, `B_FRONTIER_ENDLESS`, `B_FRONTIER_TOWER_DISABLE_MULTI_LINK` | `config/frontier.h`, `src/battle_tower.c`, `src/battle_frontier.c`, `BattleTowerLobby`/`BattleTowerBattleRoom` scripts | ⚠️ partial | Brings the Factory's 6v6 + endless treatment to the **Battle Tower** for Singles and Doubles. **Picks 6** (Singles/Doubles both select a full team via the picker fix above). **Endless:** the battle-room loop never ends after a fixed number of wins — **BP is awarded after every win** (per-win scaling shared with the Factory in `GiveBattlePoints`, the Tower branch reading `towerWinStreaks`), the **Salon Maiden (Anabel)** appears at the 50th/100th win (`gFrontierBrainInfo[TOWER].streakAppearances` `{50,100,50,1}` under the flag) and, once beaten, awards BP = the win number and the streak **continues** instead of warping out. **The opponent draw is keyed to the per-mode win streak, never `curChallengeBattleNum`** (`SetNextTowerOpponent`/`SetTowerBattleWon` derive `battleNum = winStreak % FRONTIER_STAGES_PER_CHALLENGE`); this is the independence mechanism — it lets a rested **Singles** run and a rested **Doubles** run continue **independently** (each mode keeps its own streak + active flag), **not** a difficulty ramp. There is **no scaling**: the opponents' mons are drawn uniformly from the competitive roster (see below). **"Rest"** saves and returns to the lobby (no reboot) with the player's real party and the streak left active; the lobby no longer auto-resumes a paused Tower challenge, so the player can leave and, on returning, talk to the attendant and **choose a fresh team** (`tower_init` preserves an active streak) to continue. **General opponents are drawn from the competitive roster** (`frontier_extended_mons.c`) like the Factory, via the shared `GetRandomFrontierExtendedMonId()` (uniform, format-aware) wired into `FillTrainerParty` for the Tower — always max IVs; the vanilla per-trainer themed `monSet`/high-tier gate is bypassed for the Tower only. **Each general opponent holds exactly one Mega Stone** (the player hand-picks their team, so opponents always bring a Mega): `FillTrainerParty` reserves a random slot for a guaranteed Mega holder, forbids megas elsewhere, and applies the shared gimmick rule (`frontier_draft.c`, caps Z-Crystals at one). **The Salon Maiden (Anabel) fields a fork-owned static competitive team** — modelled on her vanilla set (psychic specialist + legendary beasts + Lati twins), full-six, with separate Silver (50th win) and tougher Gold (100th win) variants (`battle_tower_trainers.c`, routed via `CreateFrontierBrainPokemon`). **Gym-leader bosses:** every set's last win (10th/20th/… that isn't a 50/100 Brain mark) is a real Hoenn gym leader — own overworld sprite, battle pic, name and class — fielding an authored competitive team with one legendary-tier mon and one Mega; chosen at **random**, the roster (`sTowerBosses[]`, 8 Hoenn leaders) is extensible (Elite Four/Champions later). A fork `BOSS_TRAINER` id range above `TRAINER_PLAYER` is recognized by branches in `FillTrainerParty`/`SetBattleFacilityTrainerGfxId`/`GetFrontierTrainerName`/`GetFrontierTrainerFrontSpriteId`/`GetFrontierOpponentClass`/`GetOpponentIntroSpeech`/`CopyFrontierTrainerText`. **Multi & Link Multi are disabled** (`B_FRONTIER_TOWER_DISABLE_MULTI_LINK`): those two attendants show a "not available" message; the code/scripts are kept intact behind the flag. **General opponents are tier-gated** like the Factory: no legendaries or mythicals, and at most one pseudo-legendary per team (`TierRejectsCandidate` from `frontier_draft.c`, every slot `TIER_NORMAL`); bosses/Brain return earlier so their fixed legendaries are unaffected. Known limitations: **hard AI** is still Factory-only; bosses are **pure-random** (no immediate-repeat avoidance — a mutable static would land in `.data`, which the ld script doesn't place for fork files); the boss/brain **battle music** is the default frontier track; resting *exactly* on a Brain-threshold win (49/99) can skip that Brain appearance; the shared `trainerIds` dedup list can briefly hold the other mode's trainers after a Singles↔Doubles switch; **gym-leader bosses *glide* in rather than walk** — their overworld sheets are only 3 frames (one pose per facing, no step cycle), unlike the Salon Maiden's full 9-frame walk sheet, so the approach to the battle spot slides; this is a sprite-art limitation (a real walk cycle needs ~6 new authored frames per leader, ×8), not a logic bug, left as-is for now (see the boss gfx hook in `frontier_util.c`). |
| Frontier battles forced to Lv100 | `B_FRONTIER_FORCE_LVL_100` | `config/frontier.h` | ✅ | Factory **and Battle Tower** force Open Level mode (both teams at `MAX_LEVEL`): the lobby skips the Lv50/Open multichoice entirely and the attendant just proceeds (assembler `.if` forces `VAR_RESULT = FRONTIER_LVL_OPEN` before the eligibility check). The Reception Gate "rules" guide NPC also hides the now-irrelevant level-mode entries (TWO STYLES / LV. 50 / OPEN LEVEL), using the reduced `MULTI_FRONTIER_RULES_LV100` list. See known quirks below. |
| Endless Frontier challenge | `B_FRONTIER_ENDLESS` (+ `FRONTIER_STAGES_PER_CHALLENGE`) | `config/frontier.h`, `constants/battle_frontier.h` | ⚠️ partial | A challenge no longer ends after a set number of wins — the player keeps battling indefinitely. A "set" is `FRONTIER_STAGES_PER_CHALLENGE` (bumped 7 → 10) wins. **BP awarded after every win** (by the attendant in the pre-battle room), scaling per set (2/win for the first set, 4 for the next, 6 for the next, …). **"Rest"** now saves + restores the party + returns to the lobby (no reboot), keeping the streak active; the attendant offers to resume. In-challenge / resume detection uses the win streak, not `curChallengeBattleNum`/`challengePaused`. Because a singles and a doubles challenge can both be rested at once, the rental team (the shared `rentalMons`) is snapshotted per battle mode into `frontier.factoryRentedMonsBackup` on rest and restored on resume, so the two modes keep separate teams. **Frontier Brain** moved to the 50th win (silver) / 100th win (gold), awarding BP = the current win number (50 / 100). Gated by `B_FRONTIER_ENDLESS` (C `#if` + script `.if`, vanilla preserved in each `.else`). **Lobby/pre-battle text is brought in line with the endless flow:** the rules board drops the "can't swap with the seventh TRAINER" note (there is no fixed final opponent), and the pre-battle-room attendant's cryptic opponent hints (type — "favors Fire" — and style — "slow and steady") are skipped entirely (the foe team is still generated; the unhelpful hint text just isn't shown), with the lobby NPC that grumbled about deciphering them reworded to match. The Battle Factory is the first facility wired up; the rest follow in later passes. |
| Frontier max PP | `B_FRONTIER_MAX_PP` | `config/frontier.h` | ✅ | Maxes PP Up bonus on all four move slots in `CreateFacilityMon`, the shared facility mon builder — so it covers every facility: Battle Factory (rentals, opponents, Brain), Tower/Dome/Palace/Arena/Pyramid trainers, Battle Tent, and multi-battle partners. |
| Frontier max IVs | `B_FRONTIER_MAX_IVS` | `config/frontier.h` | ⚠️ partial | Forces 31 IVs in every stat via `GetFactoryMonFixedIV` (the one Factory IV source), so rentals, opponents, and the Frontier Brain are all maxed instead of the vanilla per-challenge ramp. Factory-only for now; other facilities' IV ramps live elsewhere. |
| Frontier hard AI | `B_FRONTIER_HARD_AI` (+ `B_FRONTIER_HARD_AI_FLAGS`) | `config/frontier.h` | ⚠️ partial | Every Factory opponent and the Frontier Brain use the strongest AI preset (default `AI_FLAG_SMART_TRAINER` — basic AI + OMNISCIENT + smart switching/mon choices + PP-stall prevention + smart Tera) instead of the vanilla per-challenge scaling. Gated in `GetAiScriptsInBattleFactory`; Battle Tent keeps no AI. Tune via `B_FRONTIER_HARD_AI_FLAGS`. Factory-only for now. |
| Species-aware AI overrides | `AI_FLAG_SMART_SPECIES_LOGIC` | `src/fork/battle_ai_species_overrides.c`, `include/fork/battle_ai_species_overrides.h` | ⚠️ partial | Opt-in AI flag (bit 34, defined in the fork-owned header) that patches spots where the generic AI misplays specific mons; OR'd into `B_FRONTIER_HARD_AI_FLAGS` so the Frontier gets it. All logic lives in `src/fork/battle_ai_species_overrides.c`, reached from three small additive hooks (each gated by the flag): a switch veto in `ShouldSwitch` (`src/battle_ai_switch.c`), a move-score entry at `sBattleAiFuncTable[34]` and a Mega-defer in `ReconsiderGimmick` (`src/battle_ai_main.c`). Covers: **Palafin** — `AI_ShouldKeepTransformedFormIn` stops the AI voluntarily pivoting out a transformed Hero form (it'll still flee a guaranteed OHKO it can't win); **Sharpedo** — `AI_ShouldBankSpeedBoostBeforeMega` makes a pre-Mega Speed Boost mon Protect on its first turn to bank +1 Speed, then Mega Evolve next turn, but only when the Mega form *loses* Speed Boost (so Mega Blaziken, which keeps it, Megas immediately). Freshness is keyed off Speed still being at its default stage. Covered by `test/fork/ai_smart_species_logic.c`. Extend by adding cases to the module + a hook if a new decision surface is needed. |
| Factory swap: opponent summary | `B_FRONTIER_FACTORY_OPP_SUMMARY` | `config/frontier.h` | ✅ | On the post-battle rental-swap screen, selecting an *opponent* mon now opens the same Summary/Swap/Rechoose popup the player's mons get (vanilla jumps straight to "Accept this Pokémon?" with no way to inspect moves/stats first); "Swap" accepts the mon. Gated in `src/battle_factory_screen.c` (`Swap_*`). The duplicate-species guard still runs before the menu. The lobby rules-board "swap notes" text is flipped to say the player *may* check the stats of the mon they're about to receive (assembler `.if` on this flag). |
| Extended frontier roster | `B_FRONTIER_EXTENDED_MONS` | `config/frontier.h`, `src/fork/frontier_extended_mons.c` | ✅ | Replaces the vanilla `gBattleFrontierMons` on the Battle Factory's code paths with a fork-owned roster of modern competitive sets (tuned around the `DETERMINISTIC_*` changes), drawn **uniformly** (the vanilla tier ramp in `sInitialRentalMonRanges`/`GetFactoryMonId` is bypassed). The roster carries **several distinct builds per species** (so the opponent's exact set can't be read off the species) and tags each set **FORMAT_SINGLES/DOUBLES/BOTH** — `GetFactoryMonId` only draws sets valid for the current battle format. (Named "extended" rather than "factory/competitive" because other frontier facilities will adopt this same list.) The list is ordered by National Pokédex number. Draft rule additions: unique species (the vanilla `currSpecies` one-dup quirk is closed under the flag), unique held item, and **at most one Mega Stone + one Z-Crystal** per generated team (`TeamHasGimmickItemConflict`, applied to player rentals, opponents, and the Frontier Brain; post-win swaps can exceed this, which is fine — the per-battle Mega/Z limit still applies). **Tier-gated drafting** (via the species tier map, `TierRejectsCandidate`): the player's initial team and ordinary opponents carry **no legendaries/mythicals and at most one pseudo**; **every `FRONTIER_STAGES_PER_CHALLENGE`-th battle** (the last of each set) the opponent gains **one guaranteed legendary** at a random slot, the rest drafted normally; the **Frontier Brain** fields **one legendary + one mythical** at two random slots, the other four drafted normally. Forced slots are reserved with `ReserveForcedTierSlot` before drafting, so the special mon's party position is randomized. **Illusion stays functional:** a drafted Illusion mon (Zoroark, the Eon duo) is never placed in the team's *last* slot, where it would have no party member behind it to disguise as (`GetIllusionMonPartyId` bails on a last-slot Illusion mon, so the disguise silently never forms) — `IllusionMonRejectsSlot` (`src/fork/frontier_draft.c`) rejects such a candidate from `partySize - 1` during the draft (applied to Factory opponents, the Frontier Brain, and Tower opponents; covered by `test/fork/frontier_draft.c`). Roster swap is **Factory-only** for now via `GetFactoryMonsTable()`/`GetFactoryMonsCount()`; Tower/Dome keep `gBattleFrontierMons` until they adopt the list. **Generations I–IX are comprehensively built out — _every_ fully-evolved species has at least one build** (1–2 flavor/utility sets for niche mons, ~2–4 for stronger ones, mega & non-mega), **with a deliberate offense/defense balance** — wall/support/pivot sets (Eviolite/Assault Vest/Rocky Helmet/Heavy-Duty Boots tanks, hazard/Defog/cleric/Trick-Room support) alongside the sweepers. **All regional forms (Alolan, Galarian, Paldean, Hisuian) and their cross-gen evolutions** appear as their own entries, slotted at their base species' National Dex number, alongside Paradox Pokémon (with Booster Energy + Protosynthesis/Quark Drive), Ultra Beasts, and notable formes (Therian, Necrozma, Origin Dialga/Palkia, Urshifu, Calyrex riders, Ogerpon masks, Bloodmoon Ursaluna, the appliance Rotoms, etc.). **Held items are deliberately varied** — Leftovers, berries, Assault Vest, Rocky Helmet, Heavy-Duty Boots, type-boost items, and screens/utility items are favored over defaulting everything to Choice items / Life Orb. Roster rules: **only fully-evolved Pokémon appear**, except NFEs with a genuine niche their evolution doesn't dominate (Eviolite Chansey, Light Ball Pikachu); cross-gen evolutions are used in place of their pre-evos (e.g. Magnezone/Electivire/Magmortar/Rhyperior/Tangrowth replace the Gen I Magneton/Electabuzz/Magmar/Rhydon/Tangela). **Innate abilities are respected** — species with an innate Levitate (`src/fork/innate_abilities.c`) are never given a redundant Air Balloon, and a set's `.ability` slot carries a **complementary** chosen ability that runs alongside the innate Levitate/Regenerator/Unaware (e.g. Slowbro `OWN_TEMPO` + innate Regenerator, Rotom `LIGHTNING_ROD` + innate Levitate, Clefable `MAGIC_GUARD` + innate Unaware). For *ability-locked* innate species whose only real ability is the one now granted innately, that complementary ability is made selectable via the fork-owned **species ability overrides** table (`src/fork/species_ability_overrides.c`; see its row) rather than by editing upstream species data. Every set's `.ability` is verified legal through that hook by `test/fork/frontier_extended_roster.c`, so a typo can't silently downgrade to slot 0. Movesets are **not restricted to the species' in-game learnset** — flavorful/powerful coverage is allowed. The vanilla `FRONTIER_MONS_HIGH_TIER` (849) Level-50 gate is **disabled under the flag** (the roster has no tier ordering), so there is no 849-entry ceiling on the list. Saved rentals reference the roster by **array index** (no stable-ID layer), so inserting/removing entries mid-list invalidates an in-progress rented team in an existing save — appending is save-safe, mid-list edits are not. Tera data is recorded but inert (Tera disabled in this fork). Plain `#if`-gated (not the runtime-config/`WITH_CONFIG` system), like the other `B_FRONTIER_*` flags. **Post-battle rental swap reverts battle form changes:** a defeated opponent's in-battle form change (e.g. Palafin's Zero to Hero → `PALAFIN_HERO`) is only undone on the *player's* party at battle end, so renting that mon used to carry the changed form into the next battle (Zero to Hero firing on send-out); `CopySwappedMonData` now reverts the swapped-in mon to its base form (`src/battle_factory_screen.c`). **Authoring helpers** (data-file readability, no behavior change, designed for editing on mobile): each entry opens with a `{ // NNNN` National-Dex comment (like `innate_abilities.c`); EVs are written by name with `EVS(.hp = 252, .def = 252, .spd = 4)` (unset stats default to 0, order-free) instead of positional `TRAINER_PARTY_EVS`; natures are written by effect with `NATURE(DEF_UP, ATK_DOWN)` (boosted stat first, lowered second; neutral natures use `NATURE_NEUTRAL`) instead of recalling that `NATURE_BOLD` is +Def/-Atk. All three are defined in `include/fork/frontier_extended_mons.h` and compile to identical bytes. **Enabled (`TRUE`).** |
| Species ability overrides | _(data; no flag)_ | `src/fork/species_ability_overrides.c`, `include/fork/species_ability_overrides.h` | ✅ | Fork-owned `{species, slot, ability}` table + `GetSpeciesAbilityOverride()`, consulted by `GetSpeciesAbility()` (`src/pokemon.c`) — the single accessor every ability read funnels through — so an override behaves like a real ability everywhere (battle, summary, AI, the Battle Factory ability picker) **without editing upstream `gSpeciesInfo`**. Built for the extended roster: several Battle Factory sets carry an innate Levitate/Regenerator yet want a *second*, chosen ability on top of it, but their species are "ability-locked" upstream (their only real ability is the one now provided innately), so the chosen ability has no slot and `CreateFacilityMon` would silently fall back to slot 0. Each row gives that ability a real slot — an empty normal slot on ability-locked floaters (Rotom→Lightning Rod, Hydreigon→Sheer Force, Lati@s→Illusion, the lake trio→Trace/Moody/Victory Star, Cresselia→Cloud Nine, Giratina-Origin→Dragon's Maw, …) or the now-redundant Regenerator slot on full-slot innate-Regenerator mons (Tangrowth→Sap Sipper, Audino→Cute Charm, Alomomola→Water Absorb), or — the **all-abilities-innate** case — a species whose *every* real slot is now innate (Skeledirge = Blaze + Unaware both innate → chosen `MUMMY`), or — the lone non-ability-locked case — a real Hidden Ability that is dead weight on the roster's sets (Sceptile's Overgrow is innately latched and its HA Unburden does nothing on its non-consumable-item sets, so the slot is repurposed for its Mega's `LIGHTNING_ROD`). Each row's *chosen* ability is picked to be **stable** — cross-referenced against `INNATE_ABILITIES_PROGRESS.md` and preferring a `:x:` (never-an-innate) ability like Lightning Rod over a `:white_large_square:` (pending) one, since a pending pick has to be re-pointed once it lands as an innate. The whole table was audited on this rule: every row now hands out a `:x:` ability (Sceptile→Lightning Rod, Solrock→Drought, Cryogonal→Snow Warning, Vikavolt→Motor Drive, Ogerpon-Cornerstone→Earth Eater, …) except the two that hand out an already-implemented `:white_check_mark:` innate (Carnivine→Chlorophyll, Tornadus-Therian→Prankster). Separately, the slot a row *frees* must already be redundant via an *implemented* `:white_check_mark:` innate. The table is sorted by National Dex number with a trailing-comment dex on each row. `CreateFacilityMon`'s ability picker was pointed at `GetSpeciesAbility` (it read `gSpeciesInfo[].abilities[]` directly) so the override is visible when choosing the slot (`FORK:`-tagged, in `src/battle_frontier.c`). Modeled on `src/fork/innate_abilities.c` (data in a fork-owned file, never in `gSpeciesInfo`). Enforced by `test/fork/frontier_extended_roster.c`, which fails CI if any roster `.ability` doesn't resolve to a real slot through the hook. |
| Species tier map | _(data; no flag)_ | `src/fork/species_tiers.c`, `include/fork/species_tiers.h` | ✅ | Fork-owned species→tier classification table + `GetSpeciesTier(species)` / `SpeciesIsTier(species, tier)` accessors, so facility logic can **control what appears where**. Three tiers (this fork's own groupings, **not** the official Game Freak categories): **`TIER_MYTHICAL`** = the strongest "restricted" legends — box/cover legendaries (Groudon, Kyogre, Dialga, Zacian, Koraidon…), Mewtwo, Lugia/Ho-Oh, **plus** the official event Mythicals (Mew, Celebi, Jirachi, Arceus, Diancie, Magearna, Pecharunt…); **`TIER_LEGENDARY`** = the sub-legendaries (birds, beasts, lake trio, Regis, genies +Therian, musketeers, Tapus, Heatran, Cresselia, Urshifu, Ogerpon, the Loyal Three…); **`TIER_PSEUDO`** = the 600-BST pseudo-legendaries (Dragonite, Garchomp, Dragapult…), Ultra Beasts, Paradox Pokémon, and Treasures of Ruin. Derived from the upstream `isRestrictedLegendary`/`isSubLegendary`/`isMythical`/`isUltraBeast`/`isParadox` flags with those groupings layered on top. **Keyed by exact species id** (forme-specific), so each forme is classified on its own merits instead of inheriting a single tier from its base's National Dex number. This lets a powerful forme outrank its base — **Shaymin-Sky is `TIER_LEGENDARY`** (its fork-boosted Serene Grace) while ordinary Shaymin is `TIER_NORMAL` (simply absent from the table) — and a weak base sit below its formes — **base Calyrex is `TIER_NORMAL`** while its Ice/Shadow riders are `TIER_MYTHICAL`. Modeled on `src/fork/innate_abilities.c` (data in a fork-owned file, never in `gSpeciesInfo`). Currently covers the special species/formes used by the extended frontier roster; species not listed return `TIER_NORMAL`. **Consumer:** the Battle Factory draft (`B_FRONTIER_EXTENDED_MONS`) gates player/opponent/Brain teams by tier — see that row. |
| All species legal in Frontier | `B_FRONTIER_ALL_SPECIES_LEGAL` | `config/frontier.h`, `include/fork/frontier_legality.h` | ✅ | When `TRUE`, the per-species `isFrontierBanned` flag (set on legendaries/mythicals etc. in `gSpeciesInfo`) is ignored, so **every** species is legal in the Battle Frontier facilities. The upstream banned-flag reads were funnelled through `IsSpeciesFrontierBanned()` (a static-inline chokepoint in the fork-owned `frontier_legality.h`), which returns `FALSE` under the flag; covers the party-menu eligibility check (`src/party_menu.c`) and the `AppendIfValid` / `CheckPartyIneligibility` / caught-banned-species-list logic (`src/frontier_util.c`). With the flag on, the "ineligible Pokémon" list is always empty (no species is banned). Upstream `gSpeciesInfo` data is left untouched, so `FALSE` restores the vanilla per-species ban. |
| Disable Frontier battle recording | `B_FRONTIER_DISABLE_RECORD_BATTLE` | `config/frontier.h`, `src/frontier_util.c` | ✅ | When `TRUE`, no facility attendant offers to record a battle onto the Frontier Pass. Recorded-battle "playback" is not a video — it re-simulates the battle from a saved RNG seed + the players' per-turn inputs, so it only renders correctly if the engine reproduces the original run byte-for-byte. The expansion's far more stateful battle engine doesn't hold that invariant reliably (a long-standing **upstream** fragility, not a fork regression — the recording engine files `recorded_battle.c`/`frontier_pass.c` are stock upstream), so the replay desyncs into a glitched battle. Forces `FRONTIER_DATA_RECORD_DISABLED` on in `GetFrontierData()`, so every attendant's "Record" menu option falls through to its existing no-record menu (the scripts already branch on this field) and the Frontier Pass "Record" area has nothing to play. The Reception Gate "Frontier Pass" guide NPC also hides its RECORD help entry, using the reduced `MULTI_FRONTIER_PASS_INFO_NO_RECORD` list. The recording/playback code is left fully intact, so flipping this back to `FALSE` (or a future upstream determinism fix) revives the feature unchanged. |
| In-battle INFO viewer | `B_FRONTIER_BATTLE_INFO` | `config/frontier.h` | ⚠️ partial | In facilities where the bag is disabled (`BATTLE_TYPE_FRONTIER_NO_PYRAMID` — Tower, Dome, Palace, Arena, Factory, Pike; **not** the Pyramid, which keeps a working bag), the BAG action slot becomes **INFO**, opening a read-only screen (`src/fork/frontier_battle_info.c`, `CB2_FrontierBattleInfo`). **Styling** reuses stock assets only (no new art): the player's chosen Options window frame drawn around the window (`DrawTextBorderOuter` + `GetWindowFrameTilesPal`), the standard menu palette (`gStandardMenuPalette`) for conventional `TEXT_COLOR_*` text, a transparent window over a soft backdrop colour, and coloured page titles (red) / footer (blue) for hierarchy — the full-width window's char block is nearly full, so the frame is drawn on its own BG (with a heap tilemap buffer freed on close, since a static one would grow EWRAM into the heap). The same viewer can also be opened with **SELECT** from the in-battle "choose a Pokémon" party menu (e.g. picking a replacement for a fainted mon), so the player can review field/foe state before deciding; closing it returns to the party menu. The return target is configurable (`OpenFrontierBattleInfo(returnCallback)`): the action-menu path returns to the battle screen, the party-menu path re-opens the party menu (`CB2_OpenBattleInfoFromPartyMenu`/`CB2_ReturnToPartyMenuFromBattleInfo` in `src/party_menu.c`, gated on `ShouldReplaceBagWithInfo()` so it's battle-only). Five pages, **L/R** cycle (both directions), with a right-aligned `n/N` page indicator (`PrintPageIndicator`) and the last-viewed page/foe remembered across re-opens (the viewer is opened repeatedly per turn; closing saves `sLastPage`/`sLastFoeIndex`, reopening resumes there) — defaulting on the first open to **Speed Tiers**, the page that actually drives a turn decision, which now leads the cycle: **Field** (weather/terrain + entry hazards & side screens both sides; plus, under `DETERMINISTIC_DAMAGE`, the current turn and that turn's fixed damage multiplier so the player can read the exact roll — `DrawDeterministicDamageLine`), **Conditions** (each on-field battler's primary status + notable volatiles — confusion, leech seed, taunt, etc. — both sides), **Stat Changes** (each on-field battler's non-default stat stages, e.g. `Atk+2 Spe-1`, both sides), **Speed Tiers** (each **revealed** foe's *possible* Speed range — `min-max`, where min = 0 IVs/0 EVs/hindering nature ×0.9 and max = 31 IVs/252 EVs/boosting nature ×1.1 at the foe's known level, derivable from base stats alone so the player can place a foe on the speed tier without knowing its exact spread; only seen slots are listed — unseen ones are omitted entirely, so neither an unrevealed mon **nor the foe's party size** leaks — and they're sorted fastest-first by the top of their range so the list reads as a true tier. The player's active mons are listed above for comparison, but using their **effective** Speed — `GetBattlerTotalSpeedStat`, which folds in everything the player already knows about their *own* side (Choice Scarf, Tailwind, paralysis, stat stages, Speed abilities) — and the fastest of them anchors a per-foe comparison **glyph**: `▼` (`CHAR_DOWN_ARROW`) when your effective Speed already exceeds that foe's best-case base Speed, `▲` (`CHAR_UP_ARROW`) when even its worst-case base Speed exceeds yours, blank when the ranges overlap. **Glyph, not colour, on purpose** (legible for colour-blind players), and framed as a pure number comparison — your *known* Speed vs the foe's *possible base* Speed — **not** a guarantee of turn order, since the foe's range still deliberately ignores its own hidden Choice Scarf / paralysis / Speed ability. `DrawSpeedPage`/`CalcSpeedBound`/`AppendSpeedGlyph`, mirrors `CalculateMonStats`. The foe range is raw base-stat only), **Foe** (the foe's **revealed-only** party data; `<>` cycles mons — species/gender/level, `FNT` when fainted, plus moves/PP/ability/held-item). "Seen" comes from `gBattleStruct->partyState[...].sentOut`; species/gender/level/fainted read from the party once seen. **Illusion-safe:** a foe whose Illusion (Zoroark/Zorua) is currently `ILLUSION_ON` shows the *disguise* mon's species/gender/level (and Speed-tier range) — not the real one — via `GetFoeDisplayMon`, mirroring the health box (which also reads `GetIllusionMonPtr`); the true species reveals only once the Illusion breaks. Everything else is gated on genuine reveal, because `gAiPartyData` is the AI's *knowledge* model (pre-filled under `AI_FLAG_OMNISCIENT` and padded with STAB/status assumptions), not "what the player saw": used moves are tracked in `gBattleStruct->infoUsedMoves` (set at the move-use site), and ability/held-item in `gBattleStruct->infoAbilityRevealed`/`infoItemRevealed` (set in `RecordAbilityBattle`/`RecordItemEffectBattle`, but **only while a turn is actually executing** — `BattleInfoCanRevealNow()` skips the reveal when `gAiLogicData->aiCalcInProgress` is set, so the AI's *speculative* move evaluation no longer leaks the foe's ability/item before the player sees it. Without this gate the foe's ability read **"Prankster"** at battle start and on every switch-in, because `GetBattleMovePriority`'s Prankster check records through this path for every status move while the AI computes turn order. The *displayed* ability value is read from a reveal-time snapshot, `gBattleStruct->infoRevealedAbility[side][slot]` (written under the same gate), **not** from `gAiPartyData->mons[].ability`: that live knowledge model is freely overwritten by the AI's speculative move/switch evaluation (e.g. a benched Prankster mon simulated in the active slot records Prankster onto it) while the already-set reveal bit stays put — which made a foe whose Intimidate fired at battle start display as **"Prankster"** anyway. **Innate-aware (`FEATURE_INNATE_ABILITIES`):** a mon's chosen ability and each of its innates reveal *independently*. The chosen ability and `infoRevealedAbility` snapshot are gated as above; each witnessed innate sets its own slot bit in `gBattleStruct->infoRevealedInnates[side][slot]` (indexed exactly as `GetSpeciesInnate` iterates the species' list). `RecordAbilityBattle` decides chosen-vs-innate from the ability the *pop-up actually displayed* — `gBattleScripting.abilityPopupOverwrite` when set (an innate Levitate/Sturdy forces the pop-up to its name), else the recorded id — so a `recordability` that hands over the chosen ability during an innate pop-up no longer leaks it. The viewer prints the chosen ability plainly and appends only witnessed innates as `(+Name, …)`; an innate seen before the chosen ability reads **`? (+Levitate)`**, and a silent passive innate (Regenerator, Unaware, Filter, …) is never recorded so it never shows. The item bit is additionally set wherever an item visibly reveals itself — a fired hold effect at the `ItemBattleEffects` chokepoint in `battle_hold_effects.c` (Life Orb recoil, Leftovers/berry heal, status orbs, stat-boost berries; silent effects like Amulet Coin return `ITEM_NO_EFFECT` and are skipped), and the visible removal sites: Knock Off in `battle_move_resolution.c`, the `Cmd_removeitem` destruction path for Fling/Corrosive Gas/Incinerate, and steals via `StealTargetItem` — so an item the player watched activate or leave reads as its name or **`None`**, never `?`). Reuses the `B_ACTION_DEBUG` controller path so the turn isn't consumed; touches only the player controller, those record fns, the move-use & item reveal/removal sites (`FORK:` tags) + a menu-label string. **Foe page reads opponent A only** (single-trainer doubles / singles); two-opponent multi battles show just the first foe trainer's party for now. |
| Deterministic critical hits | `DETERMINISTIC_CRITICAL_HITS` | `config/deterministic.h` | ✅ | First of the `DETERMINISTIC_*` flags (a project to strip RNG from the game). Removes the random crit-chance roll in `IsCriticalHit()` (`src/battle_util.c`): crits still land only when *guaranteed* — always-crit moves, Laser Focus, Merciless vs. a poisoned target, or crit-stage stacks that already reach 1/1 odds. Crit-blocking (Battle Armor, Shell Armor, Lucky Chant) unchanged. The AI is made config-aware: its damage prediction (`ShouldCalcCritDamage` in `src/battle_ai_util.c`) no longer assumes partial-chance crits that can't land — counting a crit only when guaranteed, via the shared `IsGuaranteedCriticalHit()` predicate (also the source of truth in `IsCriticalHit`) — and its crit-*chance* move valuations (`src/battle_ai_main.c`: the Risky high-crit-move bonus, Focus Energy, and Dragon Cheer) stop rewarding chance it can no longer cash in (Laser Focus and always-crit synergies still count, since those crits are guaranteed). **Enabled (`TRUE`) in this fork.** Covered by `test/fork/deterministic_critical_hits.c` (opt-in via `WITH_CONFIG`); see the determinism test harness note below. |
| Deterministic damage | `DETERMINISTIC_DAMAGE` (+ `DETERMINISTIC_DAMAGE_BASE_PERCENT`, `DETERMINISTIC_DAMAGE_TURN_INCREMENT`) | `config/deterministic.h` | ✅ | Replaces the random 85%–100% damage roll with a fixed multiplier that scales with the battle's turn count: `BASE_PERCENT` on turn 1, `+TURN_INCREMENT` per elapsed turn (`DETERMINISTIC_DAMAGE_PERCENT` = `BASE_PERCENT + TURN_INCREMENT * gBattleTurnCounter`). Defaults 92% / +1% → 92% turn 1, 93% turn 2, … **Intentionally uncapped** — from turn 9 on it exceeds 100%, so prolonged battles deal more than the stock maximum (set `TURN_INCREMENT` to 0 for a flat multiplier). The AI's damage prediction (`src/battle_ai_util.c` roll helpers) is fed the same figure, so its min/median/max/random rolls collapse to the deterministic value and it never mis-predicts. **Enabled (`TRUE`) in this fork.** Covered by `test/fork/deterministic_damage.c` (opt-in via `WITH_CONFIG`); see the determinism test harness note below. |
| Deterministic flinch | `DETERMINISTIC_FLINCH` | `config/deterministic.h` | ✅ | An **anti flinch-lock cap** that *composes* with `DETERMINISTIC_ADDITIONAL_EFFECTS`: a flinch effect is first gated on super effective / STAB like any other effect (see that flag — Iron Head/Rock Slide only flinch on a super effective hit, Stomp only from a Normal user), and this flag then prevents a *gated* flinch from being re-applied to a foe that was flinched the previous turn, so a fast flincher can't chain it into an inescapable lock (each flinch "uses up" the next turn's). Implemented in `TryTriggerAdditionalEffect()` (`src/battle_util.c`); the "flinched last turn" bit is snapshotted per battler in `HandleEndTurn_ContinueBattle` (`src/battle_main.c`, `BattlerState.flinchedLastTurn`) just before the volatile is cleared. Guaranteed flinches (chance ≥ 100%) and **Fake Out** / any first-turn-only flincher are exempt (they bypass both the gate and the cap). Inner Focus / Shield Dust / Covert Cloak immunity unchanged. The AI's flinch valuation (`IsFlinchGuaranteed`, `AI_CalcAdditionalEffectScore` via `AI_IsAdditionalEffectReliable`) is taught the composed rule. Required by `DETERMINISTIC_ADDITIONAL_EFFECTS` so its gated flinches can't lock. **Enabled (`TRUE`).** Covered by `test/fork/deterministic_flinch.c`. |
| Deterministic additional effects | `DETERMINISTIC_ADDITIONAL_EFFECTS` | `config/deterministic.h` | ✅ | A move's chance-based additional effect (burn, paralysis, a stat drop, …) stops being a random roll and lands on a fixed, state-based condition: if the move's type **can** be super effective (every type but Normal in the stock chart), the effect lands only on a super effective hit (e.g. Fire Punch burns only a Fire-weak target); if the type **never** can (Normal), it lands only when the move is **STAB** (e.g. Body Slam paralyzes only from a Normal user). **Only applies to sub-100% effects** — guaranteed (≥ 100%) or chance-less (primary) effects always land, unchanged. **Flinch obeys the same gate** (Iron Head/Rock Slide only flinch on a super effective hit, Stomp only from a Normal user); `DETERMINISTIC_FLINCH` (required) then adds an anti-lock cap on top so a gated flinch can't chain into a stunlock. The stock secondary-chance boosters (**Serene Grace** and the **Pledge Rainbow**), which would otherwise be near-useless under this flag, instead make a holder's effect *certain* — bypassing the gate and always landing (detected as "computed chance > base chance" so both sources and their quirks fall out for free). This **includes flinch**: a boosted flinch lands even on a neutral/resisted hit, but still keeps the anti-lock cap (it just can't be re-applied next turn), so the boosters can't restore flinch-lock. Implemented in `TryTriggerAdditionalEffect()` / `DeterministicAdditionalEffectApplies()` (`src/battle_util.c`), called from `Cmd_setadditionaleffects`; the AI is taught the same condition via `AI_IsAdditionalEffectReliable` (`src/battle_ai_util.c`, used by `AI_CalcAdditionalEffectScore`). **Enabled (`TRUE`).** Covered by `test/fork/deterministic_additional_effects.c`. (The super-effective branch relies on a recorded damage result; in practice every one of the game's chance-based additional effects is on a damaging move, so this is always available.) |
| Deterministic paralysis | `DETERMINISTIC_PARALYSIS` (+ `DETERMINISTIC_PARALYSIS_PP_TAX`, `DETERMINISTIC_PARALYSIS_PRIORITY_TAX`) | `config/deterministic.h` | ✅ | Turns paralysis from a coin-flip into a flat, predictable tax. Removes the random 25% full-paralysis miss (`CancelerParalyzed`, `src/battle_move_resolution.c`) and the Speed cut (`GetBattlerTotalSpeedStat`, `src/battle_main.c`); in their place every move the paralyzed battler uses costs **+`DETERMINISTIC_PARALYSIS_PP_TAX` PP** (`CancelerPPDeduction`) and has its **priority lowered by `DETERMINISTIC_PARALYSIS_PRIORITY_TAX`** (`GetBattleMovePriority`), defaults 1/1, so it acts later in its bracket and burns PP faster while keeping full Speed (set either tax to 0 to drop it). **Quick Feet** (which already ignores the Speed drop) is exempt from both taxes. The full-paralysis roll still consults `RNG_PARALYSIS` (guaranteed pass via `RandomChance(1, 1)`) so `PASSES_RANDOMLY` tests stay valid. Because turn order (Speed + priority) is read through the shared engine functions, the AI's turn-order *prediction* tracks this automatically; the AI's paralysis *valuation* (`IncreaseParalyzeScore`) is also made config-aware so it values the priority-bracket demotion instead of the stale Speed-halving check. **Enabled (`TRUE`) in this fork.** Covered by `test/fork/deterministic_paralysis.c` (opt-in via `WITH_CONFIG`); see the determinism test harness note below. |
| Deterministic hold effects | `DETERMINISTIC_HOLD_EFFECTS` | `config/deterministic.h` | ✅ | Turns chance-to-trigger hold items into guaranteed one-shot **entry items**: the effect always fires, but only on the first turn the holder is on the field (its entry turn — `IsBattlersFirstTurn`, so leads *and* mid-battle switch-ins each get one window), then the item is consumed. **Focus Band** becomes a Focus Sash that works from **any** HP (not just full) but only on the entry turn — survives one lethal hit at 1 HP then is consumed (`GetAdjustedDamage` in `src/battle_util.c`, consumed via `BattleScript_HangedOnMsg`); like Sash, a multi-hit move still gets around it. **Quick Claw / Razor Fang share-effect aside**, **Quick Claw** always moves first in-bracket on the entry turn regardless of move, then is consumed (`TryChangingTurnOrderEffects`, `src/battle_main.c`; consumed in `BattleScript_QuickClawActivation`, which announces "The *item* was used up…" like the other consumed items). **Crit items** — Scope Lens / Razor Claw / Lucky Punch (Chansey) / Leek (Farfetch'd) — make the holder's **first landed attack** a guaranteed crit (honoring crit-blockers), then are consumed; this *composes* with `DETERMINISTIC_CRITICAL_HITS` to give the crit-item class a purpose again (`IsCriticalHit`). **Lansat Berry** behaves the same way once its HP threshold is reached: the berry is consumed and the holder's next attack is a guaranteed crit, reusing Laser Focus's volatile instead of a (dead-under-determinism) crit-stage boost (`CriticalHitRatioUp`). **Flinch items** — King's Rock / Razor Fang — flinch the target on the holder's **first attack that didn't itself flinch the target**, then are consumed; this includes an attack whose own flinch was **gated out by `DETERMINISTIC_ADDITIONAL_EFFECTS`** (e.g. a non-super-effective Rock Slide) — the rock fills the gap rather than bowing out just because the move *can* flinch (decided by the `!volatiles.flinched` check after the move's own effects resolve, so it never double-flinches). Like Fake Out the flinch is set via `SetMoveEffect` (not the additional-effect path), so it bypasses `DETERMINISTIC_FLINCH`'s anti-lock cap and flinches even a target that flinched last turn (`TryKingsRock`, `src/battle_hold_effects.c`). Crit/flinch items are consumed at move end via the new `MOVEEND_DETERMINISTIC_HOLD_CONSUME` (`src/battle_move_resolution.c`), which announces the consumption ("The *item* was used up…", `BattleScript_DeterministicHoldEffectConsume`) so the otherwise-silent crit/flinch is credited to the item. Separately, **Starf Berry** raises the holder's currently-highest stat instead of a random one (`RandomStatRaiseBerry`). The AI's OHKO reasoning (`ShouldTryOHKO`, `src/battle_ai_util.c`) and crit/damage prediction are taught the new behavior. **Evasion items (BrightPowder / Lax Incense) are intentionally left alone** here — handled by `DETERMINISTIC_ACCURACY_EVASION` (below). **Enabled (`TRUE`) in this fork.** Covered by `test/fork/deterministic_hold_effects.c` (opt-in via `WITH_CONFIG`). |
| Deterministic accuracy/evasion | `DETERMINISTIC_ACCURACY_EVASION` (+ `DETERMINISTIC_OHKO_MAX_HP_PERCENT`) | `config/deterministic.h` | ✅ | Accuracy/evasion stop being a hit/miss coin flip and become a **PP economy**. Every move that reaches the accuracy roll always hits (`DoesMoveMissTarget`, `src/battle_util.c`); semi-invulnerability, Protect and immunities are resolved earlier (`ResolveMoveEffects`) and still avoid the move. A sub-100% move's **max PP is scaled down by its base accuracy, rounded down** (a 5 PP/80% move → 4 PP; a 10 PP/85% move → 8) to pay for the misses it no longer suffers — applied in `CalculatePPWithBonus` (`src/pokemon.c`, so the overworld PP counter shows it) and clamped onto battle PP in `REQUEST_ALL_BATTLE` (`src/battle_controllers.c`) so stored/effective PP can't diverge when the flag is toggled; only the 50<acc<100 band is scaled (100%/0-acc exempt). A **per-use PP economy** (`CancelerPPDeduction`, `src/battle_move_resolution.c`) for moves targeting opponents, derived from the same net accuracy/evasion stage as the old hit calc (`GetAccEvasionStageDelta`, `src/battle_util.c` — so Keen Eye/Unaware/Foresight/Minds Eye and the repurposed Compound Eyes/Victory Star carry over): +acc/−evasion recover 1 PP per net stage, −acc/+evasion cost 1, accuracy and evasion cancel, doubles sums both foes. Always spends ≥1 PP (last PP spent if it can't pay in full), recovery clamped to max PP. Flat additive uncapped taxes (`GetDeterministicMoveTargetPPTax`): BrightPowder/Lax Incense, Sand Veil, Snow Cloak, Tangled Feet add 1 PP to offensive moves; Wonder Skin adds 1 to status moves; Hustle adds 1 to the user's physical moves; Micle Berry (the old accuracy berry) makes its next move ignore the user's accuracy drops and the foe's evasion increases (never taxed by them, still recovers from boosts/drops) plus a flat 1 PP. **OHKO moves** become always-hitting attacks dealing `DETERMINISTIC_OHKO_MAX_HP_PERCENT` (40%) of max HP instead of a full KO, but **keep the OHKO immunities** — Sturdy, Dynamax, a higher-level target, and type immunity still block them (`DoesOHKOMoveMissTarget` + `EFFECT_OHKO` in `DoMoveDamageCalc`). The AI treats every move as always-hitting (`Ai_SetMoveAccuracy`, `src/battle_ai_main.c`) so it neither avoids low-accuracy moves nor fears evasion. **Sleep moves** that weren't 100% accurate (Hypnosis, Sleep Powder, …) now cause drowsiness like Yawn instead of sleeping outright (intercepted in `Cmd_setnonvolatilestatus`/`TRIGGER_ON_MOVE`, `src/battle_script_commands.c`, via `BattleScript_DeterministicSleepBecomesDrowsy`); 100% Spore is unchanged. **Moves that were exactly 50% accurate** (Zap Cannon, Inferno, DynamicPunch) now require a recharge turn like Hyper Beam, set at move end via the new `MOVEEND_DETERMINISTIC_RECHARGE` reusing Hyper Beam's `rechargeTimer`/`gLockedMoves` state. **AI awareness:** the shared `MoveGainsDeterministicRecharge` predicate (`src/battle_util.c`) drives both the move-end hook and the AI, so the AI treats a 50% move as a recharge move (`AI_IsMoveEffectInMinus` downside + Instruct avoidance, `src/battle_ai_util.c`/`battle_ai_main.c`). For the sub-100% sleep→drowse conversion, the shared `MoveSleepBecomesDrowsy` predicate (`src/battle_util.c`, also driving the engine's `Cmd_setnonvolatilestatus`) gates `AI_CanPutToSleep`: such a move is treated like Yawn, so the AI won't waste it on a foe that is already drowsy. **Move-info display:** because accuracy is now meaningless, the in-battle move-info submenu (press L on move select, `MoveSelectionDisplayMoveDescription`, `src/battle_controller_player.c`) replaces its `ACC` field with the move's **projected net PP cost** this turn — `GetProjectedMovePPCost` (`src/battle_util.c`) mirrors `CancelerPPDeduction`'s deduction (base, Pressure, paralysis tax, the accuracy/evasion stage economy and flat item/ability taxes, minus accuracy/Micle refunds), projecting single-target moves against the first live foe. **Enabled (`TRUE`).** Covered by `test/fork/deterministic_accuracy_evasion.c`. |
| Deterministic abilities | `DETERMINISTIC_ABILITIES` | `config/deterministic.h` | ⚠️ partial | Strips RNG (and a random choice or two) out of abilities so they pay off on board state, not luck. All gated on `GetConfig(DETERMINISTIC_ABILITIES)` in `src/battle_util.c` unless noted. **Contact status/effects always *attempt* their effect** (immunity/substitute/contact checks still gate): Static (paralysis), Poison Point & Poison Touch (poison), Flame Body (burn), Cute Charm (infatuation — and the opposite-gender requirement is dropped, attempts regardless of gender), Toxic Chain (bad poison; roll in `SetToxicChainPriority`, `src/battle_script_commands.c`), Cursed Body (disable the used move). **Effect Spore** drops the trigger roll *and* the 3-way poison/paralysis/sleep pick and always attempts drowsiness/Yawn (`BattleScript_EffectSporeDrowsy`, new `STRINGID_EFFECTSPOREDROWSY` keyed to the attacker). **Stench** only attempts its flinch on the holder's first turn out (`IsBattlersFirstTurn`) but then always does, resolving at `MOVEEND_ABILITIES_ATTACKER` (before attacker hold items, so it precedes a King's Rock flinch). **End-of-turn:** Shed Skin always cures status, Healer always cures the ally's status, Harvest always recovers a used berry and, in sun, also heals 1/16 max HP (`BattleScript_HarvestActivatesSunHeal` + new `STRINGID_HARVESTHPGAIN`). **Quick Draw** only activates on the user's first turn out (`IsBattlersFirstTurn`, `src/battle_main.c`) — the attacker-ability mirror of Quick Claw. **Moody** compares the *raw* current values of the five battle stats (acc/evasion **always** excluded, ignoring `B_MOODY_ACC_EVASION`), raises the lowest-valued by +2 and lowers the highest-valued by −1, ties affecting every tied stat (`GetMoodyStatValue`). **Pickup** recovers the first valid used item in a fixed order — self → partner → directly-opposing foe → across foe — instead of a random battler. **Trace** (doubles) copies the directly-opposing foe (same flank) when traceable, else the other. **Forewarn** tie-breaks equal-power moves by battler/move-slot order instead of randomly. **Rivalry** keys off shared type instead of gender (+25% sharing a type, −25% sharing none). **AI awareness:** the Rivalry damage mod rides the shared `GetBattlerAbility` path so the AI's damage prediction tracks it for free; and `AI_DeterministicContactAbilityPunishes` (`src/battle_ai_util.c`) teaches the AI to treat a contact move into a known Static/Flame Body/Poison Point/Effect Spore/Cute Charm holder as a guaranteed downside (when its attacker can actually receive the status), so among equal-damage moves it prefers a non-contact one (via `AI_IsMoveEffectInMinus`/`CompareMoveEffects`, mirroring the Rough Skin/Iron Barbs handling). `AI_WhoStrikesFirst` (`src/battle_ai_util.c`) also models Quick Draw's guaranteed entry-turn first-strike, so the AI's turn-order prediction matches the engine. And `AI_DeterministicAbilityGuaranteesStatus` teaches the AI that its *own* Poison Touch (on a contact hit) / Toxic Chain (on any damaging hit) now guarantees poison, scored as a plus via `AI_IsMoveEffectInPlus` (reusing `AI_CanPoison`), so e.g. a Poison Touch user prefers an equal-damage contact move. And `StatusWillBeCuredDeterministically` (`src/battle_ai_util.c`) teaches the AI the flip side — that a non-volatile status inflicted on a target whose **known** ability cures it every end-of-turn is wasted, so it stops re-applying it: **Shed Skin** (unconditional) and **Hydration** (only while the target is being rained on, mirroring the engine's weather check via the AI's `AI_GetWeather()` view) are OR'd into all five status gates (`AI_CanPutToSleep`/`AI_CanPoison`/`AI_CanParalyze`/`AI_CanBurn`/`AI_CanGiveFrostbite`), so a doomed status scores the same −10 as a do-nothing move. **Healer is deliberately not modelled here** — it cures the *partner*, not the holder, so it isn't keyed on the target's own ability (it would need a doubles-only partner-ability lookup, plus an end-of-turn-liveness assumption for the Healer mon). **Overworld ability RNG is out of scope** (post-battle Pickup finds, Cute Charm/Synchronize & encounter-type/level/held-item biases) — deferred to the Battle Pyramid survival rework. **Enabled (`TRUE`).** Covered by `test/fork/deterministic_abilities.c`. |
| Deterministic status | `DETERMINISTIC_STATUS` (+ `DETERMINISTIC_INFATUATION_TURNS`, `DETERMINISTIC_INFATUATION_DMG_PERCENT`, `DETERMINISTIC_SLEEP_TURNS`) | `config/deterministic.h` | ✅ | Makes the status conditions whose effect is a roll into legible, state-based outcomes. **Infatuation**: the opposite-gender requirement is dropped (matching `DETERMINISTIC_ABILITIES`' Cute Charm) at all three application sites (Attract `Cmd_*`, `BS_TrySetInfatuation`, Cute Charm); instead of the 50% "won't attack" coin flip the battler always acts, and its moves **against the loved target deal `DETERMINISTIC_INFATUATION_DMG_PERCENT`% damage** (a final-damage modifier in `GetOtherModifiers`, `src/battle_util.c`, so the AI's shared damage prediction tracks it); the infatuation lasts a fixed `DETERMINISTIC_INFATUATION_TURNS` of the battler's actions (new `VOLATILE_INFATUATION_TIMER`, decremented in `CancelerInfatuation`, then cured via new `BattleScript_DeterministicInfatuationEnds`). **Sleep** always lasts `DETERMINISTIC_SLEEP_TURNS` turns (default 2, like Rest) at both the move (`MOVE_EFFECT_SLEEP`) and end-of-turn (Yawn/Effect Spore) sites. **Confusion** stops being a 2-5 turn chain of self-hit rolls (`CancelerConfused`): on its first confused action the battler takes **one** guaranteed 40-BP typeless self-hit but **still carries out its chosen move that turn** (the move is never denied); the volatile then **lingers until the battler's next action, when it snaps out**. Because the battler is never robbed of its action, a faster foe can't chain confusion into an action lock — and because the volatile persists in the interim, the foe can't *refresh* it either (a confused target can't be re-confused, and `AI_CanBeConfused` already declines one), so the self-hit costs at most once per spell of confusion. Implemented via a `confusionTurns == 1` snap-out sentinel and `BattleScript_DeterministicConfusionSelfDmg` (self-damages on `BS_ATTACKER` then returns so the move continues — it does not repoint `gBattlerTarget`, so the continuing move keeps its real target; a self-hit that would KO falls back to ending the move). Infinite confusion never snaps out (it self-hits each action). The AI's confusion *valuation* (`IncreaseConfusionScore`) is lowered to a light-chip `WEAK_EFFECT` to match (it no longer disables the foe). **Enabled (`TRUE`).** Freeze is out of scope (handled by the frostbite-over-freeze config). The AI's risk heuristics that treat a confused foe as possibly-incapacitated (e.g. Focus Punch / Counter safety) are not retuned. Covered by `test/fork/deterministic_status.c`. |
| Deterministic move results | `DETERMINISTIC_MOVE_RESULTS` (+ `DETERMINISTIC_MULTI_HIT_COUNT`, `DETERMINISTIC_MULTI_HIT_MAX_COUNT`, `DETERMINISTIC_POPULATION_BOMB_COUNT`, `DETERMINISTIC_POPULATION_BOMB_LOADED_DICE_COUNT`, `DETERMINISTIC_RAMPAGE_TURNS`, `DETERMINISTIC_WRAP_TURNS`, `DETERMINISTIC_WRAP_GRIP_CLAW_TURNS`, `DETERMINISTIC_PRESENT_POWER`, `DETERMINISTIC_PRESENT_HEAL_DENOMINATOR`) | `config/deterministic.h` | ✅ | Fixes or makes state-based the rolls baked into move outcomes. **Multi-hit** 2-5 moves always hit `DETERMINISTIC_MULTI_HIT_COUNT` (3); Skill Link and Loaded Dice guarantee `DETERMINISTIC_MULTI_HIT_MAX_COUNT` (5). **Population Bomb** (nominal 10 strikes, each rolling accuracy) would land all 10 once `DETERMINISTIC_ACCURACY_EVASION` makes every strike hit, so it is toned down to `DETERMINISTIC_POPULATION_BOMB_COUNT` (5); Loaded Dice or Skill Link restore the full `DETERMINISTIC_POPULATION_BOMB_LOADED_DICE_COUNT` (10). The other fixed-count moves (Triple Kick, Beat Up, Dragon Darts) are untouched (`SetRandomMultiHitCounter`/`CancelerMultihitMoves`). **Protect**-family moves used on consecutive turns always fail (`CanUseMoveConsecutively`, `src/battle_util.c`). **Rampage** (Thrash/Outrage/Petal Dance) always lasts `DETERMINISTIC_RAMPAGE_TURNS` (2). **Speed ties** are broken by a fixed ladder — higher raw base Speed → lighter weight → higher remaining-HP% → (only then) the random permutation, inverted under Trick Room — via the shared `DeterministicSpeedTieWins` (`src/battle_main.c`), also taught to the AI's `AI_WhoStrikesFirst`. **Binding** moves last `DETERMINISTIC_WRAP_TURNS` (4), or `DETERMINISTIC_WRAP_GRIP_CLAW_TURNS` (7) with Grip Claw (`SetWrapTurns`). **Tri Attack** burns a target whose Attack is higher, frostbites one whose Sp. Atk is higher, paralyzes on a tie. **Dire Claw** paralyzes if the target's Speed beats either defense, sleeps on a three-way Speed/Def/SpD tie, else poisons. **Magnitude**'s power is chosen by the attacker:target weight ratio (like Heavy Slam), keeping Magnitude's 10-150 range (`CalculateMagnitudeDamage`). **Present** always damages a foe (`DETERMINISTIC_PRESENT_POWER`, 80 BP) and always heals an ally (1/`DETERMINISTIC_PRESENT_HEAL_DENOMINATOR`, 1/4 HP), keyed off the target's side. **Fickle Beam** only doubles on a super-effective hit. **Shell Side Arm**'s physical/special pick defaults to physical on a projected-damage tie (its poison is already super-effective-gated by `DETERMINISTIC_ADDITIONAL_EFFECTS`). **Roar/Whirlwind/Dragon Tail/Red Card** drag out the next living party member in slot order (wrapping), so party order matters. **AI awareness:** multi-hit damage prediction uses the fixed counts (`src/battle_ai_util.c`); turn-order prediction uses the speed-tie ladder; the weight/effectiveness-based powers ride the shared damage calc. **Enabled (`TRUE`).** Covered by `test/fork/deterministic_move_results.c`. |
| Shell Bell buff | `BUFF_SHELL_BELL` (+ `BUFF_SHELL_BELL_DENOMINATOR`) | `config/buff.h` | ✅ | First of the `BUFF_*` flags (a project to rebalance items/functionality). Buffs Shell Bell recovery from the stock 1/8 of damage dealt to 1/`BUFF_SHELL_BELL_DENOMINATOR` (1/4 by default) in `TryShellBell()` (`src/battle_hold_effects.c`). Registered boolean toggle (test-flippable) + plain compile-time denominator, mirroring `DETERMINISTIC_DAMAGE`'s toggle+tuning split. **Enabled (`TRUE`) in this fork.** Covered by `test/fork/buff_shell_bell.c` (opt-in via `WITH_CONFIG`); see the fork-flag test harness note below. |
| Leech Seed buff | `BUFF_LEECH_SEED` (+ `BUFF_LEECH_SEED_DENOMINATOR`) | `config/buff.h` | ✅ | A `BUFF_*` flag. Two changes: (a) **stacking** - several battlers can seed one target at once, each draining it independently every turn (a doubly-seeded foe loses HP twice per turn, one share to each seeder); (b) re-using Leech Seed on a foe **you already seed** deals an **immediate 1/8 drain** instead of failing (seeding a foe only *another* battler seeded stacks your seed on). Per-tick (and immediate) drain fraction is 1/`BUFF_LEECH_SEED_DENOMINATOR` (kept at 8 = vanilla rate; the buff is the stacking + re-drain, not a bigger tick). Storage is additive: upstream's single-seeder `leechSeed` volatile is left as the "primary" seeder (truthiness/Baton Pass/AI), and a fork-owned `leechSeededBy` per-battler bitmask holds the full seeder set; the end-turn loop drains one seeder per engine step (low-index-first) via a re-entrancy tracker, the immediate re-drain reuses dedicated `BattleScript_LeechSeedReDrain*` scripts, and the AI's already-seeded penalties + end-of-turn drain prediction are flag-gated (`battle_ai_main.c`, `battle_ai_util.c`). Registered boolean toggle + plain compile-time denominator, mirroring `BUFF_SHELL_BELL`. **Enabled (`TRUE`) in this fork.** Covered by `test/fork/buff_leech_seed.c` (opt-in via `WITH_CONFIG`). |
| Innate abilities | `FEATURE_INNATE_ABILITIES` | `config/feature.h` | ⚠️ partial (Levitate, Regenerator, Unaware, Sturdy, Natural Cure, Prankster, Overgrow/Blaze/Torrent/Swarm, Swift Swim/Chlorophyll/Sand Rush/Slush Rush, Filter, Pressure, Stench, Battle Armor/Shell Armor, Speed Boost, Limber, Cute Charm, Oblivious) | First of the `FEATURE_*` flags. Some species gain one or more **innate** abilities that are always active *in addition to* their single chosen ability. The table lives in `src/fork/innate_abilities.c`, **not** `gSpeciesInfo`, so upstream syncs never touch it; each species maps to an `ABILITY_NONE`-terminated, variable-length innate list (no fixed cap). **Coverage is comprehensive for Levitate**, in two groups. (a) **Native-Levitate species** — everything that carries Levitate in any ability slot (Gastly, the Unown forms, Rotom appliances, Latias/Latios, Lati@s/Eelektross/Delphox/Chimecho megas, Hydreigon, etc.); for these the row is currently redundant (`ability == LEVITATE || IsInnateActive(...)` short-circuits, so no double pop-up/record) but **forward-looking** — a later `FEATURE_` flag will re-home them onto a different primary, at which point this table becomes the live source of their Levitate with the migration only touching `gSpeciesInfo`. (b) **Flavor floaters** — species with no native Levitate that hover/levitate by design (the Beldum & Gengar lines, Magnemite/Porygon/Klink, ghostly spirits, floating sword/teacup/candle/gem Pokémon, the lake & cosmic legendaries, several UBs/paradox, etc.), so their innate Levitate is **observable** (and is what the tests exercise). Forms are judged individually: **Gengar-Mega and Mewtwo-Mega-X are omitted** (grounded), as is **Gimmighoul's Chest form**; Flying-types are excluded throughout (they fly by wing, not levitation). **Terrain summoners are deliberately included** — the four Tapus (Surge abilities), Miraidon (Hadron Engine), and Galarian Weezing's Misty Surge build — because the fork makes an innate Levitate a *pure boon* (see below): they float over Ground moves and hazards yet still reap the terrain they set, so the innate only helps them. **Scope is deliberately narrow to minimize upstream merge-conflict surface:** rather than routing every "does this battler have ability X?" check through `BattlerHasAbility()` across hundreds of upstream-owned sites, the fork supports an **allowlist** of abilities whose innate behavior is wired in a targeted way, and grows it **one ability at a time**. **Today the allowlist is `LEVITATE`, `REGENERATOR`, `UNAWARE`, `STURDY`, `NATURAL_CURE`, `PRANKSTER`, the pinch abilities `OVERGROW`/`BLAZE`/`TORRENT`/`SWARM`, the weather speed-doublers `SWIFT_SWIM`/`CHLOROPHYLL`/`SAND_RUSH`/`SLUSH_RUSH`, `FILTER`, `PRESSURE`, `STENCH` (a 10% on-hit flinch — guaranteed first-turn under `DETERMINISTIC_ABILITIES` — run additively beside the chosen-ability dispatch at the move-end attacker site, with the King's Rock non-stacking guard in `TryKingsRock` made innate-aware so an innate holder's flinch item bows out too; bypasses the `DETERMINISTIC_FLINCH` anti-lock cap like the real ability since it sets the flinch via `SetMoveEffect`; a clean-upside 1:1 copy with no AI wiring needed), `BATTLE_ARMOR`/`SHELL_ARMOR` (the two identical crit-immunity abilities — a clean-upside 1:1 copy that forces `CRITICAL_HIT_BLOCKED` at the two `CalcCritChanceStage` crit sites, with the lone AI read — don't waste Laser Focus on a crit-immune target — made innate-aware), and `SPEED_BOOST` (the fork's first **active, scripted end-turn** innate — +1 Speed at the end of every turn — a clean-upside 1:1 copy fired through a small end-turn driver, see below), and `LIMBER` (the holder cannot be paralyzed — a clean-upside 1:1 copy at the paralysis block site `CanSetNonVolatileStatus` and the switch-in cure site `TryImmunityAbilityHealStatus` in `src/battle_util.c`, plus the out-of-battle Battle Pike status room; AI correct for free since its paralysis checks funnel through `CanSetNonVolatileStatus`), and `CUTE_CHARM` (a 30% chance to infatuate an opposite-gender attacker on contact — guaranteed regardless of gender under `DETERMINISTIC_ABILITIES` — run additively beside the chosen-ability dispatch at the move-end on-hit site via `TryCuteCharmInfatuate`, with the pop-up overwritten to Cute Charm; a clean-upside 1:1 copy, AI made innate-aware only at the deterministic contact-punish predictor).** The generic engine is in place for future abilities: innates satisfy "does this battler have ability X?" via `BattlerHasAbility(battler, ability)` (`src/battle_util.c`), but are **never the battler's copyable/swappable identity** — Trace, Skill Swap, Role Play, the ability pop-up, `RecordAbilityBattle`, etc. all still read only the primary slot (`GetBattlerAbility`), so those resolve **deterministically**. `IsInnateActive()` honors the exact same *suppression* as a real ability of that name (Gastro Acid, Neutralizing Gas, Mold Breaker on breakable abilities, Ability Shield, not-on-field) — suppression parity. **The Levitate _effect_, however, is a deliberate pure-boon divergence:** an innate Levitate is intentionally a touch stronger than a real one. It still grants Ground-move and entry-hazard immunity, but the fork *also* keeps the mon grounded for the **beneficial** ground interactions — **field terrain and Toxic Spikes absorption** — via `IsBattlerGroundedForBenefit()` (`src/battle_util.c`), wired into the terrain chokepoint (`IsBattlerTerrainAffected`), the Grassy-Terrain end-of-turn heal (`battle_end_turn.c`), and the Toxic-Spikes switch-in absorb (`battle_switch_in.c`). So an innate-Levitate mon reaps the terrain it/an ally sets and a Poison-type still clears Toxic Spikes — things a real Levitate forgoes (a real Levitate stays terrain-exempt/canon; only the innate diverges). This is why terrain summoners (the Tapus, Miraidon) and Poison floaters happily carry it. **Levitate is wired to full primary-ability parity:** grounding spine (`IsBattlerUngroundedByAbilityItemOrEffect`) and type-effectiveness calc (`src/battle_util.c`) grant Ground immunity; **markers** (`RecordAbilityBattle` → `gBattleHistory`, AI party memory, reveal bitmask) and the **ability pop-up** fire on block (pop-up forced to Levitate); **AI** sees it everywhere — on-field damage/grounding for free via the shared calc, plus innate-aware off-field switch evaluation (`battle_ai_switch.c`), doubles partner spread-move scoring (`battle_ai_main.c`), Sky Battle eligibility (`CanMonParticipateInSkyBattle`), and Battle Dome move rating (`battle_dome.c`); and **displays** list every allowlisted innate (no per-ability work) on the **repurposed summary-screen Contest Moves page** — this hack has no contests, so that page is retitled "Innates" and lists the species' innate abilities in place of the contest move slots (`PrintInnateAbilitiesPage`); when a species has more than one innate, pressing **A** opens a **browse cursor** (mirroring the Battle Moves page's move cursor, reusing the move-selector sprite): it highlights the first innate, Up/Down scroll the list and show each highlighted innate's description, and A/B exit — the list **scrolls** so more than four innates stay reachable (`SwitchToInnateBrowse`/`Task_HandleInput_InnateBrowse`/`MoveInnateCursor`); the Info page shows only the chosen ability, as in stock — and on the Frontier battle-info viewer (folded into the Foe page's "Ability:" line as a trailing "(+Name)" parenthetical, once revealed — kept on one line so the Moves list stays on-screen). **`REGENERATOR`** is wired as a silent **1/3-max-HP switch-out heal**, added additively at the single switch-out site (`Cmd_switchoutabilities`, `src/battle_script_commands.c`) and gated by `BattlerHasAbility`/`IsInnateActive` so it heals — and is suppressed (Gastro Acid etc.) — exactly like a real Regenerator; it writes the party mon's HP directly (mirroring the controller's `REQUEST_HP_BATTLE` handler) so it never clobbers the single switch-out controller emit a chosen ability (e.g. Corsola's Natural Cure) may already have queued. **AI is innate-aware**: the heal isn't in any shared calc the AI runs, so the AI's dedicated Regenerator switch/pivot reads (`ShouldSwitchIfAbilityBenefit`, the bad-odds and hazard-switchin checks in `battle_ai_switch.c`, and `ShouldPivot` in `battle_ai_util.c`) credit an innate Regenerator via `BattlerHasAbility()`, so it values an innate-only Regenerator's pivot heal. Its species list is the **canon Regenerator users** (so they keep the pivot heal whatever ability slot the build picks — Mega/regional forms included) plus a few **flavor regenerators** (Staryu/Starmie, the axolotl Wooper lines, Zygarde). **`UNAWARE`** is wired as a **calc modifier** that makes the holder ignore the foe's stat-stage changes — beside each of the four existing `ABILITY_UNAWARE` reads in `src/battle_util.c` (offensive and defensive stat stages in the damage calc, plus evasion/accuracy in `GetTotalAccuracy` and `GetAccEvasionStageDelta`) the innate routes the stage through `InnateUnawareBoonStage()`. **Like Levitate, the Unaware _effect_ is a deliberate pure-boon divergence:** a real Unaware blanks the foe's stat stage *both* ways — so it also ignores the foe's *drops* and takes more damage / deals less for it — whereas the innate ignores only the foe's *boosts* and keeps the foe's *drops* (always the favorable half), so an innate Unaware is strictly a boon. No pop-up. Suppression parity is automatic — Unaware is `breakable`, so an attacker's Mold Breaker drops both the chosen-ability path and the innate; on-field AI damage prediction is correct for free (it reads the real battler through `IsInnateActive`), and the off-field AI *setup* heuristics are now innate-aware too — the "don't boost against an Unaware foe" sites (`ShouldRaiseAnyStat`, `GetAllyStatChangeScore`, the Belly-Drum score in `battle_ai_main.c`) pair their `AI_IsAbilityOnSide` read with the fork helper `AI_IsInnateOnSide()`, and the Yawn evasion-dodge stay-in check (`battle_ai_switch.c`) gets an `IsInnateActive()` clause, so the AI no longer wastes a boost against an innate-only Unaware. Its species list is the **canon Unaware users** (Clefable, Bidoof/Bibarel, Woobat/Swoobat, Pyukumuku, the Fuecoco line, Dondozo, and the Wooper/Quagsire/Clodsire lines — those last carry it *with* their innate Regenerator) plus **flavor picks** too dull/dazed/asleep to notice the foe's buffs (Numel/Camerupt, the Psyduck line, Komala, the Snorlax line). The extended frontier roster's nine Unaware sets had their `.ability` slot freed to a complementary chosen ability (e.g. Clefable→Magic Guard, Pyukumuku→Innards Out, Dondozo→Water Veil) since the innate now supplies Unaware. **`STURDY`** is wired as the only **clean-upside** ability so far, so the innate is a plain **1:1 copy** (no pure-boon divergence needed): beside the two `ABILITY_STURDY` effect sites in `src/battle_util.c` — the full-HP **endure at 1 HP** (`GetAdjustedDamage`, `B_STURDY >= GEN_5`) and the **OHKO-move immunity** (the OHKO accuracy gate) — an `IsInnateActive()` clause grants the same effect, and the "endured"/Sturdy pop-up & message flow from the existing `MOVE_RESULT_STURDIED` / `MOVE_RESULT_ONE_HIT_KO_STURDY` flags (the pop-up's `abilityPopupOverwrite` is forced to Sturdy only when the chosen ability differs, so the real-Sturdy path is byte-for-byte untouched). Suppression parity is automatic — Sturdy is `breakable`, so an attacker's Mold Breaker pierces an innate Sturdy exactly as it would the real ability. **AI is innate-aware** here, and it *had* to be wired explicitly: unlike Levitate (immunity in the shared type calc) and Unaware (stat-ignore in the shared damage calc), Sturdy's survival reasoning lives in **dedicated AI helpers** that read the cached chosen ability — the endure/KO predictor (`CanEndureHit`), the OHKO-move avoidance, `BattlerHasMaxHPProtection` (`src/battle_ai_util.c`), and the switch-in KO simulation (`src/battle_ai_switch.c`) each now credit an innate Sturdy via `BattlerHasAbility()`/`SpeciesHasInnate()`, so the AI doesn't throw a hit it can't actually KO. Its species list is the **canon Sturdy users** (the Geodude/Onix/Aron/Roggenrola/Nacli rock-&-steel lines, Sudowoodo, Pineco/Forretress, Shuckle, Skarmory, Donphan, the Nosepass/Carbink/Regirock gem-rocks, Shieldon/Bastiodon, Dwebble/Crustle, Tirtouga/Carracosta, Tyrunt, Bergmite/Avalugg, Togedemaru, Archaludon — Mega/regional/Hisuian forms included so the innate survives a form change) plus the **flavor** Shellder/Cloyster line (the archetypal unbreakable shell); the Magnemite line carries it *alongside* its innate Levitate. Species whose **only** ability is Sturdy are omitted as redundant *when they aren't in the frontier roster* (Cosmoem, Togedemaru-Totem); **Cornerstone Ogerpon** is the exception — it's sole-Sturdy too but it *is* a roster set, so instead of omitting it the fork gives it the innate **plus** a chosen `DEFIANT` via the **species ability overrides** table (`src/fork/species_ability_overrides.c`), so its frontier slot isn't wasted on the now-innate Sturdy (the same ability-lock trick used for innate-Levitate floaters like Rotom). The roster's ~22 Sturdy sets had their `.ability` slot freed to a complementary chosen ability (e.g. Skarmory→Keen Eye, Sawk→Mold Breaker, Forretress→Overcoat, Regirock/Carbink→Clear Body, Ogerpon-Cornerstone→Defiant) since the innate now supplies Sturdy. **`NATURAL_CURE`** is wired as another **clean-upside** ability (a plain **1:1 copy**, no pure-boon divergence): at the same single switch-out site as Regenerator (`Cmd_switchoutabilities`, `src/battle_script_commands.c`) a `BattlerHasAbility`-gated block silently clears the holder's `status1`, additively next to the chosen-Natural-Cure path so a mon carrying it as an innate self-cleanses exactly like the real ability. Like the innate Regenerator there, it writes the party mon's status **directly** (mirroring the controller's `REQUEST_STATUS_BATTLE` handler), so it never clobbers the single switch-out emit a chosen ability — or the innate Regenerator heal beside it — may have queued. Silent (no script/pop-up), so no driver is needed; suppression parity (Gastro Acid / Neutralizing Gas / not-on-field) is automatic via `IsInnateActive()`. **AI is innate-aware**: the cure isn't in any shared calc, so the AI's dedicated Natural Cure switch reads credit an innate one via `BattlerHasAbility()` — the switch-to-cure heuristic (`ShouldSwitchIfAbilityBenefit`, factored into `ShouldSwitchForNaturalCure` exactly like Regenerator's helper), the Yawn anti-sleep switch (`ShouldSwitchIfBadlyStatused`), and the burned/frostbitten force-switch move scoring (`src/battle_ai_main.c`). Its species list is the **canon Natural Cure users** (the Chansey/Blissey line, Roselia/Roserade, Budew, Swablu/Altaria, Phantump/Trevenant, the Pawmi line — base forms only where a Mega/alternate forme drops the ability, e.g. Altaria-Mega→Pixilate) plus **flavor** herbal/aromatic healers (the Chikorita line's restorative aroma, Bellossom's revitalizing dance); the Staryu/Starmie line and Corsola carry it *alongside* their innate Regenerator, and Celebi/Comfey *alongside* their innate Levitate (combined lists). **Celebi and Shaymin are sole-Natural-Cure** like Cornerstone Ogerpon was sole-Sturdy, so they take the innate **plus** a fork-owned chosen ability via the **species ability overrides** table (Celebi→`TRIAGE`, Shaymin→`SERENE_GRACE`). The roster's 15 Natural Cure sets had their `.ability` slot freed to a complementary chosen ability (e.g. Blissey→Serene Grace, Starmie→Analytic, Trevenant→Harvest, Pawmot→Iron Fist, Altaria→Cloud Nine) since the innate now supplies Natural Cure. **`PRANKSTER`** is wired as a **+1 priority boost on status moves** at the single move-priority site (`GetBattleMovePriority`, `src/battle_main.c`): an `IsInnateActive()` clause sits beside the chosen-ability `IsAbilityAndRecord()` test (`IsInnateActive` is exported from `src/battle_util.c` for this — it credits the innate without recording or leaking the chosen ability, which matters because the call receives a *hypothetical* ability the chosen-slot test owns). No script/pop-up/driver — priority is a pure turn-order calc. **AI is innate-aware for free**: its turn-order prediction (`AI_WhoStrikesFirst` → `GetBattleMovePriority`) runs the same calc keyed off the real battler, so the AI both threatens and respects an innate Prankster's priority. **Like Levitate and Unaware, the Prankster _effect_ is a deliberate pure-boon divergence:** a real Prankster sets `gProtectStructs.pranksterElevated`, which makes its boosted status moves *fail* against Dark-types (`B_PRANKSTER_DARK_TYPES >= GEN_7`, `BlocksPrankster`); the innate keeps the +1 priority but never sets that flag, so its status moves still land on Dark-types — dropping the real ability's only cost. (Because the innate never elevates the flag, the AI's Dark-type avoidance check in `battle_ai_main.c` correctly leaves an innate Prankster's status moves unpenalized — no wiring needed; the doubles Psychic-Terrain heuristic in `battle_ai_field_statuses.c` *is* made innate-aware via a fork `AI_IsInnateOnSide()` helper, since Psychic Terrain blanks priority moves regardless of source — so unlike Unaware's analogous `AI_IsAbilityOnSide` setup gap, this one is closed.) Its species list is the **canon Prankster users** (Murkrow, Sableye, Volbeat, Illumise, Riolu, the Purrloin and Shroodle lines, Tornadus/Thundurus-Incarnate, male Meowstic, and the Impidimp line with Grimmsnarl-Gmax) plus a **small, on-theme flavor set** lacking the real ability — Hoopa (the "Mischief Pokémon," ring trickery), the playful Aipom line, and the illusion-trickster Unovan Zorua line; the flavor set is kept narrower than other abilities' because Prankster's +1 priority is potent. Cottonee/Whimsicott, Klefki, and Hoopa carry it *alongside* their innate Levitate (combined lists). A form is listed only where its ability data also carries Prankster — Banette/Sableye/Meowstic Megas and the Therian formes have a different signature ability and are omitted, like the Natural Cure form rule (the vengeful Hisuian Zorua/Zoroark are likewise excluded from the flavor set as a tonal mismatch). The roster's 19 Prankster sets had their `.ability` slot freed to a complementary chosen ability (e.g. Grimmsnarl→Pickpocket, Klefki→Magician, Tornadus/Thundurus→Defiant, Meowstic/Whimsicott→Infiltrator, Sableye→Keen Eye, Grafaiai→Poison Touch) since the innate now supplies Prankster; **Tornadus-Therian is the one Prankster set left untouched** — its forme is canon Regenerator, *not* a Prankster species, so it keeps the fork-owned chosen Prankster from its `species_ability_overrides.c` row. **The pinch abilities `OVERGROW`/`BLAZE`/`TORRENT`/`SWARM`** (+50% to Grass/Fire/Water/Bug moves at low HP) are wired by an **additive block** in `CalcAttackStat` (`src/battle_util.c`), placed *beside* the chosen-ability switch rather than editing its cases — so a real/chosen pinch ability stays vanilla and there's minimal conflict surface. **The innate _effect_ is a deliberate pure-boon divergence that LATCHES:** a real pinch ability only boosts while the holder is *currently* ≤1/3 HP, so healing back above the line strips it (notably an innate Regenerator's switch-out heal, but also Leftovers/a Berry); the innate instead sets a per-mon `reachedPinchHp` flag the first time the holder hits ≤1/3 HP (latched each end-of-turn in `src/battle_end_turn.c`, stored in `gBattleStruct`'s `PartyState` so it survives switch-out and resets per battle) and keeps the boost for the rest of the battle. A `chosen != ABILITY_X` guard in the block means a starter running its real pinch ability never double-applies. Suppression parity holds via `IsInnateActive()` (feature flag + Gastro Acid / Neutralizing Gas / not-on-field); pinch abilities aren't breakable, so Mold Breaker never touches them. **AI is correct for free** — the boost lives in the shared damage calc, which the AI runs keyed off the real battler via `IsInnateActive()`. Species list is **canon-only** (no flavor picks): every species whose ability data carries the pinch ability in any slot, so the signature survives whichever slot a build picks (a Chlorophyll Venusaur / Solar Power Charizard / Protean Greninja keeps its boost); Megas swap to a different ability (Thick Fat / Tough Claws / Drought / Mega Launcher / etc.) and are omitted, while Gigantamax forms and the Hisuian starters keep theirs. The Bulbasaur, Chikorita and Fuecoco lines and Volbeat already carry other innates, so they take a combined `INNATES(...)` list with the pinch ability added. The roster's **55** pinch sets had their `.ability` slot freed to a complementary chosen ability (e.g. Venusaur→Chlorophyll, Charizard→Solar Power, Greninja→Protean, Durant→Hustle) since the innate now supplies the boost; the Fuecoco line is the one **all-abilities-innate** case (Blaze + Unaware both innate), so Skeledirge takes a chosen `CURSED_BODY` via `species_ability_overrides.c`. **The weather speed-doublers `SWIFT_SWIM`/`CHLOROPHYLL`/`SAND_RUSH`/`SLUSH_RUSH`** (×2 Speed in rain/sun/sandstorm/snow) are wired at the single speed-calc site `GetBattlerTotalSpeedStat` (`src/battle_main.c`): each `ability == ABILITY_X` test gains an `|| IsInnateActive(battler, ABILITY_X)` clause, so an innate holder doubles exactly like the real ability. These are **clean-upside** abilities (no downside), so the innate is a plain **1:1 copy** — no pure-boon divergence. Sand Rush's *second* effect, immunity to end-of-turn sandstorm chip damage, is also granted (a boon): mirrored at the damage site (`src/battle_end_turn.c`) and the AI's two sandstorm-damage predictors (`DoesBattlerTakeSandstormDamage` in `battle_ai_util.c`, `GetSwitchinWeatherImpact` in `battle_ai_switch.c`). Suppression parity holds via `IsInnateActive()` — none of the four is `breakable`, so Mold Breaker never touches them. **AI is innate-aware**: turn-order prediction runs the same `GetBattlerTotalSpeedStat` keyed off the real battler, so it both threatens and respects an innate doubler's speed *for free* (innates are species-derived, so this never leaks a hidden chosen ability); additionally the AI's weather-**setting** heuristics (`DoesAbilityBenefitFromWeather` in `battle_ai_field_statuses.c`, `DoesAbilityBenefitFromSunOrRain` in `battle_ai_main.c`) credit an innate doubler so the AI sets the matching weather to enable it. Species list is **canon-only** (like the pinch abilities — a ×2-Speed weather sweeper is potent): every species whose ability data carries the doubler in any slot, so the signature survives whichever slot a build picks (a Rain Dish Ludicolo / Sand Force Excadrill keeps its doubling); forms are listed only where the form's ability data still carries it (Mega Swampert, Gigantamax Drednaw/Venusaur, Hisuian Qwilfish/Lilligant/Overqwil, the seasonal Deerling/Sawsbuck). Beartic carries **both** Swift Swim and Slush Rush; many species already carry other innates (the Bulbasaur/Tangela/Bellossom/Cottonee/Psyduck/Relicanth/Tirtouga lines) and take a combined `INNATES(...)` list. The roster's **47** doubler sets had their `.ability` slot freed to a complementary chosen ability (e.g. Ludicolo→Rain Dish, Excadrill→Sand Force, Dracovish→Strong Jaw, Sandslash→Sand Veil) since the innate now supplies the doubling; **Venusaur is the all-abilities-innate case** (Overgrow + Chlorophyll both innate), so it takes a chosen `THICK_FAT` via `species_ability_overrides.c`. **`FILTER`** is wired as another **clean-upside** calc modifier (a plain **1:1 copy**, like Sturdy): at the single defensive calc site `GetDefenderAbilitiesModifier` (`src/battle_util.c`) an `IsInnateActive()` clause applies the same **0.75× reduction to supereffective damage** beside the chosen-ability `FILTER`/`SOLID_ROCK`/`PRISM_ARMOR` switch case (guarded against those three so it never double-applies, and multiplied so it stacks correctly with any other defender-ability modifier such as a chosen Multiscale). No script/pop-up/driver, and the innate is **not** recorded as identity (like Unaware). Suppression parity is automatic — Filter is `breakable`, so an attacker's Mold Breaker pierces an innate Filter exactly as it would the real ability. **AI is correct for free** — the reduction lives in the shared damage calc the AI runs keyed off the real battler (like Unaware's stat-ignore), so the AI both threatens and respects it on-field; the off-field switch-in damage prediction is left unwired (the Unaware scope call — a 25% reduction is not a KO-flipping immunity like Levitate/Sturdy). Species list is **canon-only** (the Filter theme is hard to attribute beyond its real users): every species whose ability data carries Filter in any slot — Mr. Mime and Mime Jr. (slot-1 Filter), Revavroom (HA), and Mega Aggron (its Mega ability data is Filter; it carries this *alongside* the innate Sturdy that persists from base Aggron). The roster's three Filter sets had their `.ability` slot freed to a complementary chosen ability (Mr. Mime→Soundproof, the two Revavroom sets→Overcoat — its only other real slot) since the innate now supplies Filter. **`PRESSURE`** is wired as a **clean-upside** passive trait (a plain **1:1 copy** — it only ever costs the *foe* extra PP, so it never hurts its holder): at the two PP-deduction sites that read `ABILITY_PRESSURE` — the real deduction in `CancelerPPDeduction` (`src/battle_move_resolution.c`) and the fork-owned deterministic PP-refund mirror in `src/battle_util.c` — each `GetBattlerAbility(x) == ABILITY_PRESSURE` test (both the spread-move loop and the single-target branch) becomes `BattlerHasAbility(x, ABILITY_PRESSURE)`, so an innate holder makes its foes spend the extra PP exactly like a real Pressure. No script/pop-up/driver, and the innate is **not** recorded as identity (the cosmetic "exerting its Pressure!" switch-in message still fires only for the chosen ability, like all innate announcements). Suppression parity is automatic via `BattlerHasAbility()`→`IsInnateActive()` (Gastro Acid / Neutralizing Gas / not-on-field); Pressure isn't `breakable`, so Mold Breaker never touches it. **AI needs no wiring** — nothing in `src/battle_ai_*.c` reads `ABILITY_PRESSURE` for an effect (the PP tax isn't modeled in the AI's damage/turn calcs), so an innate Pressure is exactly as (in)visible to the AI as a real one. Species list is **canon-only** (the "exerts pressure" theme is hard to attribute beyond its real users, and the +1 PP tax is a potent stall tool): every species whose ability data carries Pressure in any slot — the legendary birds/beasts, Mewtwo, Lugia/Ho-Oh, Aerodactyl, Absol, the Deoxys formes, the creation trio (Origin formes included), Dialga/Palkia/Giratina-Altered, Spiritomb, Dusclops/Dusknoir, Vespiquen, Wailmer/Wailord, the Pawniard line, Kyurem, Corviknight, Eternatus, and the Sneasel→Weavile / Hisuian Sneasel→Sneasler lines — so the signature survives whichever slot a build picks; the Galarian birds, the Mega/Kyurem-B-W and Mewtwo-Mega-Y forms swap to a different signature and are omitted. Mewtwo, Dusclops, the Deoxys formes and Giratina-Altered carry it *alongside* their innate Levitate, and Ho-Oh *alongside* its innate Regenerator (combined lists). **Ho-Oh, the Deoxys formes, Kyurem and Eternatus** are **sole-Pressure** (or, for Ho-Oh, **all-abilities-innate**: Pressure + Regenerator), so each takes the innate **plus** a fork-owned chosen ability via `species_ability_overrides.c` (Ho-Oh→`FLAME_BODY`, Deoxys→`TRACE`, Kyurem→`SNOW_WARNING`, Eternatus→`POISON_TOUCH`). The roster's **34** Pressure sets had their `.ability` slot freed to a complementary chosen ability (e.g. Aerodactyl→Rock Head, the beasts→Inner Focus, Weavile→Pickpocket, the creation trio→Telepathy, Corviknight→Mirror Armor) since the innate now supplies Pressure. **`BATTLE_ARMOR` and `SHELL_ARMOR`** (the two crit-immunity abilities — identical effect: *attacks landed on the holder are never critical hits*) are wired as a **clean-upside** passive immunity (a plain **1:1 copy**, no pure-boon divergence, since crit immunity never hurts its holder): at the two crit-calc sites in `src/battle_util.c` (`CalcCritChanceStage` and the Gen-1 `CalcCritChanceStageGen1`) an `IsInnateActive()` clause beside the cached chosen-ability test forces `critChance = CRITICAL_HIT_BLOCKED` for an innate holder too. No script/pop-up/driver, and the innate is **not** recorded as identity (only the chosen-ability path calls `RecordAbilityBattle`, so the block is silent like Filter and Trace/Skill Swap still read the chosen slot). Suppression parity is automatic via `IsInnateActive()` — both are `breakable`, so an attacker's Mold Breaker pierces an innate Armor exactly as it would the real ability (the chosen path is pierced too: `GetBattlerAbility()` already returns `NONE` for a mold-broken breakable ability, so the cached read misses). **AI is innate-aware**: the shared damage calc's crit prediction runs through `CalcCritChanceStage` keyed off the real battler, so a predicted crit against an innate holder is blocked *for free*; the one dedicated AI read — "don't waste Laser Focus on a crit-immune target" (`EFFECT_LASER_FOCUS`, `battle_ai_main.c`) — now credits an innate holder via `BattlerHasAbility()`. Species list is **canon-only** (no flavor picks — crit immunity is hard to theme beyond an actual armored shell, and the set is already large): every species whose ability data carries Battle Armor or Shell Armor in any slot — the Krabby/Kingler, Lapras, Cubone/Marowak, Skorupi/Drapion, Anorith/Armaldo, Kabuto/Kabutops, Omanyte/Omastar, Shellder/Cloyster, Turtwig, Oshawott, Dwebble/Crustle, Escavalier, Shelmet, Corphish/Crawdaunt, Torkoal, Clamperl, Chewtle/Drednaw, Goodra (Hisui), Type: Null, Turtonator, Perrserker, Falinks, Klawf lines and more — so the signature survives whichever slot a build picks. Forms are listed only where the form's ability data still carries it: **Slowbro-Mega and Scolipede-Mega** gain Shell Armor (their Mega ability data is Shell Armor, replacing the base's), the **Hisuian** Sliggoo/Goodra carry it where the base forms carry Hydration instead, and the Drednaw/Kingler/Lapras Gigantamax forms keep theirs. Many of these already carry other innates (Kabuto/Omastar/Anorith's Swift Swim, the Turtwig/Oshawott lines' Overgrow/Torrent, Shellder/Dwebble's Sturdy, Slowbro-Mega's Regenerator, Escavalier's Swarm, Klawf's Regenerator), so they take a combined `INNATES(...)` list. The roster's 13 Armor sets had their `.ability` slot freed to a complementary chosen ability (Omastar/Kabutops→Weak Armor, Drapion→Sniper, Goodra-Hisui→Sap Sipper, Drednaw→Strong Jaw); the **all-real-abilities-now-innate** species take a fork-owned chosen override via `species_ability_overrides.c` (Armaldo/Samurott→`WATER_ABSORB`, Torterra→`SAND_STREAM`, Turtonator→`FLAME_BODY`). **`SPEED_BOOST`** is the fork's first **active, scripted end-turn** innate (+1 Speed at the end of every turn) — unlike the passive abilities above it can't be wired with a one-site clause, so it gets a small **end-turn driver**: `TryActivateInnateEndTurnEffects` (`src/fork/innate_abilities.c`) is hooked from a new `THIRD_EVENT_BLOCK_ABILITIES_INNATE` step of the end-turn loop (`src/battle_end_turn.c`), right after the chosen-ability end-turn block. The driver delegates to the upstream end-turn handler with the innate passed explicitly — `AbilityBattleEffects(ABILITYEFFECT_ENDTURN, battler, ABILITY_SPEED_BOOST, …)` — so the +1 Speed, the stat-change script and the pop-up are identical to the real ability; the pop-up's `abilityPopupOverwrite` is forced to Speed Boost only when the chosen ability differs (so a real Speed Boost stays byte-for-byte untouched, the Sturdy/Levitate precedent), and the driver skips an innate equal to the chosen ability so a real Speed Boost never boosts twice. A **clean-upside 1:1 copy** (Speed Boost never hurts its holder). Suppression parity is automatic via `IsInnateActive()` (Gastro Acid / Neutralizing Gas / not-on-field); Speed Boost isn't `breakable`, so Mold Breaker never touches it. **AI is innate-aware** at the two reads that key off a *foe's* Speed Boost — "don't bother lowering an innate Speed Boost foe's Speed" in `CanLowerStat` (`battle_ai_util.c`) and `CanStatChange` (`battle_stat_change.c`) — via `IsInnateActive()`; the ability-transfer scoring is left alone since innates are never transferable. The driver is **re-entrant** (a per-battler resume cursor in `gBattleStruct->eventState`): it fires one scripted effect per pass, holds the end-turn block until the battler's innate list is exhausted, and resets the cursor for the next battler — so a battler carrying several active end-turn innates fires each in turn. Speed Boost is just the only one on the allowlist today. Species list is **canon-only** (a +1 Speed-per-turn snowball is potent, like the pinch/weather/Pressure abilities): every species whose ability data carries Speed Boost in any slot — the Torchic line, Ninjask, Yanma/Yanmega, the Carvanha and Venipede lines, Flittle/Espathra, with Blaziken-Mega listed where its Mega data carries it (Sharpedo-Mega→Strong Jaw and Scolipede-Mega→Shell Armor are omitted). The Torchic line (innate Blaze) and the Venipede line (innate Swarm) take a combined `INNATES(...)` list. The roster's Speed Boost sets had their `.ability` slot freed to a complementary chosen ability (Ninjask→Infiltrator, Sharpedo→Rough Skin, Yanmega→Tinted Lens, Scolipede→Poison Point, Espathra→Opportunist); **Blaziken is the all-abilities-innate case** (Blaze + Speed Boost both innate), so it takes a chosen `SHEER_FORCE` via `species_ability_overrides.c`. **`LIMBER`** (the holder cannot be paralyzed) is wired as a **clean-upside** passive immunity (a plain **1:1 copy**, no pure-boon divergence): at the paralysis block site `CanSetNonVolatileStatus` (`src/battle_util.c`) an `IsInnateActive()` clause beside the chosen-ability test blocks the paralysis, reassigns `abilityDef` to Limber and forces the pop-up to Limber only when the chosen ability differs (the Sturdy/Levitate precedent, so the real path is byte-for-byte untouched), and at the switch-in cure site `TryImmunityAbilityHealStatus` an innate Limber clears pre-existing paralysis on switch-in exactly like the real ability. It's also mirrored at the out-of-battle Battle Pike paralysis room (`DoesAbilityPreventStatus`, `src/battle_pike.c`, feature-gated). Suppression parity is automatic — Limber is `breakable`, so an attacker's Mold Breaker pierces an innate Limber exactly as it would the real ability. **AI is correct for free** — its paralysis reasoning funnels through `CanBeParalyzed()`/`AI_CanParalyze()` → `CanSetNonVolatileStatus()`, whose fork clause reads `IsInnateActive()` keyed off the real on-field battler (not the passed-in ability), so the AI never tries to paralyze an innate-Limber foe; nothing in `src/battle_ai_*.c` reads `ABILITY_LIMBER` directly. Species list is the **canon Limber users** (the agile cats Persian/Glameow, the boneless contortionists Hitmonlee/Hawlucha/Clobbopus/Graploct, the flexible Buneary/Lopunny, the formless Ditto, and Stunfisk; Mega Lopunny→Scrappy and Galarian Stunfisk→Mimicry swap their data and are omitted) plus a tight **supple-serpent flavor set** lacking the real ability (the coiling Ekans/Arbok and Seviper). Purrloin/Liepard carry it *alongside* their innate Prankster and Mareanie/Toxapex *alongside* their innate Regenerator (combined lists). The roster's five Limber sets had their `.ability` slot freed to a complementary chosen ability (Persian/Graploct→Technician, Lopunny→Cute Charm, Liepard→Unburden, Toxapex→Merciless) since the innate now supplies Limber. **`CUTE_CHARM`** (when the holder is hit by a contact move, a 30% chance to infatuate the attacker if they are of opposite genders — guaranteed regardless of gender under `DETERMINISTIC_ABILITIES`) is wired as a **clean-upside** scripted on-contact innate (a plain **1:1 copy**, no pure-boon divergence — it only ever infatuates the *foe*): the existing Cute Charm body at the `ABILITYEFFECT_MOVE_END` on-hit site (`src/battle_util.c`) is factored into a `TryCuteCharmInfatuate()` helper, and because that dispatch keys off the *target's* `gLastUsedAbility`, an innate Cute Charm whose chosen ability differs is run additively in a pre-check beside the switch (guarded `!= ABILITY_CUTE_CHARM` so a real Cute Charm never infatuates twice). The effect reuses the upstream `BattleScript_CuteCharmActivates` (pop-up + infatuation), so the one extra step a pop-up'd innate needs is forcing `abilityPopupOverwrite` to Cute Charm when the chosen ability differs (the Limber/Speed Boost precedent), so the pop-up shows Cute Charm; the real-Cute-Charm path is byte-for-byte untouched. Unlike Speed Boost it needs **no driver** — the contact dispatch already fires once per hit. Suppression parity is automatic via `IsInnateActive()` (Gastro Acid / Neutralizing Gas / not-on-field); Cute Charm isn't `breakable`, so Mold Breaker never touches it. **AI is innate-aware**: the only AI read of `ABILITY_CUTE_CHARM` is the `DETERMINISTIC_ABILITIES` contact-punish predictor `AI_DeterministicContactAbilityPunishes` (`src/battle_ai_util.c`), which now also credits an innate Cute Charm on the defender via `BattlerHasAbility()` so the AI treats contact as a downside even when the chosen ability differs; under non-deterministic play neither a real nor an innate Cute Charm is modeled, so parity holds. Species list is **canon-only** (no flavor picks — infatuation can fully disable a foe for a turn, a potent disruption, like Prankster/the pinch abilities): every species whose ability data carries Cute Charm in any slot — the Clefairy and Jigglypuff lines, the Skitty/Delcatty and Minccino/Cinccino lines, Milotic (HA), Lopunny, Sylveon, Stufful (HA), and Enamorus-Incarnate — so the signature survives whichever slot a build picks; forms lacking it in their data (Buneary, Bewear, Enamorus-Therian, Clefable-Mega) are omitted. Clefable carries it *alongside* its innate Unaware and Lopunny *alongside* its innate Limber (combined lists). The roster's Cute Charm sets had their `.ability` slot freed: Enamorus→Contrary (its real HA), while **Lopunny is the all-non-drawback-abilities-innate case** (Cute Charm + Limber both innate; Klutz is a drawback), so it takes a chosen `SHEER_FORCE` via `species_ability_overrides.c`. (Audino is intentionally *not* given the innate — its ability data lacks Cute Charm; it only runs Cute Charm as a fork-chosen ability via the override table, so its frontier set keeps that and needs no freeing.) **`OBLIVIOUS`** (the holder cannot be infatuated or Taunted (`B_OBLIVIOUS_TAUNT` >= `GEN_6`) and is unaffected by Intimidate (`B_UPDATED_INTIMIDATE` >= `GEN_8`) — a passive trait, no driver, a clean-upside 1:1 copy). Wired innate-aware at the scattered immunity sites: the Attract/Taunt blocks (`src/battle_script_commands.c`), the generic `BS_TrySetInfatuation` setter, the Captivate and Intimidate blocks (`src/battle_stat_change.c`), the Cute-Charm self-infatuation check on the contacting attacker, and the immunity-ability cure of pre-existing infatuation/Taunt (`TryImmunityAbilityHealStatus`, `src/battle_util.c`) — each overwriting the pop-up to Oblivious when the chosen ability differs (the Limber/Cute Charm precedent). Suppression parity via `IsInnateActive()` (Oblivious is breakable, so Mold Breaker pierces it like the real ability). AI made innate-aware at the foe reads `AI_CanBeInfatuated`, `CanIntimidateLowerOpponentAtk` (`src/battle_ai_switch.c`), and the Cute-Charm contact-punish predictor's attacker check (`src/battle_ai_util.c`); the AI's Taunt scoring models no Oblivious immunity even for the real ability, so it needs none for the innate. Canon-only (no flavor picks): the canon roster already embodies the "oblivious" theme — every species whose data carries Oblivious in any slot gets it (the Slowpoke/Numel/Spheal/Swinub lines, Smoochum/Jynx, the Wailmer line, Feebas, Dondozo, etc.), many via a combined list alongside an existing innate; forms are listed only where their data still carries it (the Slowpoke Galarian/Mega forms and Tsareena are omitted). The roster's Oblivious sets had their `.ability` slot freed: Whiscash→Hydration (its real HA), and Dondozo (Unaware *and* Oblivious now both innate) takes its real Water Veil slot. **Adding the next ability** (the ability-by-ability workflow): wire that ability's specific effect at the relevant site(s), extend the allowlist comment in `src/fork/innate_abilities.c`, add the species row, and add a test. **Known limitations:** (1) only `LEVITATE`, `REGENERATOR`, `UNAWARE`, `STURDY`, `NATURAL_CURE`, `PRANKSTER`, `OVERGROW`/`BLAZE`/`TORRENT`/`SWARM`, `SWIFT_SWIM`/`CHLOROPHYLL`/`SAND_RUSH`/`SLUSH_RUSH`, `FILTER`, `PRESSURE`, `STENCH`, `BATTLE_ARMOR`/`SHELL_ARMOR`, `SPEED_BOOST`, `LIMBER`, `CUTE_CHARM`, and `OBLIVIOUS` are supported — declaring any other ability as an innate would silently do nothing (no effect site honors it yet); (2) **Speed Boost** (end-turn) and **Cute Charm** (on-contact) are the active, scripted innates wired so far — Speed Boost via the re-entrant end-turn driver above, Cute Charm via the existing per-hit contact dispatch (no driver) — but the other **active** innates (switch-in/on-contact effects like Intimidate, Static, Rough Skin) and the field/side helpers and trapping remain **removed** from the scale-back — they return as their abilities join the allowlist (the end-turn driver is already re-entrant, so it supports a battler carrying several active end-turn innates — Speed Boost is just the only one wired today). Registered in the runtime config system so tests flip it with `WITH_CONFIG`; the test baseline forces it off. **Enabled (`TRUE`).** Covered by `test/fork/innate_abilities.c`. **Extending it** ("add ability X as an innate for species A/B/C") is documented step-by-step in [`INNATE_ABILITIES.md`](INNATE_ABILITIES.md). |
| New types | `FEATURE_NEW_TYPES` | `config/feature.h` | ✅ | Overwrites the types of selected species with a fork-defined typing. The override table lives in `src/fork/new_types.c`, **not** `gSpeciesInfo`, so upstream syncs never touch it; each row maps a species to its two replacement type slots (a pure type repeats in both). Applied at the single canonical accessor `GetSpeciesType()` (`src/pokemon.c`), so the re-typing flows everywhere with one hook: battle type matchups/STAB (battle setup fills `gBattleMons[].types` from it), the type icons, the Pokédex, the summary screen, and `IsSpeciesOfType()` (`GetTeraTypeFromPersonality()` and the `MON_DATA_TERA_TYPE` default were also routed through `GetSpeciesType` so type identity stays uniform). The in-battle dynamic type (`gBattleMons[].types`, mutated by Soak/Reflect Type/Roost) starts from the override and is otherwise untouched. **First entry re-types Galarian Ponyta (stock pure Psychic) and Galarian Rapidash (stock Psychic/Fairy) to Fire/Fairy.** Type identity only — movepool/abilities/learnset unchanged. Registered in the runtime config system so tests flip it with `WITH_CONFIG`; the test baseline forces it off. **Enabled (`TRUE`).** Covered by `test/fork/new_types.c`. Adding a species is documented in [`NEW_TYPES.md`](NEW_TYPES.md). |
| Item-free battle gimmicks | `FEATURE_FREE_GIMMICKS` | `config/feature.h` | ✅ | Drops the held-item / key-item / charge requirements for the four battle-transformation gimmicks and lets a mon pick among them with a picker. **(1) Item-free enablement:** Mega Evolution needs no Mega Stone, Z-Moves no Z-Crystal, Terastallization no Tera Orb (nor `B_FLAG_TERA_ORB_*`), Dynamax no Dynamax Band (nor `B_FLAG_DYNAMAX_BATTLE`); the bag/hold-item/charge checks in `CanMegaEvolve`/`CanUseZMove`/`CanTerastallize`/`CanDynamax` are skipped. **(2) Stat-based X/Y Megas:** with no stone to disambiguate, `GetMegaStoneForBattler()` (`src/battle_util.c`) picks the X/Y form by the battler's stats — physical (X) when Attack ≥ Sp. Atk, special (Y) otherwise, so a tie defaults to X — by feeding the stat-appropriate stone into the existing item-driven form-change machinery (so eligibility, the form change, and the fork Speed-Boost AI override all resolve through one path). Only Charizard and Mewtwo have X/Y forms. **(3) Item-free Z derivation:** any move becomes its signature Z-Move (matched on species+move) or its type-based Z-Move. **(4) Picker:** the candidate gimmicks per battler are tracked as a set (`gimmick.gimmickCandidates[]`) alongside the selected one (`usableGimmick`); during move selection **Start cycles** through every gimmick the mon can currently use, then off (`CycleGimmickSelection`/`RefreshGimmickTriggerSprite`, `src/battle_gimmick.c`). **(5) Restrictions are unchanged** — one gimmick per mon and once per type per trainer fall out of the existing `GetActiveGimmick`/`HasTrainerUsedGimmick` checks, so a trainer can still use e.g. one Mega and one Z across a battle but each mon picks one. A **transformed mon (Ditto/Imposter) cannot Mega Evolve** (`CanMegaEvolve` mirrors `CanBattlerFormChange`'s `B_TRANSFORM_FORM_CHANGES >= GEN_5` transform guard): item-free Mega no longer reads the held stone, which previously let a transformed Ditto arm Mega only for the form change to be silently refused *after* the gimmick was spent; Z-Move/Dynamax/Tera stay available to a transformed Ditto. The picker also **always opens unarmed on the base moves** — `PlayerHandleChooseMove` clears `playerSelect` before the initial move-name paint, so a stale armed selection (e.g. Dynamax's Max-move names) can't leak into a fresh menu. **(6) AI uses everything:** `ShouldTrainerBattlerUseGimmick` returns TRUE for all gimmicks for AI battlers, so Tera/Dynamax are no longer opt-in and every eligible AI mon gimmicks; with the per-type-once tracking the team spreads its gimmicks across mons by priority (existing `ShouldUseZMove`/`DecideTerastal` reconsideration still applies). Makes trainer battles noticeably tougher by design. The Mega message reuses the item-neutral "fervent wish" line. **Known limitations:** Ultra Burst (Necrozma) deliberately **out of scope** — it still requires holding Ultranecrozium Z; the AI gimmick choice is highest-priority-available, not situationally scored; the extended frontier roster still carries its (now inert) Mega Stones / Z-Crystals as held items — re-itemizing those tuned sets is left as balance work (`species_tiers.c`), and the draft restriction `TeamHasGimmickItemConflict` is neutralized under the flag. Registered in the runtime config system so tests flip it with `WITH_CONFIG`; the test baseline forces it off. **Enabled (`TRUE`).** Covered by `test/fork/free_gimmicks.c`. |
| Color-blind HP bar | `COLOR_BLIND` | `config/accessibility.h` | ✅ | First of a planned accessibility set. Recolors the in-battle HP bar's healthy (>50%) band from green to the EXP bar's blue, so the bands no longer put the confusable colors at the extremes for red-green color blindness (stock ramp is green→yellow→red; new is blue→yellow→red). 2-entry palette swap on `TAG_HEALTHBAR_PAL` entries 10–11 in `ApplyHealthbarColorBlindPalette()` (`src/battle_gfx_sfx_util.c`); the yellow/red bands and the EXP bar are untouched, no new graphics. **Enabled (`TRUE`) in this fork.** Battle bar only for now; the party-menu/summary HP bars use a different (palette-by-index) mechanism and still follow stock colors. |
| Clean battle healthboxes | `B_CLEAN_HEALTHBOX` | `config/battle.h`, `src/graphics.c`, `src/battle_interface.c` | ✅ | When `TRUE`, declutters the battle healthboxes two ways. **(1) Corner pointer/tail removed:** the dark triangle on each box corner that points at the mon (in doubles it overlaps the on-healthbox type icons from `B_SHOW_TYPES`). It's **baked into the box sprite art** (the bar "frame end" tiles are the box's right *border*, not the tail), so the flag swaps `gHealthbox{Singles,Doubles}{Player,Opponent}Gfx` for fork-owned `*_notail.png` variants via `#if` in `src/graphics.c`. Each tail was edited out by hand against the pixel maps — doubles corners reconstructed by mirroring the box's clean opposite corner; player/opponent tails cleared with the border/HP bar left intact. **(2) Singles EXP bar removed:** the singles **player** box's EXP bar + its wrap-around frame (which also crowded the type icons) is gone — the `*_notail.png` for that box has a clean rounded bottom (top border mirrored down) and no EXP frame, and `UpdateHealthboxAttribute()` skips drawing the EXP bar fill under the flag (`src/battle_interface.c`). HP bar, level/HP text, status, and nick are untouched everywhere. **Enabled (`TRUE`) in this fork.** Vanilla art/behavior fully preserved at `FALSE`. |
| Illusion-safe effectiveness readout | `B_SHOW_EFFECTIVENESS` (fix; no new flag) | `src/battle_controller_player.c` | ✅ | Stops the move-select type-effectiveness indicator (`B_SHOW_EFFECTIVENESS = ALWAYS` in this fork) from **leaking an Illusion disguise**: against a foe under an active Illusion, `CheckTypeEffectiveness` would call the real damage calc and surface e.g. Zoroark's Dark weaknesses through the disguise. The fork instead computes the readout from the **apparent (illusion) species' base types** via `CalcIllusionTypeEffectiveness` (`GetIllusionMonSpecies` + the public `GetTypeModifier`, mirroring upstream's `TryNoticeIllusionInTypeEffectiveness` "presumed" calc), falling back to the true calc once the Illusion is broken or absent. Damaging moves only — status moves read NORMAL on the standard path and can't reveal anything. **Known limitation:** pure type-chart only, so it deliberately ignores the hidden mon's ability/item and move-specific overrides (Freeze-Dry, Flying Press) — an acceptable trade for never exposing the real typing. `FORK:`-tagged. **Enabled (`TRUE`).** |

Legend: ✅ done · ⚠️ partial / has known limitations.

### Determinism (`DETERMINISTIC_*`)

An ongoing project to remove as much RNG from the game as possible, one source
at a time, so outcomes follow from player choices and battle state rather than
luck. Each source gets its own flag in `config/deterministic.h` (`FALSE` = stock
behavior); the fork enables them as they mature. As random upsides are removed,
the plan is to introduce compensating systems so battles stay balanced rather
than simply harder or easier. See **[`DETERMINISM.md`](DETERMINISM.md)** for the
full rationale and per-flag breakdown.

**Design intent — this is a single-player game.** These changes are *not* a
competitive-meta rebalance; there's no human opponent, so multiplayer concerns
(how a move like Scald shifts a metagame, EV-neutral tweaks, etc.) are out of
scope. What's being protected is the **single player's experience against the
AI**, above all in the Battle Frontier facilities, where the draw is sustaining a
**long win streak on skill and Pokémon knowledge**. In stock play a strong run
can be cut short by pure hax with no counterplay — a surprise crit, a Quick Claw
flip into a Sheer Cold OHKO, a flinch you never act through. Determinism removes
the unlucky *and* the lucky variance from that loop, so a streak comes down to
decisions rather than dice; the `BUFF_*` line then compensates for the lucky
breaks the player gives up.

Flags so far: `DETERMINISTIC_CRITICAL_HITS`,
`DETERMINISTIC_DAMAGE`, `DETERMINISTIC_PARALYSIS`, `DETERMINISTIC_FLINCH`,
`DETERMINISTIC_ADDITIONAL_EFFECTS`, `DETERMINISTIC_HOLD_EFFECTS`,
`DETERMINISTIC_ACCURACY_EVASION`, `DETERMINISTIC_ABILITIES`,
`DETERMINISTIC_STATUS`, and `DETERMINISTIC_MOVE_RESULTS` (all enabled — see
table). The general shape
is that a *random upside* (a lucky crit/burn/flinch) is swapped for something the
player can read off the board: a guaranteed-or-impossible outcome, or one gated
on a legible condition (super effective, STAB, "did they flinch last turn"). The
battle AI is taught each new rule so it keeps valuing moves by what will actually
happen.

**Test harness.** Each `DETERMINISTIC_*` flag is registered into the existing
runtime config system (`DETERMINISTIC_CONFIG_DEFINITIONS` in
`include/constants/config_changes.h`), so engine code reads it via
`GetConfig(DETERMINISTIC_X)` and battle tests toggle it per-test with
`WITH_CONFIG(DETERMINISTIC_X, TRUE)`. The `#define`s in `config/deterministic.h`
remain the production defaults (on in the shipped ROM); the per-test baseline
forces every flag **off** (`TestInitConfigData`), so the entire inherited test
suite keeps running against stock behavior untouched, and only the dedicated
`test/fork/deterministic_*.c` files opt in. This means **adding a new
determinism flag costs one `#define` + one line in
`DETERMINISTIC_CONFIG_DEFINITIONS` + swapping its consumption site(s) to
`GetConfig` — no scattering `#if` guards across the upstream suite.**

### Balance / buffs (`BUFF_*`)

An ongoing project to rebalance items and other game functionality, usually as
compensation for other changes we make (e.g. the `DETERMINISTIC_*` project trades
random upsides away, so some items get buffed to keep battles balanced). Each
tweak gets its own flag in `config/buff.h` (`FALSE` = stock behavior); the fork
enables them as they mature. Flags so far: `BUFF_SHELL_BELL` and
`BUFF_LEECH_SEED` (both enabled — see table).

`BUFF_*` flags ride the **same runtime config system and test harness** as the
`DETERMINISTIC_*` flags above (`BUFF_CONFIG_DEFINITIONS` in
`include/constants/config_changes.h`): engine code reads each toggle via
`GetConfig(BUFF_X)`, battle tests opt in with `WITH_CONFIG(BUFF_X, TRUE)`, and the
per-test baseline forces every flag **off** (`TestInitConfigData`) so the
inherited suite keeps asserting stock behavior. Where a buff has a *magnitude*
(not just on/off), the toggle is registered but the magnitude is a plain
compile-time constant alongside it — e.g. `BUFF_SHELL_BELL` (registered toggle) +
`BUFF_SHELL_BELL_DENOMINATOR` (tuning), mirroring how `DETERMINISTIC_DAMAGE` pairs
its toggle with `BASE_PERCENT`/`TURN_INCREMENT`. This keeps the boolean test
baseline uniform (`force off` = stock) while leaving the numbers tunable in one
fork-owned file.

## Known quirks / future work

- **Forced Lv100 leaves dual record tracking (`B_FRONTIER_FORCE_LVL_100`).** With
  only Open Level in use, the always-empty "Lv 50" block is dropped from facility
  record displays. The **Battle Factory** and **Battle Tower** results windows now
  do this (Open Level streak only), gated on `B_FRONTIER_FORCE_LVL_100` in
  `ShowFactoryResultsWindow` / `ShowTowerResultsWindow` (`src/frontier_util.c`).
  The remaining facilities' record windows still show both blocks — same treatment
  to follow. The save block also still persists Lv50 records that can no longer
  change — candidate to drop for space if needed.
- **Battle Dome at 6v6.** See the 6v6 Factory row: the Dome's fixed 3-mon
  coordinate tables aren't generalized yet, so its layout is wrong at 6 mons.
- **The Battle Factory and Battle Tower are wired up so far.** `B_FRONTIER_ENDLESS`
  is a Frontier-wide flag; the Battle Factory and the Battle Tower (Singles +
  Doubles) implement the endless flow (per-win BP scaling, "rest to leave /
  resume", Brain-at-50/100). The Tower additionally draws opponents from the
  competitive roster and disables Multi/Link (see its row). The other facilities
  still run their vanilla once-per-challenge flow until they are converted.
  Planned next: extend it to the rest, Tower opponent tier-gating + hard AI, and
  boss battles every 10 challenges (the Frontier Brain is the current stand-in).
  **Converting a facility follows a repeatable pattern** — the step-by-step
  checklist, shared infrastructure to reuse, the rental-vs-own-party split, and
  the footguns we hit (the `1`/`0` flag gotcha, charmap chars, `make release`,
  empty rental backups) are written up in
  [`FRONTIER_ENDLESS.md`](FRONTIER_ENDLESS.md). Read it before doing Palace/Arena/etc.
- **Bumped `FRONTIER_STAGES_PER_CHALLENGE` (7 → 10) is global.** Per review, the
  shared constant was changed rather than adding a Factory-specific one, so the
  other facilities now see 10-stage challenges too. Their fixed-size layout
  tables (e.g. Battle Pyramid floor/pickup offsets, floor-name arrays) still have
  7 entries, so those facilities are visually/functionally off until generalized
  — accepted for now, to be fixed alongside their endless conversion.
- **Dead "won challenge" lobby path.** With `B_FRONTIER_ENDLESS` on, the lobby's
  `CHALLENGE_STATUS_WON` handler is no longer reached by new runs (it only ever
  fires for a save left mid-WON by a pre-update build, or with the flag off).
  Kept for that compatibility; a later cleanup can remove it.

## CI / infrastructure

- **Hardened `Install binutils` CI step.** Upstream's CI installs the GBA
  toolchain with an inline `apt-get` step copy-pasted into each job, which hung
  intermittently on dpkg-lock/mirror stalls (retrying "fixed" it). We extracted
  it into a fork-owned composite action,
  [`.github/actions/install-binutils`](../.github/actions/install-binutils/action.yml),
  that (1) **caches the ~107 MB of toolchain `.deb`s** so jobs install from
  GitHub's cache instead of the intermittently-slow Ubuntu mirror — the actual
  root cause — and (2) wraps each apt invocation in a per-attempt `timeout` and
  retries with backoff, so a stalled mirror/dpkg-lock is killed and
  **auto-retried in-job** (no manual workflow restart), failing only after 3
  attempts exhaust. A generous `timeout-minutes` backstop on each call site is
  the outer safety net. Every CI job (`build.yml`) and the on-demand ROM build
  (`rom-artifact.yml`) call it via `uses:`. The `uses:` call sites in `build.yml`
  are tagged `FORK:` (it's upstream-owned); on a merge conflict, keep our
  `uses:` + `timeout-minutes` and port any upstream apt-package change into the
  action's package list rather than re-inlining the step.

## Conventions

- Intentional divergences from upstream inside upstream-owned files are tagged
  `FORK:` (greppable: `grep -rn "FORK:" src include .github`).
- Prefer adding behavior behind a config flag in `include/config/*.h` over
  patching core logic, so changes land in files we own and survive upstream
  syncs cleanly. See `CLAUDE.md` for the full rationale.
