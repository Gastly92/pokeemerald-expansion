name: Labels

on:
  pull_request:
    types: [opened, synchronize, labeled, unlabeled]
  pull_request_review:
    types: [submitted]

jobs:
  label:
    if: ${{ github.actor != 'allcontributors[bot]' && github.event.review.state == 'approved' }}
    runs-on: ubuntu-26.04 # FORK: upstream uses ubuntu-latest; pinned ahead of its Oct 2026 move to 26.04. Taking upstream's line on conflict is fine once that move has happened.
    steps:
      - name: check labels
        uses: mheap/github-action-required-labels@v5
        with:
          mode: minimum
          count: 1
          labels: |
            General
            General Merge
            category: ability
            category: battle-ai
            category: battle-mechanic
            category: battle-tests
            category: items
            category: move animation
            category: move effect
            category: overworld
            category: pokemon
            category: sprite-issue
            type: documentation
  allcontributors:
    if: github.actor == 'allcontributors[bot]'
    runs-on: ubuntu-26.04 # FORK: upstream uses ubuntu-latest; pinned ahead of its Oct 2026 move to 26.04. Taking upstream's line on conflict is fine once that move has happened.
    needs: []
    steps:
      - name: Automatically pass for allcontributors
        run: echo "CI automatically passes for allcontributors" && exit 0

